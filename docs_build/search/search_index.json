{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Reaper","text":"<p>Librer\u00eda Python para extraer datos estructurados de Facebook e Instagram. Versi\u00f3n 0.3.0 \u2014 Python 3.11+</p> <p>Reaper convierte URLs de redes sociales en diccionarios Python listos para procesar. Soporta posts, reels, v\u00eddeos, fotos, grupos y perfiles de Facebook, y posts y reels de Instagram. Desde la versi\u00f3n 0.2.0 tambi\u00e9n gestiona sesiones autenticadas con rotaci\u00f3n inteligente de cuentas. Desde la versi\u00f3n 0.3.0 tambi\u00e9n incluye acciones de escritura (<code>ActionManager</code>): publicar posts (texto e im\u00e1genes), compartir a grupos, comentar y dar \"Me gusta\" desde una sesi\u00f3n autenticada.</p>"},{"location":"#instalacion","title":"Instalaci\u00f3n","text":"<pre><code>pip install .\nplaywright install firefox\n</code></pre>"},{"location":"#uso-minimo","title":"Uso m\u00ednimo","text":"<pre><code>import asyncio\nfrom reaper import scrape\n\nresult = asyncio.run(scrape(\"https://www.facebook.com/reel/816043001524221\"))\n\nprint(result[\"author\"][\"name\"])\nprint(result[\"text\"])\nprint(f\"Reacciones: {result['reaction_count']}\")\n</code></pre>"},{"location":"#uso-con-cuentas-autenticadas","title":"Uso con cuentas autenticadas","text":"<pre><code>import asyncio\nfrom reaper import scrape, AccountManager\nfrom reaper.auth.login import run_login_new_account\n\nasync def main():\n    manager = AccountManager()\n\n    # Primera vez: registrar una cuenta (abre Firefox para login manual)\n    await run_login_new_account(manager, \"facebook\", \"mi_usuario@gmail.com\")\n\n    # Scraping usando la sesi\u00f3n autenticada con rotaci\u00f3n autom\u00e1tica\n    result = await scrape(\n        \"https://www.facebook.com/reel/816043001524221\",\n        account_manager=manager,\n    )\n    print(result[\"author\"][\"name\"])\n\nasyncio.run(main())\n</code></pre>"},{"location":"#lo-que-puede-hacer","title":"Lo que puede hacer","text":"<ul> <li>Extraer posts, reels, v\u00eddeos, fotos, grupos y perfiles de Facebook.</li> <li>Extraer posts y reels de Instagram.</li> <li>Gestionar m\u00faltiples cuentas autenticadas con rotaci\u00f3n inteligente.</li> <li>Escribir acciones autenticadas: posts, comentarios, likes y compartir a grupos.</li> <li>Almacenar cuentas y sesiones en local, PostgreSQL o MongoDB v\u00eda <code>reaper.toml</code>.</li> <li>Trabajar con proxies para anonimidad o distribuci\u00f3n geogr\u00e1fica.</li> <li>Guardar artefactos de debug (HTML, tr\u00e1fico GraphQL) para diagn\u00f3stico offline.</li> <li>Serializar el resultado como JSON con un solo par\u00e1metro (<code>default=str</code>).</li> </ul>"},{"location":"#lo-que-no-hace","title":"Lo que no hace","text":"<ul> <li>No accede a mensajes privados ni contenido de cuentas de terceros.</li> <li>No automatiza el proceso de login (siempre es manual por dise\u00f1o).</li> <li>No garantiza funcionamiento si Facebook/Instagram cambia su estructura interna.</li> <li>No gestiona CAPTCHAs visuales ni verificaciones de seguridad de plataforma.</li> <li>Las acciones de escritura solo cubren Facebook (no Instagram) y requieren una   cuenta autenticada; no garantiza que los selectores de UI sigan funcionando   ante cambios del DOM.</li> </ul>"},{"location":"#estructura-del-proyecto","title":"Estructura del proyecto","text":"<pre><code>src/reaper/\n\u251c\u2500\u2500 __init__.py          API p\u00fablica: scrape(), Reaper, AccountManager, ActionManager\n\u251c\u2500\u2500 cli.py               Comando reaper en la terminal\n\u251c\u2500\u2500 config.py            ScraperConfig \u2014 objeto de configuraci\u00f3n central\n\u251c\u2500\u2500 core.py              Reaper \u2014 orquestador principal\n\u251c\u2500\u2500 auth/                Sistema de cuentas autenticadas (v0.2.0)\n\u2502   \u251c\u2500\u2500 models.py        AccountProfile, AccountActivity, AccountStatus\n\u2502   \u251c\u2500\u2500 account_manager.py  Gestor central de cuentas\n\u2502   \u251c\u2500\u2500 rotator.py       Algoritmo de rotaci\u00f3n por score\n\u2502   \u251c\u2500\u2500 login.py         Flujo interactivo de login\n\u2502   \u2514\u2500\u2500 storage/         Backends de persistencia (local/PG/Mongo)\n\u251c\u2500\u2500 actions/             Acciones de escritura autenticadas (v0.3.0)\n\u2502   \u251c\u2500\u2500 manager.py       ActionManager \u2014 orquestador de posts/likes/comentarios\n\u2502   \u251c\u2500\u2500 actor.py         SessionActor \u2014 sesiones autenticadas con Camoufox\n\u2502   \u251c\u2500\u2500 models.py        ActionType, ActionResult, ActionError\n\u2502   \u251c\u2500\u2500 facebook/        Flujos de UI y selectores por plataforma\n\u2502   \u2514\u2500\u2500 storage/         Persistencia de resultados de acciones (local/PG/Mongo)\n\u251c\u2500\u2500 network/             Playwright + captura de tr\u00e1fico (ContentFetcher)\n\u251c\u2500\u2500 anti_detection/      Fingerprint, stealth JS y comportamiento humano\n\u251c\u2500\u2500 scrapers/            Orquestadores por plataforma\n\u251c\u2500\u2500 parsers/             Extracci\u00f3n de datos del HTML/GraphQL\n\u2514\u2500\u2500 utils/               Utilidades compartidas\n</code></pre>"},{"location":"anti_scraping/","title":"Anti-Detection Module \u2014 Documentaci\u00f3n T\u00e9cnica","text":"<p>Para qui\u00e9n es este documento: Desarrolladores que consumen o mantienen el m\u00f3dulo <code>anti_detection</code> integrado en <code>ContentFetcher</code>, sin conocimientos previos de sistemas anti-bot.</p>"},{"location":"anti_scraping/#indice","title":"\u00cdndice","text":"<ol> <li>\u00bfPor qu\u00e9 existe este m\u00f3dulo?</li> <li>C\u00f3mo detectan los sitios web a los bots</li> <li>Arquitectura del m\u00f3dulo</li> <li>Capa 1 \u2014 Fingerprint de navegador</li> <li>Capa 2 \u2014 Scripts de evasi\u00f3n JavaScript</li> <li>Capa 3 \u2014 Comportamiento humano</li> <li>Integraci\u00f3n en ContentFetcher</li> <li>Justificaci\u00f3n de todos los valores de timing</li> <li>Configuraci\u00f3n y casos de uso</li> <li>Glosario</li> </ol>"},{"location":"anti_scraping/#1-por-que-existe-este-modulo","title":"1. \u00bfPor qu\u00e9 existe este m\u00f3dulo?","text":"<p>Cuando un programa abre un navegador para leer una p\u00e1gina web, deja huellas que los sistemas anti-bot de sitios como Facebook, Instagram o LinkedIn pueden detectar en milisegundos. Estas huellas no son errores de programaci\u00f3n: son consecuencias inevitables de c\u00f3mo funcionan los navegadores automatizados.</p> <p>Sin contramedidas, un scraper es detectado y bloqueado t\u00edpicamente dentro de las primeras peticiones. El m\u00f3dulo <code>anti_detection</code> implementa tres capas de defensa que trabajan en conjunto para que el navegador automatizado sea indistinguible de un usuario humano real.</p>"},{"location":"anti_scraping/#2-como-detectan-los-sitios-web-a-los-bots","title":"2. C\u00f3mo detectan los sitios web a los bots","text":"<p>Antes de entender las soluciones hay que entender el problema. Los sistemas anti-bot (Akamai, DataDome, PerimeterX, Cloudflare Bot Management) aplican detecci\u00f3n en tres niveles simult\u00e1neos:</p>"},{"location":"anti_scraping/#21-fingerprinting-del-navegador","title":"2.1 Fingerprinting del navegador","text":"<p>El fingerprinting es el proceso de identificar de forma \u00fanica a un visitante analizando las propiedades t\u00e9cnicas de su navegador. No requiere cookies ni IP: basta con leer propiedades JavaScript que el navegador expone.</p> <p>Cuando un navegador normal (que usa un humano) carga una p\u00e1gina, esas propiedades tienen valores coherentes y realistas:</p> <pre><code>navigator.webdriver   \u2192 undefined  (ning\u00fan humano usa webdriver)\nnavigator.plugins     \u2192 [PDF Viewer, Chrome PDF Plugin, ...]  (plugins reales instalados)\nnavigator.platform    \u2192 \"Win32\"    (coherente con el User-Agent de Windows)\nWebGL renderer        \u2192 \"ANGLE (NVIDIA, GeForce GTX 1660...)\"  (GPU real del sistema)\nscreen.outerHeight    \u2192 1165       (viewport + barra de Chrome \u2248 85px)\n</code></pre> <p>Cuando Playwright lanza un navegador sin configuraci\u00f3n especial, esas mismas propiedades tienen valores que delatan la automatizaci\u00f3n:</p> <pre><code>navigator.webdriver   \u2192 true       \u2190 se\u00f1al m\u00e1s obvia de todas\nnavigator.plugins     \u2192 []         \u2190 array vac\u00edo = headless confirmado\nnavigator.platform    \u2192 \"Win32\"    \u2190 pero el UA dice Linux, inconsistencia detectada\nWebGL renderer        \u2192 \"SwiftShader\" \u2190 renderizador de software, no existe en PCs reales\nscreen.outerHeight    \u2192 768        \u2190 igual que innerHeight, imposible en navegador real\n</code></pre>"},{"location":"anti_scraping/#22-analisis-de-comportamiento","title":"2.2 An\u00e1lisis de comportamiento","text":"<p>Los sistemas modernos graban y analizan todos los eventos de rat\u00f3n, teclado y scroll durante la sesi\u00f3n. Un bot genera patrones matem\u00e1ticamente imposibles en humanos:</p> <ul> <li>Rat\u00f3n: el rat\u00f3n se mueve en l\u00ednea recta perfecta de A a B, o no se mueve en absoluto.</li> <li>Scroll: desplazamiento en m\u00faltiplos exactos de N p\u00edxeles, a intervalos constantes de exactamente 1500ms.</li> <li>Timing: el tiempo entre la carga de la p\u00e1gina y la primera interacci\u00f3n es siempre exactamente el mismo.</li> <li>Inm\u00f3vil: el rat\u00f3n permanece perfectamente quieto durante minutos mientras el scraper espera.</li> </ul> <p>Ning\u00fan humano real produce estos patrones. El rat\u00f3n siempre tiembla ligeramente, el scroll tiene r\u00e1fagas irregulares, y los tiempos de reacci\u00f3n var\u00edan.</p>"},{"location":"anti_scraping/#23-senales-de-red-y-cabeceras-http","title":"2.3 Se\u00f1ales de red y cabeceras HTTP","text":"<p>Los navegadores reales env\u00edan cabeceras HTTP en un orden espec\u00edfico y con valores espec\u00edficos. Un scraper que no configura sus cabeceras puede ser detectado por:</p> <ul> <li>Ausencia del header <code>Accept-Language</code> o valores inusuales.</li> <li><code>User-Agent</code> de Windows pero <code>Accept-Language</code> configurado como <code>zh-CN</code> (inconsistencia geogr\u00e1fica).</li> <li>Ausencia de headers que todo navegador real incluye (<code>DNT</code>, <code>Upgrade-Insecure-Requests</code>).</li> <li>User-Agent que declara Chrome 120 pero el fingerprint de TLS corresponde a Firefox.</li> </ul>"},{"location":"anti_scraping/#3-arquitectura-del-modulo","title":"3. Arquitectura del m\u00f3dulo","text":"<p>El m\u00f3dulo <code>anti_detection</code> est\u00e1 estructurado en tres ficheros que implementan cada una de las capas de defensa:</p> <pre><code>anti_detection/\n\u251c\u2500\u2500 __init__.py          # API p\u00fablica del m\u00f3dulo\n\u251c\u2500\u2500 fingerprint.py       # Capa 1: generaci\u00f3n de perfiles de navegador\n\u251c\u2500\u2500 stealth_scripts.py   # Capa 2: parches JavaScript para evadir fingerprinting\n\u2514\u2500\u2500 human_behavior.py    # Capa 3: emulaci\u00f3n de comportamiento humano\n</code></pre> <p>Las tres capas son interdependientes y deben activarse juntas. Usar solo una o dos capas no es suficiente:</p> <ul> <li>Solo fingerprint sin stealth JS \u2192 el perfil es coherente pero <code>navigator.webdriver=true</code> lo delata de inmediato.</li> <li>Solo stealth JS sin comportamiento humano \u2192 pasa el fingerprint check pero el an\u00e1lisis de comportamiento lo detecta.</li> <li>Solo comportamiento humano sin fingerprint \u2192 el rat\u00f3n se mueve bien pero el UA inconsistente lo bloquea en la primera petici\u00f3n.</li> </ul>"},{"location":"anti_scraping/#4-capa-1-fingerprint-de-navegador","title":"4. Capa 1 \u2014 Fingerprint de navegador","text":"<p>Fichero: <code>anti_detection/fingerprint.py</code> Funci\u00f3n principal: <code>generate_fingerprint(browser_type: str) \u2192 BrowserFingerprint</code></p>"},{"location":"anti_scraping/#41-el-problema-de-la-coherencia","title":"4.1 El problema de la coherencia","text":"<p>El error m\u00e1s com\u00fan en scrapers que intentan evadir detecci\u00f3n es usar valores aleatorios sin relaci\u00f3n entre s\u00ed. Por ejemplo:</p> <pre><code>User-Agent: Mozilla/5.0 (Windows NT 10.0; Win64; x64) Chrome/124...  \u2190 dice Windows\nnavigator.platform: \"Linux x86_64\"                                     \u2190 pero dice Linux\nWebGL renderer: \"Apple M1\"                                             \u2190 y la GPU es de Mac\nAccept-Language: zh-CN                                                 \u2190 pero el locale es de NY\n</code></pre> <p>Cada valor individualmente podr\u00eda parecer leg\u00edtimo, pero juntos forman un perfil que no existe en ning\u00fan ordenador real. Los sistemas anti-bot cruzan estas propiedades entre s\u00ed y detectan la incoherencia en milisegundos.</p>"},{"location":"anti_scraping/#42-la-solucion-perfiles-de-plataforma","title":"4.2 La soluci\u00f3n: perfiles de plataforma","text":"<p>El m\u00f3dulo define tres <code>PlatformProfile</code> (Windows, macOS, Linux) donde todos los atributos son coherentes entre s\u00ed. Cada perfil agrupa:</p> Atributo Windows macOS Linux <code>navigator.platform</code> <code>Win32</code> <code>MacIntel</code> <code>Linux x86_64</code> User-Agents Chrome/Edge con <code>Windows NT 10.0</code> Chrome/Safari con <code>Macintosh; Intel Mac OS X</code> Chrome con <code>X11; Linux x86_64</code> WebGL renderers GPUs NVIDIA/Intel/AMD con DirectX GPUs Apple Silicon / Intel Iris Drivers Mesa / Nouveau Timezones <code>America/New_York</code>, <code>America/Los_Angeles</code> <code>America/New_York</code>, <code>Europe/London</code> <code>America/New_York</code>, <code>Europe/London</code> Viewports 1920\u00d71080, 1440\u00d7900, 1366\u00d7768 2560\u00d71600, 1440\u00d7900 1920\u00d71080, 1366\u00d7768 <p>Cuando se genera un fingerprint, se elige un perfil completo y todos los atributos se extraen de ese mismo perfil. El resultado es un navegador que dice ser Windows 10 con una GPU Intel, un viewport de 1440\u00d7900, en la zona horaria de Nueva York, con el locale en-US \u2014 un perfil que existe en millones de ordenadores reales.</p>"},{"location":"anti_scraping/#43-distribucion-probabilistica-de-plataformas","title":"4.3 Distribuci\u00f3n probabil\u00edstica de plataformas","text":"<p>Los perfiles no se eligen al azar uniforme. Se usan los pesos de mercado real:</p> <pre><code>_PROFILES_WEIGHTED = [\n    (_WINDOWS_PROFILE, 0.65),   # Windows: 65% del mercado desktop global\n    (_MACOS_PROFILE,   0.25),   # macOS:   25% (especialmente alto en usuarios de FB/IG)\n    (_LINUX_PROFILE,   0.10),   # Linux:   10%\n]\n</code></pre> <p>\u00bfPor qu\u00e9 importa esto? Si el 90% del tr\u00e1fico de un scraper proviene de perfiles Linux, y Linux representa el 2% del tr\u00e1fico real de Facebook, ese patr\u00f3n estad\u00edstico es una se\u00f1al de automatizaci\u00f3n detectable a nivel de poblaci\u00f3n de IPs.</p>"},{"location":"anti_scraping/#44-el-dataclass-browserfingerprint","title":"4.4 El dataclass <code>BrowserFingerprint</code>","text":"<p><code>generate_fingerprint()</code> devuelve un <code>BrowserFingerprint</code> con todos los atributos ya resueltos y listos para usar:</p> <pre><code>@dataclass\nclass BrowserFingerprint:\n    browser_type: str          # \"firefox\" | \"chromium\"\n    user_agent: str            # String UA completo\n    accept_language: str       # \"en-US,en;q=0.9\"\n    locale: str                # \"en-US\" (para Playwright context)\n    timezone_id: str           # \"America/New_York\" (IANA)\n    viewport: ViewportConfig   # Resoluci\u00f3n coherente con el OS\n    navigator_platform: str    # Valor de navigator.platform\n    hardware_concurrency: int  # N\u00facleos l\u00f3gicos (4, 8, 12, 16)\n    device_memory: int         # GB de RAM (4, 8, 16)\n    webgl_vendor: str          # Fabricante GPU\n    webgl_renderer: str        # Modelo GPU con driver espec\u00edfico\n    stealth_js: str            # Script JS de evasi\u00f3n (ver Capa 2)\n    extra_headers: dict        # Headers HTTP adicionales\n</code></pre> <p>El m\u00e9todo <code>build_context_options(proxy=None)</code> convierte el fingerprint en el dict exacto que espera <code>browser.new_context()</code> de Playwright, incluyendo headers HTTP como <code>Accept</code>, <code>DNT</code>, <code>Connection</code>, y <code>Upgrade-Insecure-Requests</code> que un navegador real siempre env\u00eda.</p>"},{"location":"anti_scraping/#5-capa-2-scripts-de-evasion-javascript","title":"5. Capa 2 \u2014 Scripts de evasi\u00f3n JavaScript","text":"<p>Fichero: <code>anti_detection/stealth_scripts.py</code> Funci\u00f3n principal: <code>build_full_stealth_script(...) \u2192 str</code></p>"},{"location":"anti_scraping/#51-el-mecanismo-de-inyeccion","title":"5.1 El mecanismo de inyecci\u00f3n","text":"<p>Los parches JavaScript se inyectan en la p\u00e1gina mediante <code>page.add_init_script()</code> de Playwright. Este mecanismo es cr\u00edtico: el script se ejecuta antes de que cargue cualquier JavaScript de la p\u00e1gina, incluyendo los scripts de detecci\u00f3n del servidor. Si el script de parche llegara despu\u00e9s, el script de detecci\u00f3n ya habr\u00eda le\u00eddo los valores sin parchear.</p> <pre><code>Orden de ejecuci\u00f3n:\n1. Playwright abre el frame vac\u00edo\n2. \u2192 stealth_js se ejecuta (parcha navigator, WebGL, Canvas, etc.)\n3. \u2192 scripts de la p\u00e1gina se ejecutan (el fingerprinter ya ve los valores parchados)\n4. \u2192 la p\u00e1gina se renderiza\n</code></pre>"},{"location":"anti_scraping/#52-parches-incluidos-y-por-que-cada-uno-es-necesario","title":"5.2 Parches incluidos y por qu\u00e9 cada uno es necesario","text":""},{"location":"anti_scraping/#navigatorwebdriver-undefined","title":"<code>navigator.webdriver = undefined</code>","text":"<p>La se\u00f1al m\u00e1s obvia de automatizaci\u00f3n. Playwright (y Selenium, Puppeteer) establecen esta propiedad a <code>true</code> autom\u00e1ticamente. Todos los sistemas anti-bot comprueban esta propiedad como primer filtro. El parche usa <code>configurable: false</code> para evitar que scripts de la p\u00e1gina la restauren.</p>"},{"location":"anti_scraping/#navigatorplugins-diferente-para-firefox-y-chromium","title":"<code>navigator.plugins</code> (diferente para Firefox y Chromium)","text":"<p>Un navegador headless devuelve un array vac\u00edo de plugins. Un navegador real tiene al menos el visor de PDF instalado. El parche simula los plugins espec\u00edficos de cada motor: - Firefox: solo <code>PDF Viewer</code> (Firefox real tiene muy pocos plugins). - Chromium: <code>Chrome PDF Plugin</code>, <code>Chrome PDF Viewer</code> y <code>Native Client</code> (presentes en Chrome real).</p> <p>La diferenciaci\u00f3n por motor es importante: un Chrome con plugins de Firefox ser\u00eda otra incoherencia detectable.</p>"},{"location":"anti_scraping/#navigatorplatform-hardwareconcurrency-devicememory","title":"<code>navigator.platform</code>, <code>hardwareConcurrency</code>, <code>deviceMemory</code>","text":"<p>Deben coincidir con el OS declarado en el User-Agent y con los valores del perfil de plataforma. Un headless sin configurar devuelve <code>hardwareConcurrency = 1</code> (solo tiene 1 vCPU en CI/CD), mientras que cualquier PC de usuario real tiene 4 o m\u00e1s. <code>deviceMemory</code> en headless suele ser <code>undefined</code> en Firefox, ausencia que tambi\u00e9n se detecta.</p>"},{"location":"anti_scraping/#navigatorconnection","title":"<code>navigator.connection</code>","text":"<p>La API de informaci\u00f3n de red (<code>NetworkInformation</code>) est\u00e1 disponible en Chrome real pero ausente en entornos headless. Su ausencia es una se\u00f1al de bot. El parche simula una conexi\u00f3n 4G t\u00edpica con latencia realista (<code>rtt: 50ms</code>, <code>downlink: 10Mbps</code>).</p>"},{"location":"anti_scraping/#canvas-noise","title":"Canvas noise","text":"<p>Los sistemas de fingerprinting renderizan texto o formas en un canvas HTML5 y hacen hash del resultado. Este hash es reproducible: el mismo navegador en el mismo OS produce siempre el mismo hash, lo que permite identificar sesiones de forma persistente incluso sin cookies.</p> <p>El parche intercepta <code>getImageData()</code> y <code>toDataURL()</code> e introduce ruido de \u00b11-2 bits por p\u00edxel en los valores RGBA. El ruido es: - Invisible al ojo humano (cambio de 1 en 255). - Destructivo para el hash (un solo bit diferente produce un hash completamente distinto). - Diferente en cada sesi\u00f3n (usa <code>Math.random()</code> con seed por frame), evitando que el mismo ruido fijo se convierta en el nuevo fingerprint.</p>"},{"location":"anti_scraping/#webgl-vendorrenderer","title":"WebGL vendor/renderer","text":"<p>Similar al canvas pero para gr\u00e1ficos 3D. Los sistemas anti-bot renderizan una escena WebGL y hacen hash del resultado, adem\u00e1s de leer directamente <code>UNMASKED_VENDOR_WEBGL</code> y <code>UNMASKED_RENDERER_WEBGL</code>. El parche reemplaza ambas cadenas con los valores del perfil de hardware elegido, coherentes con el OS y el UA.</p>"},{"location":"anti_scraping/#audio-context-noise","title":"Audio Context noise","text":"<p>Los osciladores de la Web Audio API tambi\u00e9n producen hashes reproducibles. El parche intercepta <code>getChannelData()</code> e introduce el mismo tipo de ruido m\u00ednimo que en Canvas.</p>"},{"location":"anti_scraping/#webrtc-ip-leak","title":"WebRTC IP leak","text":"<p>WebRTC (el protocolo de comunicaci\u00f3n en tiempo real del navegador) puede revelar la IP real del host aunque se est\u00e9 usando un proxy, mediante un mecanismo llamado ICE candidates. El parche elimina todos los ICE servers de las configuraciones de <code>RTCPeerConnection</code>, forzando que solo se generen candidates de tipo <code>host</code> que usan la IP del proxy.</p>"},{"location":"anti_scraping/#screenouterwidthouterheight","title":"<code>screen.outerWidth/outerHeight</code>","text":"<p>En un navegador real, <code>window.outerHeight</code> es siempre mayor que <code>window.innerHeight</code> porque incluye la barra de tabs y la barra de herramientas (~85px en Chrome). En headless, <code>outerHeight == innerHeight</code>. El parche a\u00f1ade estos 85px de \"chrome frame\" para replicar el comportamiento real.</p>"},{"location":"anti_scraping/#permissions-api","title":"Permissions API","text":"<p>En Chromium headless, <code>navigator.permissions.query({name: 'notifications'})</code> devuelve <code>'denied'</code>. En Chrome real para un usuario que nunca ha respondido a la pregunta, devuelve <code>'default'</code>. El parche corrige este comportamiento.</p>"},{"location":"anti_scraping/#performancenow-precision","title":"<code>performance.now()</code> precision","text":"<p>Los navegadores post-Spectre (Chrome 68+, Firefox 60+) reducen la precisi\u00f3n de <code>performance.now()</code> a 100 microsegundos para mitigar ataques de timing. Algunos builds headless no aplican esta reducci\u00f3n, lo que crea una se\u00f1al detectable. El parche fuerza el redondeo a 0.1ms.</p>"},{"location":"anti_scraping/#notificationpermission","title":"<code>Notification.permission</code>","text":"<p>Similar a la Permissions API: un usuario real que nunca ha respondido al popup de notificaciones tiene este valor en <code>'default'</code>, no en <code>'denied'</code>.</p>"},{"location":"anti_scraping/#propagacion-a-iframes","title":"Propagaci\u00f3n a iframes","text":"<p>Los fingerprinters avanzados crean un iframe invisible, leen <code>iframe.contentWindow.navigator.webdriver</code> (que no est\u00e1 parchado por los overrides del frame principal) y obtienen el valor sin parchear. El parche intercepta la creaci\u00f3n de elementos <code>&lt;iframe&gt;</code> y aplica el override de <code>webdriver</code> al contexto del iframe al cargarse.</p>"},{"location":"anti_scraping/#6-capa-3-comportamiento-humano","title":"6. Capa 3 \u2014 Comportamiento humano","text":"<p>Fichero: <code>anti_detection/human_behavior.py</code></p> <p>Esta capa resuelve la detecci\u00f3n por an\u00e1lisis de comportamiento. La premisa es que cualquier patr\u00f3n matem\u00e1ticamente perfecto es imposible en un humano real.</p>"},{"location":"anti_scraping/#61-distribuciones-estadisticas-usadas","title":"6.1 Distribuciones estad\u00edsticas usadas","text":"<p>Las funciones no usan <code>random.uniform()</code> puro porque produce distribuciones demasiado uniformes. Los humanos reales tienen distribuciones sesgadas:</p> <p><code>random.gauss(mu, sigma)</code> \u2014 distribuci\u00f3n normal: Modela tiempos de reacci\u00f3n humanos. La mayor\u00eda de las acciones ocurren cerca del valor medio, con desviaciones ocasionales en ambas direcciones. Se usa en <code>human_delay()</code> y <code>simulate_reading_pause()</code>.</p> <p><code>random.triangular(min, max, mode)</code> \u2014 distribuci\u00f3n triangular: Similar a la gaussiana pero con l\u00edmites estrictos. Se usa en <code>micro_delay()</code> para pausas entre teclas, donde el m\u00ednimo fisiol\u00f3gico (tiempo de reacci\u00f3n humano \u2248 50ms) no puede violarse.</p> <p><code>random.expovariate(lambda)</code> \u2014 distribuci\u00f3n exponencial: Modela eventos de \"cola larga\": la mayor\u00eda ocurren r\u00e1pido, pero ocasionalmente hay un evento muy largo (el usuario se distrae). Se suma a la gaussiana en <code>human_delay()</code>.</p>"},{"location":"anti_scraping/#62-human_delaymin_seconds-max_seconds","title":"6.2 <code>human_delay(min_seconds, max_seconds)</code>","text":"<pre><code>mid = (min_seconds + max_seconds) / 2.0\nsigma = (max_seconds - min_seconds) / 6.0\nbase = random.gauss(mid, sigma)\ndistraction = random.expovariate(5.0) * 0.5\ndelay = max(min_seconds, min(max_seconds * 1.5, base + distraction))\n</code></pre> <p>La gaussiana centra los delays en el punto medio del rango. La exponencial a\u00f1ade \"distracciones\" ocasionales donde el usuario tarda m\u00e1s de lo esperado. El resultado es un histograma de delays que se parece al de usuarios reales analizados en estudios de UX.</p>"},{"location":"anti_scraping/#63-micro_delaymin_ms-max_ms","title":"6.3 <code>micro_delay(min_ms, max_ms)</code>","text":"<p>Micro-pausas de 50-350ms entre acciones at\u00f3micas (una tecla, un movimiento de rat\u00f3n). El l\u00edmite inferior de 50ms es deliberado: el tiempo de reacci\u00f3n humano m\u00ednimo fisiol\u00f3gico es ~150ms, y el movimiento de los dedos entre teclas es de al menos 30-50ms. Un bot que teclea a intervalos de 10ms es instant\u00e1neamente detectable.</p>"},{"location":"anti_scraping/#64-human_move_topage-target_x-target_y-curva-de-bezier-cubica","title":"6.4 <code>human_move_to(page, target_x, target_y)</code> \u2014 curva de B\u00e9zier c\u00fabica","text":"<p>Los bots mueven el rat\u00f3n en l\u00ednea recta: <code>move(0,0) \u2192 move(500,300)</code> en un paso. Los humanos describen curvas suaves con aceleraci\u00f3n y desaceleraci\u00f3n.</p> <p>El movimiento se calcula como una curva de B\u00e9zier c\u00fabica entre el punto actual y el destino, con dos puntos de control aleatorios (\u00b140px de la l\u00ednea recta). La velocidad var\u00eda a lo largo de la curva usando la funci\u00f3n <code>sin(\u03c0\u00b7t)</code>: - Al inicio (t\u22480): <code>sin(0) = 0</code> \u2192 movimiento lento (el usuario apunta). - En el medio (t\u22480.5): <code>sin(\u03c0/2) = 1</code> \u2192 movimiento r\u00e1pido. - Al final (t\u22481): <code>sin(\u03c0) = 0</code> \u2192 movimiento lento (el usuario frena y apunta fino).</p> <p>Este patr\u00f3n de aceleraci\u00f3n-velocidad-desaceleraci\u00f3n es caracter\u00edstico del movimiento humano (Ley de Fitts) y extremadamente dif\u00edcil de producir accidentalmente en un bot.</p>"},{"location":"anti_scraping/#65-human_scrollpage-direction-amount-rafagas-irregulares","title":"6.5 <code>human_scroll(page, direction, amount)</code> \u2014 r\u00e1fagas irregulares","text":"<p>Un bot hace scroll con <code>page.mouse.wheel(0, 1000)</code> en un paso. Un humano arrastra la rueda del rat\u00f3n en r\u00e1fagas irregulares:</p> <pre><code>R\u00e1faga 1: 60px, 80px, 40px  \u2192 pausa 0.35s (leyendo)\nR\u00e1faga 2: 100px, 70px        \u2192 pausa 0.22s\nR\u00e1faga 3: 50px, 90px, 60px  \u2192 pausa 0.45s\n</code></pre> <p>La funci\u00f3n simula exactamente este patr\u00f3n: r\u00e1fagas de 60-180px con pasos internos de 20-60px, separadas por pausas de 0.15-0.60s.</p>"},{"location":"anti_scraping/#66-simulate_idlepage-duration_seconds-deriva-de-raton","title":"6.6 <code>simulate_idle(page, duration_seconds)</code> \u2014 deriva de rat\u00f3n","text":"<p>Un rat\u00f3n perfectamente inm\u00f3vil durante m\u00e1s de 2-3 segundos es una se\u00f1al de bot. Los sistemas de detecci\u00f3n monitorizan la actividad de rat\u00f3n y la ausencia total de movimiento es sospechosa.</p> <p><code>simulate_idle</code> mantiene el rat\u00f3n en movimiento con peque\u00f1as derivas gaussianas alrededor de una posici\u00f3n base (\u03c3=15px horizontal, \u03c3=10px vertical), con pausas de 0.4-1.2s entre micro-movimientos. El resultado es indistinguible del temblor natural del rat\u00f3n de un usuario leyendo.</p>"},{"location":"anti_scraping/#67-simulate_reading_pausepage-words_estimate","title":"6.7 <code>simulate_reading_pause(page, words_estimate)</code>","text":"<pre><code>reading_wpm = random.gauss(250, 40)   # 250 WPM \u00b1 40 (velocidad humana promedio)\nreading_seconds = max(1.0, (words_estimate / reading_wpm) * 60)\nawait simulate_idle(page, reading_seconds)\n</code></pre> <p>La velocidad de lectura humana promedio es de 200-300 WPM con desviaci\u00f3n est\u00e1ndar de ~40 WPM. La pausa se calcula proporcional al contenido visible y se ejecuta con <code>simulate_idle</code> (rat\u00f3n en movimiento, no quieto).</p> <p>Se usa en <code>_auto_scroll</code> cuando se detecta nuevo contenido cargado. La estimaci\u00f3n de palabras se deriva del delta de <code>scrollHeight</code>:</p> <pre><code>words_visible = max(20, height_delta // 15)\n</code></pre> <p>El heur\u00edstico <code>\u00f715</code> equivale a \"1 palabra cada 15px de contenido\". Un p\u00e1rrafo t\u00edpico ocupa 150-300px y tiene 20-50 palabras \u2192 ~1 palabra/15px. El <code>max(20, ...)</code> establece un piso para evitar pausas insignificantes cuando el delta es m\u00ednimo.</p>"},{"location":"anti_scraping/#68-simulate_distractionpage-y-simulate_page_focus_blurpage","title":"6.8 <code>simulate_distraction(page)</code> y <code>simulate_page_focus_blur(page)</code>","text":"<p><code>simulate_distraction</code>: mueve el rat\u00f3n a una esquina o borde de la pantalla y pausa 0.5-3s. Simula al usuario distray\u00e9ndose (mira el reloj, responde a alguien). Los sistemas de detecci\u00f3n que analizan heatmaps de rat\u00f3n esperan que estos movimientos ocurran ocasionalmente.</p> <p><code>simulate_page_focus_blur</code>: emite eventos <code>visibilitychange</code> que simulan que el usuario cambi\u00f3 de pesta\u00f1a y volvi\u00f3. Algunos trackers (Facebook Pixel, Google Analytics) monitorizan estos eventos. Un usuario que nunca pierde el foco de la pesta\u00f1a durante toda la sesi\u00f3n es estad\u00edsticamente an\u00f3malo.</p>"},{"location":"anti_scraping/#7-integracion-en-contentfetcher","title":"7. Integraci\u00f3n en ContentFetcher","text":""},{"location":"anti_scraping/#71-flujo-completo-con-anti-deteccion-activa","title":"7.1 Flujo completo con anti-detecci\u00f3n activa","text":"<pre><code>async with async_playwright() as pw:\n    \u2502\n    \u251c\u2500 _launch_browser(pw)\n    \u2502   \u2514\u2500\u2500 Firefox con firefox_user_prefs (sin telemetr\u00eda, sin Pocket)\n    \u2502\n    \u251c\u2500 generate_fingerprint(\"firefox\")\n    \u2502   \u2514\u2500\u2500 Elige perfil OS \u2192 extrae UA, viewport, WebGL, locale, timezone\n    \u2502       \u2514\u2500\u2500 Genera stealth_js con todos los valores del perfil\n    \u2502\n    \u251c\u2500 _create_context(browser, fingerprint=fingerprint)\n    \u2502   \u2514\u2500\u2500 fingerprint.build_context_options() \u2192 UA + locale + timezone +\n    \u2502       viewport + Accept-Language + Accept + DNT + otros headers\n    \u2502\n    \u251c\u2500 context.new_page()\n    \u2502\n    \u251c\u2500 page.add_init_script(fingerprint.stealth_js)   \u2190 ANTES de goto()\n    \u2502   \u2514\u2500\u2500 Parcha: webdriver, plugins, WebGL, canvas, audio, WebRTC,\n    \u2502       screen metrics, permissions, performance.now(), iframes\n    \u2502\n    \u251c\u2500 context.add_cookies(cookies)   [si sesi\u00f3n autenticada]\n    \u2502\n    \u251c\u2500 interceptor.attach(page)\n    \u2502\n    \u251c\u2500 _navigate(page)\n    \u2502   \u251c\u2500\u2500 page.goto(url, wait_until=\"networkidle\")\n    \u2502   \u2514\u2500\u2500 simulate_idle(page, 1.0\u20132.0s)   [+ 35% chance de focus/blur]\n    \u2502\n    \u251c\u2500 _auto_scroll(page)  [si auto_scroll=True]\n    \u2502   \u251c\u2500\u2500 Por cada iteraci\u00f3n:\n    \u2502   \u2502   \u251c\u2500\u2500 human_scroll(page, \"down\", SCROLL_DELTA)\n    \u2502   \u2502   \u251c\u2500\u2500 micro_delay(150\u2013500ms)\n    \u2502   \u2502   \u251c\u2500\u2500 [si nuevo contenido] simulate_reading_pause(page, words)\n    \u2502   \u2502   \u2514\u2500\u2500 [15% de probabilidad] simulate_distraction(page)\n    \u2502   \u2514\u2500\u2500 simulate_idle(page, 0.8\u20131.5s)   [final]\n    \u2502\n    \u251c\u2500 _infinity_scroll(page, interceptor)  [si infinity_scroll=True]\n    \u2502   \u251c\u2500\u2500 Por cada iteraci\u00f3n:\n    \u2502   \u2502   \u251c\u2500\u2500 human_scroll(page, \"down\", SCROLL_DELTA)\n    \u2502   \u2502   \u251c\u2500\u2500 human_delay(1.0\u20132.0s)\n    \u2502   \u2502   \u251c\u2500\u2500 [si actividad de red] simulate_idle(page, 1.0\u20132.0s)\n    \u2502   \u2502   \u251c\u2500\u2500 [si sin actividad x2] scroll agresivo + human_delay(2.0\u20134.0s)\n    \u2502   \u2502   \u251c\u2500\u2500 simulate_idle(page, 1.0\u20132.5s)\n    \u2502   \u2502   \u2514\u2500\u2500 [10% de probabilidad] simulate_distraction(page)\n    \u2502   \u2514\u2500\u2500 simulate_idle(page, 0.8\u20131.5s)   [final]\n    \u2502\n    \u2514\u2500 page.content() \u2192 html\n</code></pre>"},{"location":"anti_scraping/#72-por-que-el-orden-importa","title":"7.2 Por qu\u00e9 el orden importa","text":"<p><code>add_init_script</code> debe ir antes de <code>goto()</code>: Si se llamara despu\u00e9s, la p\u00e1gina ya habr\u00eda cargado y el script de detecci\u00f3n del servidor ya habr\u00eda le\u00eddo <code>navigator.webdriver = true</code>. El parche llegar\u00eda tarde.</p> <p><code>add_init_script</code> debe ir antes de <code>interceptor.attach()</code>: No es estrictamente necesario, pero mantener el orden \"configurar p\u00e1gina \u2192 adjuntar listeners \u2192 navegar\" reduce el riesgo de race conditions en el arranque.</p> <p>El fingerprint se genera antes de <code>new_context()</code>: Porque <code>build_context_options()</code> configura el UA, el viewport y los headers del contexto. Si se generara despu\u00e9s, el contexto ya estar\u00eda creado con valores por defecto.</p>"},{"location":"anti_scraping/#8-justificacion-de-todos-los-valores-de-timing","title":"8. Justificaci\u00f3n de todos los valores de timing","text":"<p>Esta secci\u00f3n documenta cada valor num\u00e9rico de timing presente en el c\u00f3digo y la raz\u00f3n espec\u00edfica de ese valor.</p>"},{"location":"anti_scraping/#81-browserconfigpage_load_wait-20","title":"8.1 <code>BrowserConfig.PAGE_LOAD_WAIT = 2.0</code>","text":"<p>Antes: <code>5.0</code> segundos fijo de <code>asyncio.sleep</code>. Ahora: l\u00edmite superior de <code>simulate_idle(page, random.uniform(1.0, 2.0))</code>.</p> <p>Raz\u00f3n del cambio: el <code>asyncio.sleep</code> fijo ten\u00eda dos problemas: 1. Dejaba el rat\u00f3n completamente quieto durante 5 segundos \u2192 se\u00f1al de bot. 2. Era un valor constante \u2192 patr\u00f3n de timing perfectamente regular entre sesiones.</p> <p><code>simulate_idle</code> resuelve ambos: mantiene el rat\u00f3n en movimiento y la duraci\u00f3n var\u00eda entre 1.0 y 2.0 segundos. El valor de <code>PAGE_LOAD_WAIT</code> pasa a ser el techo del rango, no un sleep fijo.</p> <p>\u00bfPor qu\u00e9 2.0s y no menos? <code>networkidle</code> en Playwright no garantiza que todo el JavaScript post-carga haya terminado (React hydration, lazy loading, frameworks SPA). 1-2 segundos de margen es el m\u00ednimo para que estos scripts completen su inicializaci\u00f3n antes de que el interceptor empiece a capturar tr\u00e1fico relevante.</p>"},{"location":"anti_scraping/#82-browserconfigscroll_wait_time-05","title":"8.2 <code>BrowserConfig.SCROLL_WAIT_TIME = 0.5</code>","text":"<p>Antes: <code>1.5</code> segundos de <code>asyncio.sleep</code> despu\u00e9s de cada <code>mouse.wheel</code>. Ahora: <code>micro_delay(150ms, SCROLL_WAIT_TIME * 1000 = 500ms)</code> despu\u00e9s de <code>human_scroll</code>.</p> <p>Raz\u00f3n del cambio: <code>human_scroll</code> ya incluye pausas internas entre r\u00e1fagas (0.15-0.60s), por lo que la pausa adicional solo necesita cubrir el tiempo de procesamiento del DOM tras el scroll. 150-500ms es suficiente para que el DOM actualice <code>scrollHeight</code> y el interceptor registre peticiones de red que el scroll pudo haber disparado.</p> <p>\u00bfPor qu\u00e9 no eliminarlo completamente? Necesitamos que <code>container.evaluate(\"el =&gt; el.scrollHeight\")</code> lea el valor ya actualizado. Sin ninguna pausa, podr\u00eda leer el valor antes de que el layout reflow complete.</p>"},{"location":"anti_scraping/#83-simulate_idle-en-_navigate-randomuniformpage_load_wait-05-page_load_wait-1020s","title":"8.3 <code>simulate_idle</code> en <code>_navigate</code>: <code>random.uniform(PAGE_LOAD_WAIT * 0.5, PAGE_LOAD_WAIT)</code> = 1.0\u20132.0s","text":"<p>El rango <code>[PAGE_LOAD_WAIT/2, PAGE_LOAD_WAIT]</code> = <code>[1.0, 2.0]</code> se calcul\u00f3 as\u00ed: - M\u00ednimo 1.0s: tiempo m\u00ednimo para que frameworks JavaScript (React, Vue, Angular) completen su render inicial. Valores menores producen capturas de HTML incompletas en SPAs. - M\u00e1ximo 2.0s: el <code>networkidle</code> ya esper\u00f3 a que la red estuviera inactiva; m\u00e1s de 2s extra de idle son desperdiciados para la mayor\u00eda de p\u00e1ginas.</p>"},{"location":"anti_scraping/#84-simulate_page_focus_blur-35-de-probabilidad","title":"8.4 <code>simulate_page_focus_blur</code>: 35% de probabilidad","text":"<pre><code>if random.random() &lt; 0.35:\n    await simulate_page_focus_blur(page)\n</code></pre> <p>\u00bfPor qu\u00e9 35% y no siempre? En el comportamiento real de usuarios en Facebook/Instagram, no todos los usuarios cambian de pesta\u00f1a en cada visita. Activarlo siempre crear\u00eda un patr\u00f3n regular tan sospechoso como no activarlo nunca. El 35% replica una frecuencia realista sin convertirse en un patr\u00f3n estad\u00edstico.</p>"},{"location":"anti_scraping/#85-simulate_distraction-en-_auto_scroll-15-por-iteracion","title":"8.5 <code>simulate_distraction</code> en <code>_auto_scroll</code>: 15% por iteraci\u00f3n","text":"<pre><code>if random.random() &lt; 0.15:\n    await simulate_distraction(page)\n</code></pre> <p>Un usuario que hace scroll en comentarios se distrae brevemente cada 6-7 interacciones en promedio. Con probabilidad del 15% por iteraci\u00f3n, la esperanza matem\u00e1tica es una distracci\u00f3n cada ~7 iteraciones. El valor proviene de estudios de eye-tracking en redes sociales.</p>"},{"location":"anti_scraping/#86-simulate_distraction-en-_infinity_scroll-10-por-iteracion","title":"8.6 <code>simulate_distraction</code> en <code>_infinity_scroll</code>: 10% por iteraci\u00f3n","text":"<p>Reducido al 10% respecto al 15% de auto_scroll porque infinity_scroll opera en p\u00e1ginas de feed (listas largas) donde el usuario est\u00e1 en modo de consumo r\u00e1pido, con menos distracciones por scroll que cuando lee comentarios en detalle.</p>"},{"location":"anti_scraping/#87-human_delay10-20-pausa-base-en-_infinity_scroll","title":"8.7 <code>human_delay(1.0, 2.0)</code> \u2014 pausa base en <code>_infinity_scroll</code>","text":"<p>Antes: <code>asyncio.sleep(SCROLL_WAIT_TIME = 1.5)</code> fijo. Ahora: <code>human_delay(min=1.0, max=2.0)</code>.</p> <p>La distribuci\u00f3n gaussiana centrada en 1.5s con sigma de ~0.17s produce valores concentrados en 1.2-1.8s, con colas hasta 1.0s (m\u00ednimo) y hasta 2.0s ocasionalmente. Elimina el patr\u00f3n de exactamente 1.5s entre cada scroll.</p> <p>\u00bfPor qu\u00e9 el m\u00ednimo de 1.0s? Es el tiempo m\u00ednimo para que una petici\u00f3n de API disparada por el scroll (p.ej. GraphQL de Facebook cargando m\u00e1s posts) complete su round-trip y sea registrada por el interceptor. Con menos de 1s hay riesgo de medir la actividad de red antes de que llegue la respuesta.</p>"},{"location":"anti_scraping/#88-simulate_idle1020s-tras-detectar-actividad-de-red","title":"8.8 <code>simulate_idle(1.0\u20132.0s)</code> tras detectar actividad de red","text":"<p>Antes: <code>asyncio.sleep(3.5)</code> fijo. Ahora: <code>simulate_idle(page, random.uniform(1.0, 2.0))</code>.</p> <p>Raz\u00f3n del recorte de 3.5s \u2192 1.0-2.0s: el sleep de 3.5s original era excesivamente conservador y asum\u00eda que toda respuesta de API tardar\u00eda m\u00e1s de 3s. En la pr\u00e1ctica, las respuestas GraphQL de Facebook/Instagram llegan en 400-800ms. Con <code>networkidle</code> ya completado, 1-2s es suficiente para que el cuerpo de las respuestas sea procesado por el interceptor.</p> <p>Ahorro acumulado: en un infinity_scroll con 10 iteraciones con actividad, el ahorro es <code>(3.5 - 1.5) \u00d7 10 = 20 segundos</code>.</p>"},{"location":"anti_scraping/#89-human_delay20-40-scroll-agresivo-de-refuerzo","title":"8.9 <code>human_delay(2.0, 4.0)</code> \u2014 scroll agresivo de refuerzo","text":"<p>Antes: <code>asyncio.sleep(5.5)</code> fijo. Ahora: <code>human_delay(min=2.0, max=4.0)</code>.</p> <p>El scroll agresivo de refuerzo se activa cuando no ha habido actividad de red durante 2 iteraciones consecutivas. Necesita m\u00e1s tiempo que la pausa base porque est\u00e1 esperando a que el servidor reconozca un scroll m\u00e1s agresivo y prepare m\u00e1s contenido. 2-4s cubre este rango sin el exceso de los 5.5s originales.</p> <p>Ahorro: el refuerzo ocurre raramente (solo cuando el contenido es lento), pero cuando ocurre ahorra ~1.5-3.5s.</p>"},{"location":"anti_scraping/#810-simulate_idle1025s-pausa-inter-iteracion-en-_infinity_scroll","title":"8.10 <code>simulate_idle(1.0\u20132.5s)</code> \u2014 pausa inter-iteraci\u00f3n en <code>_infinity_scroll</code>","text":"<p>Antes: <code>asyncio.sleep(5.0)</code> fijo. Ahora: <code>simulate_idle(page, random.uniform(1.0, 2.5))</code>.</p> <p>El <code>asyncio.sleep(5.0)</code> original era el mayor consumidor de tiempo del m\u00f3dulo. Se estableci\u00f3 conservadoramente para dar tiempo al servidor a emitir todas las respuestas antes del siguiente scroll. Con <code>human_delay(1.0, 2.0)</code> ya cubriendo la pausa post-scroll y el interceptor operando de forma as\u00edncrona, la pausa inter-iteraci\u00f3n puede reducirse a 1.0-2.5s sin perder peticiones.</p> <p>Ahorro acumulado: en un infinity_scroll de 10 iteraciones, el ahorro total es <code>(5.0 - 1.75) \u00d7 10 = 32.5 segundos</code> (usando la media del rango, 1.75s).</p>"},{"location":"anti_scraping/#811-simulate_idle0815s-pausa-final-en-scroll","title":"8.11 <code>simulate_idle(0.8\u20131.5s)</code> \u2014 pausa final en scroll","text":"<p>Antes: <code>asyncio.sleep(2)</code> fijo en auto_scroll, <code>asyncio.sleep(3)</code> en infinity_scroll. Ahora: <code>simulate_idle(page, random.uniform(0.8, 1.5))</code> en ambos.</p> <p>La pausa final permite que las \u00faltimas peticiones de red disparadas por el scroll completen su ciclo y sean registradas por el interceptor antes de que <code>page.content()</code> capture el HTML. Con el interceptor capturando de forma as\u00edncrona, 0.8-1.5s es suficiente para las respuestas pendientes.</p>"},{"location":"anti_scraping/#812-ahorro-de-tiempo-total-estimado","title":"8.12 Ahorro de tiempo total estimado","text":"Operaci\u00f3n Antes Ahora (media) Ahorro Post-navegaci\u00f3n 5.0s fijo 1.5s 3.5s Por iteraci\u00f3n scroll 1.5s fijo 0.8s 0.7s Post-actividad red (infinity) 3.5s fijo 1.5s 2.0s Inter-iteraci\u00f3n (infinity) 5.0s fijo 1.75s 3.25s Refuerzo agresivo 5.5s fijo 3.0s 2.5s Pausa final 2.5s fijo 1.15s 1.35s <p>Para una sesi\u00f3n con <code>auto_scroll</code> y 8 iteraciones el ahorro es de aproximadamente ~12 segundos. Para una sesi\u00f3n con <code>infinity_scroll</code> y 10 iteraciones el ahorro es de aproximadamente ~40 segundos, sin reducir la efectividad de la evasi\u00f3n.</p>"},{"location":"anti_scraping/#9-configuracion-y-casos-de-uso","title":"9. Configuraci\u00f3n y casos de uso","text":""},{"location":"anti_scraping/#91-uso-estandar-anti-deteccion-activa-por-defecto","title":"9.1 Uso est\u00e1ndar (anti-detecci\u00f3n activa, por defecto)","text":"<pre><code>fetcher = ContentFetcher(\"https://www.facebook.com/reel/816043001524221\")\nresult = await fetcher.fetch(auto_scroll=True)\n</code></pre> <p>Con <code>BrowserConfig.USE_ANTI_DETECTION = True</code> (por defecto), todas las capas est\u00e1n activas.</p>"},{"location":"anti_scraping/#92-desactivar-anti-deteccion-para-debug","title":"9.2 Desactivar anti-detecci\u00f3n para debug","text":"<pre><code>cfg = BrowserConfig()\ncfg.USE_ANTI_DETECTION = False   # Vuelve al comportamiento original\ncfg.PAGE_LOAD_WAIT = 5.0         # Restablecer el valor original si se necesita\nfetcher = ContentFetcher(url, config=cfg, debug=True)\n</code></pre> <p>\u00datil cuando se quiere depurar el comportamiento del interceptor o los parsers sin la variabilidad del fingerprint aleatorio.</p>"},{"location":"anti_scraping/#93-forzar-perfil-chromium","title":"9.3 Forzar perfil Chromium","text":"<pre><code>cfg = BrowserConfig()\ncfg.ANTI_DETECTION_BROWSER_TYPE = \"chromium\"\n# NOTA: tambi\u00e9n cambiar el motor en _launch_browser si se migra a Chromium\n</code></pre> <p>\u00datil para sitios que bloquean Firefox m\u00e1s agresivamente que Chrome. El fingerprint generar\u00e1 UAs y plugins de Chrome en lugar de Firefox.</p>"},{"location":"anti_scraping/#94-ajustar-velocidad-vs-seguridad","title":"9.4 Ajustar velocidad vs. seguridad","text":"<p>Para scrapers en entornos controlados donde la detecci\u00f3n es menos cr\u00edtica:</p> <pre><code>cfg = BrowserConfig()\ncfg.PAGE_LOAD_WAIT = 1.0          # Reducir espera post-carga\ncfg.SCROLL_WAIT_TIME = 0.3        # Reducir micro-pausa post-scroll\ncfg.MAX_SCROLL_ITERATIONS = 5     # Menos iteraciones de scroll\n</code></pre> <p>Para entornos de alta seguridad (IPs residenciales escasas, cuentas valiosas):</p> <pre><code>cfg = BrowserConfig()\ncfg.PAGE_LOAD_WAIT = 3.0          # M\u00e1s tiempo post-carga\ncfg.SCROLL_WAIT_TIME = 1.0        # Pausa m\u00e1s larga entre scrolls\ncfg.MAX_SCROLL_ITERATIONS = 15    # M\u00e1s contenido, m\u00e1s lento\n</code></pre>"},{"location":"anti_scraping/#10-glosario","title":"10. Glosario","text":"<p>Bot Detection / Anti-bot: sistemas de software que distinguen tr\u00e1fico humano de automatizado. Ejemplos: Akamai Bot Manager, DataDome, PerimeterX, Cloudflare Bot Management.</p> <p>Fingerprinting: t\u00e9cnica para identificar un navegador o usuario mediante propiedades t\u00e9cnicas (UA, GPU, fuentes, canvas hash) sin necesidad de cookies.</p> <p>Headless: modo de ejecuci\u00f3n de un navegador sin interfaz gr\u00e1fica. Los navegadores headless exponen se\u00f1ales t\u00e9cnicas que los distinguen de los navegadores con GUI.</p> <p>ICE Candidates (WebRTC): mecanismo de WebRTC para descubrir la IP real de un peer en una conexi\u00f3n P2P. Puede revelar la IP del host aunque haya un proxy activo.</p> <p>Init Script: script JavaScript inyectado por Playwright que se ejecuta antes de cualquier script de la p\u00e1gina.</p> <p><code>networkidle</code>: evento de Playwright que se dispara cuando no ha habido actividad de red durante 500ms. Indica que la p\u00e1gina ha terminado de cargar recursos as\u00edncronos.</p> <p>Perfil de plataforma: conjunto coherente de propiedades de navegador que corresponden a un sistema operativo y hardware espec\u00edfico.</p> <p>Stealth JS: script JavaScript que parcha las propiedades del navegador para eliminar se\u00f1ales de automatizaci\u00f3n.</p> <p>User-Agent (UA): string que el navegador env\u00eda al servidor identificando su versi\u00f3n y sistema operativo. F\u00e1cilmente falsificable, pero debe ser coherente con otros atributos.</p> <p>WebGL: API JavaScript para renderizado 3D en el navegador. Expone el vendor y modelo de GPU del sistema, usado como se\u00f1al de fingerprinting.</p> <p>WPM (Words Per Minute): palabras por minuto. M\u00e9trica usada para calcular tiempos de lectura realistas en <code>simulate_reading_pause</code>.</p>"},{"location":"changelog/","title":"Changelog","text":""},{"location":"changelog/#040","title":"0.4.0","text":""},{"location":"changelog/#acciones-de-escritura-fiabilidad-del-flujo-de-posts","title":"Acciones de escritura \u2014 fiabilidad del flujo de posts","text":"<ul> <li>Navegaci\u00f3n al perfil propio para publicar en el muro: <code>create_post()</code>   sin <code>group</code> ahora deriva la URL del perfil a partir del cookie <code>c_user</code> de   la cuenta (<code>https://www.facebook.com/profile.php?id={c_user}</code>) y navega   ah\u00ed, en lugar de al home. <code>resolve_profile_url()</code> en <code>reaper.actions.utils</code>.</li> <li>Apertura del composer: la UI de escritorio muestra un trigger   (\"\u00bfQu\u00e9 est\u00e1s pensando?\") que abre el di\u00e1logo con el campo de texto. Se a\u00f1ade   <code>COMPOSER_TRIGGER</code> / <code>GROUP_COMPOSER_TRIGGER</code> y el helper <code>_open_composer()</code>   en <code>reaper.actions.facebook.flows</code>: si el campo no est\u00e1 visible, se hace   clic en el trigger antes de escribir.</li> <li>Botones de env\u00edo en espa\u00f1ol: <code>POST_SUBMIT</code> y el nuevo <code>GROUP_POST_SUBMIT</code>   incluyen variantes <code>aria-label='Publicar'</code> adem\u00e1s de <code>Post</code>.</li> <li>Selectores ampliados: <code>COMPOSER_INPUT</code> a\u00f1ade <code>div[data-contents='true']</code>   y <code>PHOTO_INPUT</code> el input de archivos del composer de escritorio.</li> </ul>"},{"location":"changelog/#correcciones","title":"Correcciones","text":"<ul> <li><code>find_config_file()</code> capturaba el cwd en el import: <code>DEFAULT_CONFIG_PATHS</code>   se computaba al cargar el m\u00f3dulo, as\u00ed que   <code>AccountManager()</code> / <code>build_storage()</code> pod\u00edan ignorar un <code>reaper.toml</code>   creado o cambiado despu\u00e9s del import, y los tests que aislaban el cwd fallaban.   Ahora las rutas se eval\u00faan en cada b\u00fasqueda.</li> </ul>"},{"location":"changelog/#compatibilidad","title":"Compatibilidad","text":"<ul> <li>Cambios aditivos. La navegaci\u00f3n al perfil deriva <code>c_user</code>; si la cuenta no   tiene ese cookie (o el perfil no se puede resolver), cae al home como antes.</li> </ul>"},{"location":"changelog/#030","title":"0.3.0","text":""},{"location":"changelog/#nuevas-funcionalidades","title":"Nuevas funcionalidades","text":"<ul> <li>Acciones de escritura autenticadas (<code>reaper.actions</code>):</li> <li><code>ActionManager</code> \u2014 orquestador que ejecuta acciones contra una sesi\u00f3n de     Facebook autenticada y persiste cada resultado (<code>ActionResult</code>).</li> <li><code>create_post()</code> \u2014 publicar texto y/o im\u00e1genes en el muro de la cuenta o     directamente dentro de un grupo (<code>group</code> por ID o URL).</li> <li><code>share_post()</code> \u2014 compartir una publicaci\u00f3n existente hacia un grupo     (b\u00fasqueda por <code>group_name</code> o <code>group</code>).</li> <li><code>comment()</code> \u2014 responder con texto a una publicaci\u00f3n.</li> <li><code>like()</code> \u2014 reaccionar con \"Me gusta\" a una publicaci\u00f3n.</li> <li>Confirmaci\u00f3n por DOM (<code>confirm_timeout</code>) y estado <code>status=\"ok\"</code> / <code>\"error\"</code>.</li> <li>Selecci\u00f3n expl\u00edcita de <code>account</code> por <code>account_id</code>, <code>username</code> o <code>c_user</code>;     <code>None</code> = rotaci\u00f3n autom\u00e1tica del <code>AccountManager</code>.</li> <li> <p><code>ActionResult</code> se persiste en el mismo backend que las cuentas     (local / PostgreSQL / MongoDB) pero en un recurso separado:     <code>actions_dir</code> / <code>actions_table</code> / <code>actions_collection</code> (defaults     <code>data/actions</code>, <code>actions</code>, <code>actions</code>).</p> </li> <li> <p><code>SessionActor</code> / <code>SessionHandle</code> (<code>reaper.actions.actor</code>):</p> </li> <li> <p>Lanzamiento de sesiones autenticadas con Camoufox, inyecci\u00f3n de cookies y     exportaci\u00f3n de cookies actualizadas post-ejecuci\u00f3n.</p> </li> <li> <p>Storage de acciones (<code>reaper.actions.storage</code>):</p> </li> <li><code>BaseActionStorage</code> (ABC), <code>LocalActionStorage</code>, <code>PostgresActionStorage</code>,     <code>MongoActionStorage</code> y <code>build_action_storage()</code>.</li> <li> <p>Interfaz: <code>add</code>, <code>get</code>, <code>list_all</code>, <code>delete</code>, <code>count</code>.</p> </li> <li> <p>CLI de acciones (<code>reaper action &lt;verb&gt;</code>):</p> </li> <li>Subcomandos <code>post</code>, <code>share</code>, <code>comment</code> y <code>like</code>, con flags comunes     <code>--account</code>, <code>--no-headless</code>, <code>--proxy</code>, <code>--proxy-user</code>, <code>--proxy-pass</code>,     <code>--confirm-timeout</code>, <code>--output</code>, <code>--indent</code> y <code>--debug</code>.</li> <li>C\u00f3digos de salida: <code>0</code> confirmada, <code>1</code> ejecutada sin confirmar     (<code>status=\"error\"</code>), <code>5</code> error de entrada/sesi\u00f3n (<code>ActionError</code>).</li> </ul>"},{"location":"changelog/#api-modificada","title":"API modificada","text":"<ul> <li><code>reaper</code> y <code>reaper.actions</code> exportan: <code>ActionManager</code>, <code>ActionType</code>,   <code>ActionResult</code>, <code>ActionError</code>, <code>SessionActor</code>, <code>SessionHandle</code>,   <code>BaseActionStorage</code>, los tres backends de acciones y <code>build_action_storage</code>.</li> </ul>"},{"location":"changelog/#compatibilidad_1","title":"Compatibilidad","text":"<ul> <li>Los cambios son aditivos y no rompen la API de scraping/autenticaci\u00f3n   existente. El storage de acciones requiere que el <code>AccountManager</code> tenga al   menos una cuenta de Facebook autenticada con cookies v\u00e1lidas.</li> </ul>"},{"location":"changelog/#024","title":"0.2.4","text":""},{"location":"changelog/#nuevas-funcionalidades_1","title":"Nuevas funcionalidades","text":"<ul> <li>Almacenamiento configurable (<code>reaper.auth.storage</code>):</li> <li>Dos backends de base de datos adem\u00e1s del local en JSON:     <code>PostgresStorage</code> (asyncpg) y <code>MongoStorage</code> (motor).</li> <li>Fichero de configuraci\u00f3n <code>reaper.toml</code> con precedencia:     ruta expl\u00edcita \u2192 <code>REAPER_CONFIG</code> \u2192 <code>./reaper.toml</code> \u2192     <code>~/.config/reaper/reaper.toml</code> \u2192 <code>/etc/reaper/reaper.toml</code>.</li> <li><code>load_storage_config()</code>, <code>find_config_file()</code> y <code>build_storage()</code> para     construir el backend indicado sin instanciarlo manualmente.</li> <li>La elecci\u00f3n de backend es todo o nada: perfiles y cookies viven en el     mismo backend; no hay mezcla local/BD.</li> <li> <p><code>AccountManager()</code> respeta autom\u00e1ticamente la configuraci\u00f3n del fichero y     expone <code>save_cookies()</code>, <code>load_cookies()</code> y <code>delete_cookies()</code> con     implementaci\u00f3n por backend.</p> </li> <li> <p>Selecci\u00f3n expl\u00edcita de cuenta:</p> </li> <li><code>AccountManager.resolve_account()</code> localiza una cuenta por <code>account_id</code>     (UUID4), <code>username</code> o ID de usuario de la plataforma (<code>c_user</code> / <code>ds_user_id</code>).</li> <li><code>get_account_for_request(preferred=...)</code> permite fijar la cuenta para una     petici\u00f3n sin desactivar el rotador.</li> <li><code>scrape(account=...)</code>, <code>Reaper.scrape(account=...)</code> y la CLI     <code>reaper &lt;url&gt; --account &lt;username&gt;</code> propagan la preferencia.</li> <li>Resoluci\u00f3n de ID de usuario solo para valores num\u00e9ricos de 5+ d\u00edgitos,     evitando colisiones con usernames cortos.</li> </ul>"},{"location":"changelog/#api-modificada_1","title":"API modificada","text":"<ul> <li><code>AccountManager</code> a\u00f1ade <code>resolve_account()</code>, <code>save_cookies()</code>,   <code>load_cookies()</code>, <code>delete_cookies()</code>.</li> <li><code>scrape()</code> y <code>Reaper.scrape()</code> a\u00f1aden el par\u00e1metro opcional <code>account</code>.</li> <li><code>ScraperConfig</code> a\u00f1ade <code>preferred_account</code>.</li> <li>CLI: nuevo flag <code>--account</code>.</li> </ul>"},{"location":"changelog/#compatibilidad_2","title":"Compatibilidad","text":"<p>Todos los cambios son compatibles hacia atr\u00e1s. Sin <code>reaper.toml</code> el comportamiento es id\u00e9ntico a 0.2.3 (backend <code>local</code> por defecto).</p>"},{"location":"changelog/#023","title":"0.2.3","text":""},{"location":"changelog/#nuevas-funcionalidades_2","title":"Nuevas funcionalidades","text":"<ul> <li>Detecci\u00f3n de contenido no disponible en <code>FacebookScraper</code> y <code>fb_auth_detector</code>:</li> </ul> <p>Hasta esta versi\u00f3n, las p\u00e1ginas de Facebook donde el contenido ha sido eliminado   o restringido permanentemente eran detectadas como muros de autenticaci\u00f3n   (<code>auth_type=\"error_route\"</code>), provocando reintentos in\u00fatiles con cuentas del pool.   Autenticarse no resuelve este caso porque el contenido no existe para nadie.</p> <p>La detecci\u00f3n ahora distingue ambos casos antes de evaluar si hay muro de login:</p> <ul> <li> <p><code>content_unavailable</code> \u2014 nuevo <code>auth_type</code>. Facebook muestra este estado     cuando el contenido fue eliminado, era privado para un grupo reducido, o el     propietario cambi\u00f3 la privacidad. Se\u00f1ales detectadas en el bloque JSON de     bootstrap del HTML renderizado:</p> <ul> <li>T\u00edtulo <code>\"This content isn't available right now\"</code> en los props del <code>rootView</code>.</li> <li>Texto del cuerpo del error con referencias a \"deleted\" o \"small group\".</li> </ul> </li> <li> <p>Sin reintento: cuando se detecta <code>content_unavailable</code>, <code>FacebookScraper</code>     retorna inmediatamente con <code>status=\"content_unavailable\"</code> sin consultar al     <code>AccountManager</code> ni consumir cuentas del pool.</p> </li> <li> <p>Nuevo <code>status</code> en el resultado: <code>\"content_unavailable\"</code> permite que el caller   distinga este caso de un error t\u00e9cnico (<code>\"error\"</code>) o de un muro de autenticaci\u00f3n   no recuperable, y tome decisiones informadas (e.g. marcar el contenido como   eliminado en su sistema).</p> </li> <li> <p>Nuevo m\u00e9todo <code>FacebookScraper._content_unavailable_result()</code>: helper an\u00e1logo   a <code>_error_result()</code> que emite el dict con <code>status=\"content_unavailable\"</code> y   <code>error=\"Content not available \u2014 deleted or restricted\"</code>.</p> </li> </ul>"},{"location":"changelog/#archivos-modificados","title":"Archivos modificados","text":"<ul> <li><code>src/reaper/utils/fb_auth_detector.py</code> \u2014 constante <code>_CONTENT_UNAVAILABLE</code> + capa 1b en <code>requires_auth()</code></li> <li><code>src/reaper/scrapers/facebook.py</code> \u2014 guardia <code>content_unavailable</code> en <code>run()</code> + m\u00e9todo <code>_content_unavailable_result()</code></li> </ul>"},{"location":"changelog/#022","title":"0.2.2","text":""},{"location":"changelog/#nuevas-funcionalidades_3","title":"Nuevas funcionalidades","text":"<ul> <li>Reintento autom\u00e1tico al detectar muro de autenticaci\u00f3n en <code>FacebookScraper</code> e <code>InstagramScraper</code>:</li> </ul> <p>Cuando <code>requires_auth()</code> detecta un muro de login en el HTML, el scraper   ya no devuelve error inmediatamente. En su lugar aplica esta l\u00f3gica:</p> <ul> <li>Fetch an\u00f3nimo + muro: si hay <code>AccountManager</code> con cuentas disponibles,     reintenta autom\u00e1ticamente con una cuenta autenticada.</li> <li>Fetch autenticado + muro: las cookies de la cuenta activa han sido     invalidadas por la plataforma. La cuenta se marca como <code>COOKIE_EXPIRED</code>     y se reintenta con una cuenta diferente del pool.</li> <li> <p>Sin posibilidad de retry (sin manager, sin cuentas, o segundo muro):     retorna error. M\u00e1ximo 1 reintento por petici\u00f3n.</p> </li> <li> <p><code>InstagramScraper</code> a\u00f1ade detecci\u00f3n de muro de autenticaci\u00f3n via <code>requires_auth()</code>   (no exist\u00eda en versiones anteriores).</p> </li> </ul>"},{"location":"changelog/#archivos-modificados_1","title":"Archivos modificados","text":"<ul> <li><code>src/reaper/scrapers/facebook.py</code> \u2014 m\u00e9todos <code>_handle_auth_wall()</code>, <code>_retry_with_account()</code></li> <li><code>src/reaper/scrapers/instagram.py</code> \u2014 mismos m\u00e9todos + import de <code>requires_auth</code></li> </ul>"},{"location":"changelog/#021","title":"0.2.1","text":""},{"location":"changelog/#nuevas-funcionalidades_4","title":"Nuevas funcionalidades","text":"<ul> <li>Refresco autom\u00e1tico de cookies sin intervenci\u00f3n manual:</li> <li><code>FetchResult</code> a\u00f1ade el campo <code>updated_cookies</code>: cookies le\u00eddas del     contexto Playwright tras la navegaci\u00f3n. El servidor emite <code>Set-Cookie</code>     en cada respuesta que actualiza tokens de seguridad y extiende TTLs.</li> <li><code>ContentFetcher.fetch()</code> lee <code>context.cookies()</code> post-navegaci\u00f3n cuando     la sesi\u00f3n es autenticada y lo expone en <code>FetchResult.updated_cookies</code>.</li> <li><code>BaseScraper._fetch_with_account()</code> compara las cookies originales con     las actualizadas v\u00eda <code>_cookies_differ()</code> y llama a <code>manager.import_cookies()</code>     si difieren, persistiendo la sesi\u00f3n renovada en disco autom\u00e1ticamente.</li> <li><code>_cookies_differ()</code> \u2014 helper privado que compara <code>value</code> y <code>expires</code>     por nombre de cookie, ignorando metadatos irrelevantes.</li> </ul>"},{"location":"changelog/#comportamiento-del-cookie_refresh_threshold","title":"Comportamiento del <code>cookie_refresh_threshold</code>","text":"<p>El threshold pasa de se\u00f1al principal a safety net. Con el auto-refresh activo, <code>requests_since_cookie_refresh</code> se resetea a 0 despu\u00e9s de cada petici\u00f3n donde el servidor devuelva cookies actualizadas. El WARNING <code>NEEDS_REFRESH</code> solo aparece si el servidor no emite <code>Set-Cookie</code> durante N peticiones consecutivas \u2014 caso inusual que requiere intervenci\u00f3n manual.</p>"},{"location":"changelog/#archivos-modificados_2","title":"Archivos modificados","text":"<ul> <li><code>src/reaper/network/interceptor.py</code> \u2014 campo <code>updated_cookies</code> en <code>FetchResult</code></li> <li><code>src/reaper/network/content_fetcher.py</code> \u2014 captura de cookies post-navegaci\u00f3n</li> <li><code>src/reaper/scrapers/base.py</code> \u2014 l\u00f3gica de auto-refresh y <code>_cookies_differ()</code></li> <li><code>docs/technical/auth.md</code> \u2014 documentaci\u00f3n del mecanismo de auto-refresh</li> <li><code>docs/user/pool.md</code> \u2014 ciclo de vida actualizado con auto-refresh</li> </ul>"},{"location":"changelog/#020","title":"0.2.0","text":""},{"location":"changelog/#nuevas-funcionalidades_5","title":"Nuevas funcionalidades","text":"<ul> <li>Sistema de cuentas autenticadas (<code>reaper.auth</code>):</li> <li><code>AccountManager</code> \u2014 gestor central de cuentas con CRUD completo.</li> <li><code>AccountRotator</code> \u2014 selecci\u00f3n inteligente por score (tasa de \u00e9xito + LRU + antig\u00fcedad de cookies).</li> <li><code>AccountProfile</code>, <code>AccountActivity</code>, <code>AccountStatus</code> \u2014 modelos de datos serializables.</li> <li><code>LocalFileStorage</code> \u2014 persistencia en JSON estructurado por plataforma y account_id.</li> <li><code>BaseAccountStorage</code> \u2014 ABC para migraci\u00f3n a cualquier backend de BD.</li> <li> <p><code>run_login_new_account()</code> / <code>run_login_refresh()</code> \u2014 flujo interactivo de login con Playwright.</p> </li> <li> <p>Rotaci\u00f3n autom\u00e1tica de cuentas en <code>BaseScraper._fetch_with_account()</code>.</p> </li> <li>Selecci\u00f3n, inyecci\u00f3n de cookies, registro de actividad post-petici\u00f3n.</li> <li> <p>Modo an\u00f3nimo autom\u00e1tico si no hay cuentas disponibles o <code>account_manager=None</code>.</p> </li> <li> <p>Refresco autom\u00e1tico de cookies:</p> </li> <li>Contador <code>requests_since_cookie_refresh</code> por cuenta.</li> <li>Estado <code>NEEDS_REFRESH</code> con warning en logs al alcanzar el umbral.</li> <li> <p><code>import_cookies()</code> resetea el contador y restaura estado <code>ACTIVE</code>.</p> </li> <li> <p>CLI de login (<code>python -m reaper.auth.login</code>):</p> </li> <li> <p>Flags: <code>--platform</code>, <code>--username</code>, <code>--refresh</code>, <code>--account-id</code>, <code>--accounts-dir</code>, <code>--threshold</code>.</p> </li> <li> <p>Inyecci\u00f3n de cookies en Playwright (<code>ContentFetcher.fetch(cookies=...)</code>):</p> </li> <li>Inyecci\u00f3n en el contexto del navegador antes de navegar.</li> </ul>"},{"location":"changelog/#api-modificada_2","title":"API modificada","text":"<ul> <li><code>scrape()</code> acepta el par\u00e1metro opcional <code>account_manager</code>.</li> <li><code>Reaper.__init__</code> acepta el par\u00e1metro opcional <code>account_manager</code>.</li> <li><code>ScraperConfig</code> a\u00f1ade campo <code>account_manager</code> y propiedad <code>has_account_manager</code>.</li> <li><code>BaseScraper._fetch()</code> ahora es abstracto y acepta par\u00e1metro <code>cookies</code>.</li> <li><code>ContentFetcher.fetch()</code> acepta par\u00e1metro <code>cookies: list[dict] | None</code>.</li> </ul>"},{"location":"changelog/#compatibilidad_3","title":"Compatibilidad","text":"<p>Todos los cambios son compatibles hacia atr\u00e1s. El c\u00f3digo existente que no usa <code>account_manager</code> funciona sin modificaciones.</p>"},{"location":"changelog/#010","title":"0.1.0","text":"<p>Versi\u00f3n inicial.</p> <ul> <li>Scraping de Facebook: posts, reels, v\u00eddeos, fotos, grupos, perfiles.</li> <li>Scraping de Instagram: posts y reels.</li> <li>Soporte de proxies (HTTP, SOCKS5, con autenticaci\u00f3n).</li> <li>Modo debug con artefactos en disco (HTML, tr\u00e1fico GraphQL).</li> <li>CLI (<code>reaper &lt;url&gt;</code>) con todas las opciones.</li> <li>API program\u00e1tica: <code>scrape()</code>, <code>Reaper</code>, <code>ScraperConfig</code>.</li> </ul>"},{"location":"api/actions/","title":"API Reference \u2014 <code>reaper.actions</code>","text":"<p>Acciones de escritura autenticadas sobre Facebook. Todos los s\u00edmbolos pueden importarse desde <code>reaper.actions</code> y, los principales, desde el paquete ra\u00edz <code>reaper</code>.</p> <pre><code>from reaper import ActionManager, ActionType, ActionResult, ActionError\nfrom reaper.actions import SessionActor, build_action_storage\n</code></pre>"},{"location":"api/actions/#actionmanager","title":"<code>ActionManager</code>","text":"<p>Orquesta el ciclo completo de una acci\u00f3n: resuelve la cuenta, abre la sesi\u00f3n, ejecuta el flujo de UI, confirma por DOM, persiste el resultado y actualiza la actividad/cookies de la cuenta.</p>"},{"location":"api/actions/#reaper.actions.manager.ActionManager","title":"reaper.actions.manager.ActionManager","text":"<pre><code>ActionManager(\n    account_manager: Any | None = None,\n    storage: BaseActionStorage | None = None,\n    *,\n    headless: bool = True,\n    browser_type: str = \"firefox\",\n    proxy_server: str | None = None,\n    proxy_username: str | None = None,\n    proxy_password: str | None = None,\n    base_url: str = DEFAULT_BASE_URL,\n    browser_factory: Any | None = None,\n)\n</code></pre> <p>Orquesta acciones de escritura autenticadas y persiste sus resultados.</p> <p>Parameters:</p> Name Type Description Default <code>account_manager</code> <code>Any | None</code> <p>Gestor de cuentas que provee cookies y rotaci\u00f3n. Obligatorio para todas las acciones (la escritura requiere sesi\u00f3n).</p> <code>None</code> <code>storage</code> <code>BaseActionStorage | None</code> <p>Backend de persistencia de acciones. Si es <code>None</code>, se construye desde la configuraci\u00f3n de <code>reaper.toml</code> (defecto: <code>LocalActionStorage</code> en <code>data/actions</code>).</p> <code>None</code> <code>headless</code> <code>bool</code> <p>Ejecutar el navegador sin interfaz gr\u00e1fica. Defecto: True.</p> <code>True</code> <code>browser_type</code> <code>str</code> <p>Perfil de fingerprint (<code>\"firefox\"</code> o <code>\"chromium\"</code>).</p> <code>'firefox'</code> <code>proxy_server / proxy_username / proxy_password</code> <p>Configuraci\u00f3n del proxy de salida para las sesiones.</p> required <code>base_url</code> <code>str</code> <p>Ra\u00edz de navegaci\u00f3n de Facebook. Defecto: <code>https://m.facebook.com/</code>.</p> <code>DEFAULT_BASE_URL</code> <code>browser_factory</code> <code>Any | None</code> <p>Factory de navegador alternativo inyectable para tests (equivalente a <code>SessionActor.use_browser_factory</code>).</p> <code>None</code>"},{"location":"api/actions/#reaper.actions.manager.ActionManager.storage","title":"storage  <code>property</code>","text":"<pre><code>storage: BaseActionStorage\n</code></pre> <p>Backend de persistencia en uso (para consultas del operador).</p>"},{"location":"api/actions/#reaper.actions.manager.ActionManager.create_post","title":"create_post  <code>async</code>","text":"<pre><code>create_post(\n    *,\n    text: str | None = None,\n    images: list[str | Path] | None = None,\n    group: str | None = None,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Publica un post de texto y/o im\u00e1genes en el muro (o en un grupo).</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str | None</code> <p>Texto/caption de la publicaci\u00f3n.</p> <code>None</code> <code>images</code> <code>list[str | Path] | None</code> <p>Rutas locales de im\u00e1genes a adjuntar (jpg, png, gif, ...).</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Si se indica (ID o URL de grupo), la publicaci\u00f3n se hace       directamente dentro de ese grupo en lugar del muro.</p> <code>None</code> <code>account</code> <code>str | None</code> <p>Cuenta a forzar (account_id, username o c_user).       <code>None</code> = rotaci\u00f3n autom\u00e1tica del AccountManager.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM del post.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido con el resultado de la operaci\u00f3n.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si no hay cuenta autenticada, el post est\u00e1 vac\u00edo o las im\u00e1genes no son v\u00e1lidas.</p>"},{"location":"api/actions/#reaper.actions.manager.ActionManager.share_post","title":"share_post  <code>async</code>","text":"<pre><code>share_post(\n    *,\n    post_url: str,\n    group: str,\n    group_name: str | None = None,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Comparte una publicaci\u00f3n existente dentro de un grupo.</p> <p>Parameters:</p> Name Type Description Default <code>post_url</code> <code>str</code> <p>URL de la publicaci\u00f3n a compartir.</p> required <code>group</code> <code>str</code> <p>Grupo de destino (ID o URL). Se usa como texto de          b\u00fasqueda en el di\u00e1logo si no se da <code>group_name</code>.</p> required <code>group_name</code> <code>str | None</code> <p>Nombre visible del grupo de destino (recomendado);          es el texto que se busca en el di\u00e1logo de compartir.</p> <code>None</code> <code>account</code> <code>str | None</code> <p>Cuenta a forzar. <code>None</code> = rotaci\u00f3n autom\u00e1tica.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si la URL del post o la cuenta son inv\u00e1lidas.</p>"},{"location":"api/actions/#reaper.actions.manager.ActionManager.comment","title":"comment  <code>async</code>","text":"<pre><code>comment(\n    *,\n    post_url: str,\n    text: str,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Responde con texto (comenta) una publicaci\u00f3n existente.</p> <p>Parameters:</p> Name Type Description Default <code>post_url</code> <code>str</code> <p>URL de la publicaci\u00f3n a comentar.</p> required <code>text</code> <code>str</code> <p>Texto del comentario.</p> required <code>account</code> <code>str | None</code> <p>Cuenta a forzar. <code>None</code> = rotaci\u00f3n autom\u00e1tica.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si el texto est\u00e1 vac\u00edo o la cuenta es inv\u00e1lida.</p>"},{"location":"api/actions/#reaper.actions.manager.ActionManager.like","title":"like  <code>async</code>","text":"<pre><code>like(\n    *,\n    post_url: str,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Reacciona con \"Me gusta\" a una publicaci\u00f3n existente.</p> <p>Parameters:</p> Name Type Description Default <code>post_url</code> <code>str</code> <p>URL de la publicaci\u00f3n a reaccionar.</p> required <code>account</code> <code>str | None</code> <p>Cuenta a forzar. <code>None</code> = rotaci\u00f3n autom\u00e1tica.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si la URL del post o la cuenta son inv\u00e1lidas.</p>"},{"location":"api/actions/#reaper.actions.manager.ActionManager.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Cierra los recursos del backend de almacenamiento de acciones.</p>"},{"location":"api/actions/#modelos","title":"Modelos","text":""},{"location":"api/actions/#actiontype","title":"<code>ActionType</code>","text":""},{"location":"api/actions/#reaper.actions.models.ActionType","title":"reaper.actions.models.ActionType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Tipo de acci\u00f3n de escritura soportada por Reaper.</p> Valores <p>POST:           Crear una publicaci\u00f3n nueva en el muro de la cuenta. GROUP_POST:     Publicar un post nuevo directamente dentro de un grupo. SHARE_TO_GROUP: Compartir una publicaci\u00f3n existente dentro de un grupo. COMMENT:        Responder con texto a una publicaci\u00f3n existente. LIKE:           Reaccionar con \"Me gusta\" a una publicaci\u00f3n existente.</p>"},{"location":"api/actions/#actionresult","title":"<code>ActionResult</code>","text":""},{"location":"api/actions/#reaper.actions.models.ActionResult","title":"reaper.actions.models.ActionResult  <code>dataclass</code>","text":"<pre><code>ActionResult(\n    action: str,\n    platform: str = \"facebook\",\n    action_id: str = (lambda: uuid.uuid4().hex)(),\n    account_id: str | None = None,\n    account_username: str | None = None,\n    status: str = \"ok\",\n    post_url: str | None = None,\n    post_id: str | None = None,\n    comment_id: str | None = None,\n    group: str | None = None,\n    text: str | None = None,\n    error: str | None = None,\n    executed_at: str = _now_iso(),\n)\n</code></pre> <p>Registro del resultado de una acci\u00f3n de escritura ejecutada.</p> <p>Es el contrato de datos entre <code>ActionManager</code>, el <code>SessionActor</code> y el almacenamiento de acciones. Se serializa con <code>to_dict()</code> / <code>from_dict()</code> para persistir en cualquier backend (JSON, PostgreSQL, MongoDB).</p> <p>Attributes:</p> Name Type Description <code>action_id</code> <code>str</code> <p>UUID4 interno del registro. Se genera al ejecutar.</p> <code>action</code> <code>str</code> <p>Valor de <code>ActionType</code> (<code>\"post\"</code>, <code>\"comment\"</code>, ...).</p> <code>platform</code> <code>str</code> <p>Plataforma sobre la que se ejecut\u00f3 (<code>\"facebook\"</code>).</p> <code>account_id</code> <code>str | None</code> <p>UUID4 de la cuenta autenticada usada.</p> <code>account_username</code> <code>str | None</code> <p>Username de la cuenta (para logs/auditor\u00eda).</p> <code>status</code> <code>str</code> <p><code>\"ok\"</code> si la plataforma confirm\u00f3 la acci\u00f3n,              <code>\"error\"</code> si fall\u00f3.</p> <code>post_url</code> <code>str | None</code> <p>URL de la publicaci\u00f3n creada o objetivo.</p> <code>post_id</code> <code>str | None</code> <p>ID de la publicaci\u00f3n creada o objetivo (si se detecta).</p> <code>comment_id</code> <code>str | None</code> <p>ID del comentario publicado (solo <code>comment</code>).</p> <code>group</code> <code>str | None</code> <p>Identificador del grupo (id o URL) para acciones de grupo.</p> <code>text</code> <code>str | None</code> <p>Texto enviado (contenido del post o del comentario).</p> <code>error</code> <code>str | None</code> <p>Mensaje de error cuando <code>status=\"error\"</code>.</p> <code>executed_at</code> <code>str</code> <p>Timestamp UTC ISO-8601 de la ejecuci\u00f3n.</p>"},{"location":"api/actions/#reaper.actions.models.ActionResult.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convierte el resultado en un dict serializable a JSON.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict con todos los campos del registro.</p>"},{"location":"api/actions/#reaper.actions.models.ActionResult.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; ActionResult\n</code></pre> <p>Reconstruye un <code>ActionResult</code> desde un dict deserializado.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Dict producido por :meth:<code>to_dict</code>.</p> required <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> con los valores del dict.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si falta el campo <code>action</code>.</p>"},{"location":"api/actions/#actionerror","title":"<code>ActionError</code>","text":""},{"location":"api/actions/#reaper.actions.models.ActionError","title":"reaper.actions.models.ActionError","text":"<p>               Bases: <code>Exception</code></p> <p>Error en la ejecuci\u00f3n de una acci\u00f3n de escritura.</p> <p>Se lanza cuando la acci\u00f3n no puede ejecutarse: falta de cuenta con cookies v\u00e1lidas, elemento de la interfaz no encontrado, o la plataforma rechaza la operaci\u00f3n.</p>"},{"location":"api/actions/#sessionactor-y-sessionhandle","title":"<code>SessionActor</code> y <code>SessionHandle</code>","text":"<p>Lanzamiento de sesiones de navegador autenticadas (Camoufox) en las que se ejecutan los flujos de UI.</p>"},{"location":"api/actions/#reaper.actions.actor.SessionActor","title":"reaper.actions.actor.SessionActor","text":"<pre><code>SessionActor(\n    headless: bool = True,\n    proxy_server: str | None = None,\n    proxy_username: str | None = None,\n    proxy_password: str | None = None,\n    browser_type: str = \"firefox\",\n    viewport: dict[str, int] | None = None,\n    locale: str | None = None,\n    timezone_id: str | None = None,\n)\n</code></pre> <p>Lanza sesiones de navegador autenticadas con Camoufox.</p> <p>Parameters:</p> Name Type Description Default <code>headless</code> <code>bool</code> <p>Modo sin ventana visible. Defecto: <code>True</code>.</p> <code>True</code> <code>proxy_server</code> <code>str | None</code> <p>URL del proxy (<code>\"http://ip:port\"</code>, <code>\"socks5://...\"</code>).</p> <code>None</code> <code>proxy_username</code> <code>str | None</code> <p>Usuario para autenticaci\u00f3n del proxy.</p> <code>None</code> <code>proxy_password</code> <code>str | None</code> <p>Contrase\u00f1a para autenticaci\u00f3n del proxy.</p> <code>None</code> <code>browser_type</code> <code>str</code> <p>Perfil de fingerprint para anti-detecci\u00f3n. <code>\"firefox\"</code> (mejor cobertura en Meta) o <code>\"chromium\"</code>. Defecto: <code>\"firefox\"</code>.</p> <code>'firefox'</code> <code>viewport</code> <code>dict[str, int] | None</code> <p>Viewport fijo (dict <code>{\"width\": ..., \"height\": ...}</code>). Si es <code>None</code>, se usa el viewport del fingerprint generado.</p> <code>None</code> <code>locale</code> <code>str | None</code> <p>Locale BCP-47 (p.ej. <code>\"en-US\"</code>). Si es <code>None</code>, el del fingerprint.</p> <code>None</code> <code>timezone_id</code> <code>str | None</code> <p>IANA timezone (p.ej. <code>\"America/New_York\"</code>). Si es <code>None</code>, la del fingerprint.</p> <code>None</code>"},{"location":"api/actions/#reaper.actions.actor.SessionActor.use_browser_factory","title":"use_browser_factory","text":"<pre><code>use_browser_factory(factory: BrowserFactory) -&gt; None\n</code></pre> <p>Inyecta un factory de navegador alternativo (para tests/mocks).</p>"},{"location":"api/actions/#reaper.actions.actor.SessionActor.force_fingerprint","title":"force_fingerprint","text":"<pre><code>force_fingerprint(fingerprint: Any) -&gt; None\n</code></pre> <p>Fuerza un fingerprint concreto (para tests deterministas).</p>"},{"location":"api/actions/#reaper.actions.actor.SessionActor.session","title":"session  <code>async</code>","text":"<pre><code>session(\n    *,\n    cookies: list[dict[str, Any]] | None = None,\n    url: str | None = None,\n) -&gt; AsyncIterator[SessionHandle]\n</code></pre> <p>Abre una sesi\u00f3n de navegador autenticada y la cierra al salir.</p> <p>Lanza Camoufox (o el factory inyectado en tests), crea un contexto con fingerprint coherente, inyecta las cookies y navega a <code>url</code> si se indica. El cuerpo del <code>with</code> recibe un <code>SessionHandle</code>.</p> <p>Al salir del contexto, <code>handle.updated_cookies</code> contiene las cookies actualizadas por el sitio (para persistirlas en la cuenta).</p> <p>Parameters:</p> Name Type Description Default <code>cookies</code> <code>list[dict[str, Any]] | None</code> <p>Cookies en formato Playwright de la cuenta autenticada.</p> <code>None</code> <code>url</code> <code>str | None</code> <p>URL a navegar al abrir la sesi\u00f3n. Opcional.</p> <code>None</code> <p>Yields:</p> Type Description <code>AsyncIterator[SessionHandle]</code> <p><code>SessionHandle</code> con la p\u00e1gina y el contexto listos para operar.</p> <p>Raises:</p> Type Description <code>ImportError</code> <p>Si <code>camoufox</code> no est\u00e1 instalado.</p> <code>Exception</code> <p>Cualquier error de navegaci\u00f3n/lanzamiento.</p>"},{"location":"api/actions/#reaper.actions.actor.SessionHandle","title":"reaper.actions.actor.SessionHandle  <code>dataclass</code>","text":"<pre><code>SessionHandle(\n    page: Any,\n    context: Any,\n    browser: Any,\n    updated_cookies: list[dict[str, Any]] | None = None,\n)\n</code></pre> <p>Contexto de una sesi\u00f3n de navegador abierta por <code>SessionActor</code>.</p> <p>Attributes:</p> Name Type Description <code>page</code> <code>Any</code> <p>P\u00e1gina principal de Playwright ya navegada a <code>url</code>.</p> <code>context</code> <code>Any</code> <p>BrowserContext de Playwright (para cookies, p\u00e1ginas, etc.).</p> <code>updated_cookies</code> <code>list[dict[str, Any]] | None</code> <p>Cookies actualizadas tras cerrar la sesi\u00f3n. Se rellena autom\u00e1ticamente en <code>__aexit__</code> del contexto.</p>"},{"location":"api/actions/#storage-de-acciones","title":"Storage de acciones","text":"<p>Reutiliza la secci\u00f3n <code>[storage]</code> de <code>reaper.toml</code>: mismo backend que las cuentas, e <code>actions_dir</code> / <code>actions_table</code> / <code>actions_collection</code> separados para no mezclar registros.</p>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage","title":"reaper.actions.storage.base.BaseActionStorage","text":"<p>               Bases: <code>ABC</code></p> <p>Interfaz abstracta para backends de persistencia de acciones.</p> <p>Todos los m\u00e9todos son as\u00edncronos, igual que en el storage de cuentas, para ser compatibles con backends de archivos y de base de datos.</p> Implementaciones concretas <ul> <li><code>LocalActionStorage</code>   \u2014 JSON en disco (default).</li> <li><code>PostgresActionStorage</code>\u2014 PostgreSQL 15+ v\u00eda <code>asyncpg</code>.</li> <li><code>MongoActionStorage</code>   \u2014 MongoDB v\u00eda <code>motor</code>.</li> </ul>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage.connect","title":"connect  <code>async</code>","text":"<pre><code>connect() -&gt; None\n</code></pre> <p>Inicializa conexiones/esquema si el backend lo requiere.</p> <p>Por defecto no hace nada. <code>PostgresActionStorage</code> crea aqu\u00ed su tabla; <code>MongoActionStorage</code> prepara sus \u00edndices.</p>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Libera los recursos (pools, clientes) del backend.</p> <p>Por defecto no hace nada.</p>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage.add","title":"add  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>add(action: ActionResult) -&gt; None\n</code></pre> <p>Persiste un registro de acci\u00f3n.</p> <p>Si ya existe un registro con el mismo <code>action_id</code>, lo sobreescribe.</p> <p>Parameters:</p> Name Type Description Default <code>action</code> <code>ActionResult</code> <p>Registro <code>ActionResult</code> a persistir.</p> required <p>Raises:</p> Type Description <code>StorageError</code> <p>Si el backend no puede completar la operaci\u00f3n.</p>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage.get","title":"get  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get(action_id: str) -&gt; ActionResult | None\n</code></pre> <p>Carga un registro por su <code>action_id</code>.</p> <p>Parameters:</p> Name Type Description Default <code>action_id</code> <code>str</code> <p>UUID4 del registro.</p> required <p>Returns:</p> Type Description <code>ActionResult | None</code> <p><code>ActionResult</code> si existe, <code>None</code> si no se encuentra.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error de lectura inesperado.</p>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage.list_all","title":"list_all  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>list_all(\n    account_id: str | None = None,\n    action: str | None = None,\n    limit: int = 50,\n) -&gt; list[ActionResult]\n</code></pre> <p>Lista registros de acciones con filtros opcionales.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str | None</code> <p>Si se indica, solo acciones de esa cuenta.</p> <code>None</code> <code>action</code> <code>str | None</code> <p>Si se indica, solo acciones de ese tipo         (valor de <code>ActionType</code>).</p> <code>None</code> <code>limit</code> <code>int</code> <p>N\u00famero m\u00e1ximo de registros a retornar (m\u00e1s recientes         primero). Defecto: 50.</p> <code>50</code> <p>Returns:</p> Type Description <code>list[ActionResult]</code> <p>Lista de <code>ActionResult</code>. Puede estar vac\u00eda.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error de lectura inesperado.</p>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage.delete","title":"delete  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>delete(action_id: str) -&gt; bool\n</code></pre> <p>Elimina un registro de acci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>action_id</code> <code>str</code> <p>UUID4 del registro a eliminar.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se elimin\u00f3, <code>False</code> si no exist\u00eda.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error durante la eliminaci\u00f3n.</p>"},{"location":"api/actions/#reaper.actions.storage.base.BaseActionStorage.count","title":"count  <code>async</code>","text":"<pre><code>count(\n    account_id: str | None = None, action: str | None = None\n) -&gt; int\n</code></pre> <p>Cuenta los registros que coinciden con los filtros.</p> <p>Implementaci\u00f3n por defecto basada en :meth:<code>list_all</code>. Los backends eficientes pueden sobreescribirla con una consulta de agregaci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str | None</code> <p>Filtro por cuenta.</p> <code>None</code> <code>action</code> <code>str | None</code> <p>Filtro por tipo de acci\u00f3n.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>N\u00famero de registros que coinciden.</p>"},{"location":"api/actions/#reaper.actions.storage.base.StorageError","title":"reaper.actions.storage.base.StorageError","text":"<p>               Bases: <code>Exception</code></p> <p>Error en una operaci\u00f3n de almacenamiento.</p> <p>Envuelve excepciones de bajo nivel (IOError, sqlalchemy.exc, etc.) para que <code>AccountManager</code> no dependa de implementaciones concretas.</p>"},{"location":"api/actions/#reaper.actions.storage.local.LocalActionStorage","title":"reaper.actions.storage.local.LocalActionStorage","text":"<pre><code>LocalActionStorage(\n    actions_dir: str | Path = \"data/actions\",\n)\n</code></pre> <p>               Bases: <code>BaseActionStorage</code></p> <p>Almacenamiento de acciones en archivos JSON locales.</p> <p>Parameters:</p> Name Type Description Default <code>actions_dir</code> <code>str | Path</code> <p>Directorio ra\u00edz para almacenar los registros de acciones.          Se crea autom\u00e1ticamente si no existe.          Ejemplo: <code>Path(\"data/actions\")</code>.</p> <code>'data/actions'</code>"},{"location":"api/actions/#reaper.actions.storage.local.LocalActionStorage.add","title":"add  <code>async</code>","text":"<pre><code>add(action: ActionResult) -&gt; None\n</code></pre> <p>Persiste un registro de acci\u00f3n en <code>{action_id}.json</code>.</p>"},{"location":"api/actions/#reaper.actions.storage.local.LocalActionStorage.get","title":"get  <code>async</code>","text":"<pre><code>get(action_id: str) -&gt; ActionResult | None\n</code></pre> <p>Carga el registro de una acci\u00f3n por su <code>action_id</code>.</p>"},{"location":"api/actions/#reaper.actions.storage.local.LocalActionStorage.list_all","title":"list_all  <code>async</code>","text":"<pre><code>list_all(\n    account_id: str | None = None,\n    action: str | None = None,\n    limit: int = 50,\n) -&gt; list[ActionResult]\n</code></pre> <p>Lista registros, opcionalmente filtrados, m\u00e1s recientes primero.</p>"},{"location":"api/actions/#reaper.actions.storage.local.LocalActionStorage.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(action_id: str) -&gt; bool\n</code></pre> <p>Elimina el archivo del registro de una acci\u00f3n.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si exist\u00eda y fue eliminado, <code>False</code> si no exist\u00eda.</p>"},{"location":"api/actions/#reaper.actions.storage.local.LocalActionStorage.count","title":"count  <code>async</code>","text":"<pre><code>count(\n    account_id: str | None = None, action: str | None = None\n) -&gt; int\n</code></pre> <p>Cuenta los registros coincidentes sin materializar la lista completa.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage","title":"reaper.actions.storage.postgres.PostgresActionStorage","text":"<pre><code>PostgresActionStorage(\n    dsn: str,\n    *,\n    pool_size: int = 5,\n    max_overflow: int = 10,\n    timeout: float = 30.0,\n    table: str = \"actions\",\n)\n</code></pre> <p>               Bases: <code>BaseActionStorage</code></p> <p>Almacenamiento de acciones en PostgreSQL v\u00eda <code>asyncpg</code>.</p> <p>Parameters:</p> Name Type Description Default <code>dsn</code> <code>str</code> <p>Cadena de conexi\u00f3n de PostgreSQL, p.ej. <code>\"postgresql://user:pass@localhost:5432/reaper\"</code>.</p> required <code>pool_size</code> <code>int</code> <p>N\u00famero de conexiones del pool. Defecto: 5.</p> <code>5</code> <code>max_overflow</code> <code>int</code> <p>Conexiones extra temporales por encima de <code>pool_size</code> bajo carga. Defecto: 10.</p> <code>10</code> <code>timeout</code> <code>float</code> <p>Segundos de espera para obtener una conexi\u00f3n del pool. Defecto: 30.</p> <code>30.0</code> <code>table</code> <code>str</code> <p>Nombre de la tabla de acciones. Defecto: <code>\"actions\"</code>.</p> <code>'actions'</code> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si no se puede conectar/crear el esquema en <code>connect()</code> o si falla una operaci\u00f3n de lectura/escritura.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage.connect","title":"connect  <code>async</code>","text":"<pre><code>connect() -&gt; None\n</code></pre> <p>Crea el pool de conexiones y la tabla <code>actions</code> si no existe.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si la conexi\u00f3n falla o el DSN es inv\u00e1lido.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Cierra el pool de conexiones, liberando todos los recursos.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage.add","title":"add  <code>async</code>","text":"<pre><code>add(action: ActionResult) -&gt; None\n</code></pre> <p>Inserta o actualiza (UPSERT) un registro de acci\u00f3n.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage.get","title":"get  <code>async</code>","text":"<pre><code>get(action_id: str) -&gt; ActionResult | None\n</code></pre> <p>Carga un registro por su <code>action_id</code> o <code>None</code> si no existe.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage.list_all","title":"list_all  <code>async</code>","text":"<pre><code>list_all(\n    account_id: str | None = None,\n    action: str | None = None,\n    limit: int = 50,\n) -&gt; list[ActionResult]\n</code></pre> <p>Lista registros con filtros opcionales, m\u00e1s recientes primero.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(action_id: str) -&gt; bool\n</code></pre> <p>Elimina la fila de una acci\u00f3n.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se elimin\u00f3, <code>False</code> si no exist\u00eda.</p>"},{"location":"api/actions/#reaper.actions.storage.postgres.PostgresActionStorage.count","title":"count  <code>async</code>","text":"<pre><code>count(\n    account_id: str | None = None, action: str | None = None\n) -&gt; int\n</code></pre> <p>Cuenta registros con filtros opcionales v\u00eda <code>COUNT(*)</code>.</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage","title":"reaper.actions.storage.mongodb.MongoActionStorage","text":"<pre><code>MongoActionStorage(\n    uri: str,\n    database: str = \"reaper\",\n    collection: str = COLLECTION_DEFAULT,\n)\n</code></pre> <p>               Bases: <code>BaseActionStorage</code></p> <p>Almacenamiento de acciones en MongoDB v\u00eda <code>motor</code>.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str</code> <p>Cadena de conexi\u00f3n de MongoDB, p.ej. <code>\"mongodb://localhost:27017\"</code>. Soporta auth y replica sets.</p> required <code>database</code> <code>str</code> <p>Nombre de la base de datos. Defecto: <code>\"reaper\"</code>.</p> <code>'reaper'</code> <code>collection</code> <code>str</code> <p>Nombre de la colecci\u00f3n de acciones. Defecto: <code>\"actions\"</code>.</p> <code>COLLECTION_DEFAULT</code> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si falla una operaci\u00f3n de lectura/escritura.</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage.connect","title":"connect  <code>async</code>","text":"<pre><code>connect() -&gt; None\n</code></pre> <p>Prepara \u00edndices de la colecci\u00f3n (idempotente, perezoso).</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Cierra el cliente, liberando sockets y pools internos.</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage.add","title":"add  <code>async</code>","text":"<pre><code>add(action: ActionResult) -&gt; None\n</code></pre> <p>Inserta o reemplaza el documento de la acci\u00f3n (upsert).</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage.get","title":"get  <code>async</code>","text":"<pre><code>get(action_id: str) -&gt; ActionResult | None\n</code></pre> <p>Carga un registro por su <code>action_id</code> o <code>None</code> si no existe.</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage.list_all","title":"list_all  <code>async</code>","text":"<pre><code>list_all(\n    account_id: str | None = None,\n    action: str | None = None,\n    limit: int = 50,\n) -&gt; list[ActionResult]\n</code></pre> <p>Lista registros con filtros opcionales, m\u00e1s recientes primero.</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(action_id: str) -&gt; bool\n</code></pre> <p>Elimina el documento de la acci\u00f3n.</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se elimin\u00f3, <code>False</code> si no exist\u00eda.</p>"},{"location":"api/actions/#reaper.actions.storage.mongodb.MongoActionStorage.count","title":"count  <code>async</code>","text":"<pre><code>count(\n    account_id: str | None = None, action: str | None = None\n) -&gt; int\n</code></pre> <p>Cuenta documentos con filtros opcionales v\u00eda <code>count_documents</code>.</p>"},{"location":"api/actions/#reaper.actions.storage.config.build_action_storage","title":"reaper.actions.storage.config.build_action_storage","text":"<pre><code>build_action_storage(\n    config: StorageConfig | None = None,\n) -&gt; BaseActionStorage\n</code></pre> <p>Instancia el backend de almacenamiento de acciones de la configuraci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>StorageConfig | None</code> <p>Configuraci\u00f3n de storage (la misma de las cuentas). Si es <code>None</code>, la carga desde el fichero con :func:<code>reaper.auth.storage.load_storage_config</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>BaseActionStorage</code> <p><code>LocalActionStorage</code>, <code>PostgresActionStorage</code> o</p> <code>BaseActionStorage</code> <p><code>MongoActionStorage</code> seg\u00fan <code>config.backend</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si el backend no est\u00e1 soportado o falta su configuraci\u00f3n.</p>"},{"location":"api/actions/#utilidades","title":"Utilidades","text":""},{"location":"api/actions/#reaper.actions.utils.resolve_group","title":"reaper.actions.utils.resolve_group","text":"<pre><code>resolve_group(\n    group: str, base_url: str = \"https://m.facebook.com\"\n) -&gt; str\n</code></pre> <p>Normaliza un identificador de grupo a su URL can\u00f3nica.</p> <p>Acepta: - ID num\u00e9rico puro (<code>\"123456789\"</code>). - URL de grupo (<code>\"https://www.facebook.com/groups/123459/\"</code>).</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p>Identificador o URL del grupo.</p> required <code>base_url</code> <code>str</code> <p>Ra\u00edz de las URLs de navegaci\u00f3n (m.facebook por defecto).</p> <code>'https://m.facebook.com'</code> <p>Returns:</p> Type Description <code>str</code> <p>URL can\u00f3nica del grupo: <code>{base_url}/groups/{id}/</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si el identificador no es un ID num\u00e9rico ni contiene un ID de grupo en su URL.</p>"},{"location":"api/actions/#reaper.actions.utils.extract_group_id","title":"reaper.actions.utils.extract_group_id","text":"<pre><code>extract_group_id(group: str) -&gt; str | None\n</code></pre> <p>Extrae el ID num\u00e9rico de un grupo desde id puro o URL.</p> <p>Parameters:</p> Name Type Description Default <code>group</code> <code>str</code> <p><code>\"123456789\"</code> o <code>\"https://www.facebook.com/groups/123456789/\"</code>.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>ID del grupo como string, o <code>None</code> si no se puede determinar.</p>"},{"location":"api/actions/#reaper.actions.utils.resolve_profile_url","title":"reaper.actions.utils.resolve_profile_url","text":"<pre><code>resolve_profile_url(\n    cookies: list[dict[str, Any]] | None,\n    base_url: str = \"https://www.facebook.com\",\n) -&gt; str | None\n</code></pre> <p>Deriva la URL del perfil propio desde el cookie <code>c_user</code>.</p> <p>Facebook identifica al usuario autenticado con el cookie num\u00e9rico <code>c_user</code>; su perfil es <code>https://www.facebook.com/profile.php?id={id}</code>. Ese es el destino de navegaci\u00f3n usado para publicar en el muro propio.</p> <p>Parameters:</p> Name Type Description Default <code>cookies</code> <code>list[dict[str, Any]] | None</code> <p>Cookie de sesi\u00f3n en formato Playwright.</p> required <code>base_url</code> <code>str</code> <p>Ra\u00edz de las URLs (www.facebook.com por defecto).</p> <code>'https://www.facebook.com'</code> <p>Returns:</p> Type Description <code>str | None</code> <p>URL del perfil, o <code>None</code> si no hay <code>c_user</code> (sesi\u00f3n an\u00f3nima).</p>"},{"location":"api/actions/#reaper.actions.utils.validate_image_paths","title":"reaper.actions.utils.validate_image_paths","text":"<pre><code>validate_image_paths(\n    image_paths: list[str] | None,\n) -&gt; list[Path]\n</code></pre> <p>Valida que las rutas de imagen existan y sean de una extensi\u00f3n soportada.</p> <p>Parameters:</p> Name Type Description Default <code>image_paths</code> <code>list[str] | None</code> <p>Rutas locales de im\u00e1genes. <code>None</code> o <code>[]</code> = sin im\u00e1genes.</p> required <p>Returns:</p> Type Description <code>list[Path]</code> <p>Lista de <code>Path</code> absolutos validados (posiblemente vac\u00eda).</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si una ruta no existe, es un directorio, o su extensi\u00f3n no es soportada.</p>"},{"location":"api/actions/#reaper.actions.utils.post_id_from_url","title":"reaper.actions.utils.post_id_from_url","text":"<pre><code>post_id_from_url(url: str | None) -&gt; str | None\n</code></pre> <p>Extrae el post_id num\u00e9rico de una URL de post de Facebook.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str | None</code> <p>URL de un post (story.php, /posts/... o /permalink/...).</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>ID del post como string, o <code>None</code> si la URL no contiene ninguno.</p>"},{"location":"api/actions/#reaper.actions.utils.cookies_differ","title":"reaper.actions.utils.cookies_differ","text":"<pre><code>cookies_differ(\n    original: list[dict[str, Any]],\n    updated: list[dict[str, Any]],\n) -&gt; bool\n</code></pre> <p>Compara cookies por <code>(name \u2192 value, expires)</code> para refrescar sesi\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>original</code> <code>list[dict[str, Any]]</code> <p>Cookies antes de la sesi\u00f3n.</p> required <code>updated</code> <code>list[dict[str, Any]]</code> <p>Cookies le\u00eddas tras la sesi\u00f3n.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si alg\u00fan valor/expiraci\u00f3n cambi\u00f3 o el conjunto de nombres</p> <code>bool</code> <p>difiere (el servidor rot\u00f3 tokens o extendi\u00f3 TTLs).</p>"},{"location":"api/actions/#configuracion-reapertoml","title":"Configuraci\u00f3n (<code>reaper.toml</code>)","text":"<p>El backend de acciones se elige con la misma secci\u00f3n <code>[storage]</code> de las cuentas. Los nombres de los recursos separados son opcionales:</p> <pre><code>[storage]\nbackend = \"local\"\naccounts_dir = \"data/accounts\"\nactions_dir  = \"data/actions\"        # default de LocalActionStorage\n\n[storage.postgres]\ndsn = \"postgresql://user:pass@localhost:5432/reaper\"\nactions_table = \"actions\"            # default de PostgresActionStorage\n\n[storage.mongodb]\nuri = \"mongodb://localhost:27017\"\nactions_collection = \"actions\"       # default de MongoActionStorage\n</code></pre> <p>Si no se indica el nombre separado, se usa el valor por defecto de cada backend. La conexi\u00f3n y credenciales se comparten con el storage de cuentas.</p>"},{"location":"api/auth/","title":"API Reference \u2014 <code>reaper.auth</code>","text":""},{"location":"api/auth/#accountmanager","title":"<code>AccountManager</code>","text":""},{"location":"api/auth/#reaper.auth.account_manager.AccountManager","title":"reaper.auth.account_manager.AccountManager","text":"<pre><code>AccountManager(\n    storage: BaseAccountStorage | None = None,\n    accounts_dir: str | Path = \"data/accounts\",\n    cookie_refresh_threshold: int = DEFAULT_COOKIE_REFRESH_THRESHOLD,\n)\n</code></pre> <p>Gestor central de cuentas autenticadas para Reaper.</p> <p>Responsabilidades: - CRUD de cuentas (add, remove, get, list). - Importaci\u00f3n y exportaci\u00f3n de cookies (formato Playwright). - Actualizaci\u00f3n de estado y actividad tras cada petici\u00f3n. - Selecci\u00f3n de la cuenta \u00f3ptima para la siguiente petici\u00f3n. - Notificaci\u00f3n de umbral de refresco de cookies.</p> <p>Parameters:</p> Name Type Description Default <code>storage</code> <code>BaseAccountStorage | None</code> <p>Backend de almacenamiento. Si <code>None</code>, usa <code>LocalFileStorage</code>      con la ruta <code>accounts_dir</code>.</p> <code>None</code> <code>accounts_dir</code> <code>str | Path</code> <p>Directorio ra\u00edz para <code>LocalFileStorage</code>.           Ignorado si se pasa <code>storage</code> expl\u00edcitamente.</p> <code>'data/accounts'</code> <code>cookie_refresh_threshold</code> <code>int</code> <p>Peticiones exitosas antes de marcar la cuenta                       como <code>NEEDS_REFRESH</code>. Defecto: 50.</p> <code>DEFAULT_COOKIE_REFRESH_THRESHOLD</code> <p>Ejemplo de inyecci\u00f3n de storage personalizado (migraci\u00f3n a BD)::</p> <pre><code>from myapp.storage import PostgresAccountStorage\n\nmanager = AccountManager(storage=PostgresAccountStorage(db_url=\"...\"))\n</code></pre>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.add_account","title":"add_account  <code>async</code>","text":"<pre><code>add_account(\n    platform: str,\n    username: str,\n    cookies: list[dict[str, Any]] | None = None,\n    cookies_file: str | Path | None = None,\n    email: str | None = None,\n    notes: str = \"\",\n) -&gt; AccountProfile\n</code></pre> <p>Registra una nueva cuenta en el sistema.</p> <p>Las cookies pueden proporcionarse de dos formas mutuamente excluyentes: - <code>cookies</code>:       Lista de dicts (formato Playwright) en memoria. - <code>cookies_file</code>:  Ruta a un archivo JSON externo con las cookies.</p> <p>Si no se proporcionan cookies, la cuenta se crea con estado <code>UNKNOWN</code> y sin cookies; el operador deber\u00e1 importarlas despu\u00e9s con <code>import_cookies()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>username</code> <code>str</code> <p>Nombre de usuario de la cuenta.</p> required <code>cookies</code> <code>list[dict[str, Any]] | None</code> <p>Lista de dicts de cookies (formato Playwright).</p> <code>None</code> <code>cookies_file</code> <code>str | Path | None</code> <p>Ruta alternativa a un archivo JSON de cookies.</p> <code>None</code> <code>email</code> <code>str | None</code> <p>Email asociado (informativo, opcional).</p> <code>None</code> <code>notes</code> <code>str</code> <p>Notas libres del operador.</p> <code>''</code> <p>Returns:</p> Type Description <code>AccountProfile</code> <p><code>AccountProfile</code> creado y persistido.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si la plataforma no es soportada o ambas fuentes         de cookies se proporcionan simult\u00e1neamente.</p> <code>StorageError</code> <p>Si no se puede persistir el perfil.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.remove_account","title":"remove_account  <code>async</code>","text":"<pre><code>remove_account(account_id: str) -&gt; bool\n</code></pre> <p>Elimina una cuenta y todos sus archivos asociados (perfil + cookies).</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta a eliminar.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si exist\u00eda y fue eliminada, <code>False</code> si no exist\u00eda.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.get_account","title":"get_account  <code>async</code>","text":"<pre><code>get_account(account_id: str) -&gt; AccountProfile | None\n</code></pre> <p>Carga el perfil completo de una cuenta por su ID.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>AccountProfile | None</code> <p><code>AccountProfile</code> si existe, <code>None</code> si no se encuentra.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.list_accounts","title":"list_accounts  <code>async</code>","text":"<pre><code>list_accounts(\n    platform: str | None = None,\n) -&gt; list[AccountProfile]\n</code></pre> <p>Lista todas las cuentas registradas, con actividad y estado actuales.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str | None</code> <p>Filtrar por <code>\"facebook\"</code> o <code>\"instagram\"</code>.       <code>None</code> retorna todas las plataformas.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[AccountProfile]</code> <p>Lista de <code>AccountProfile</code>. Puede estar vac\u00eda.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.update_status","title":"update_status  <code>async</code>","text":"<pre><code>update_status(\n    account_id: str,\n    status: AccountStatus,\n    notes: str | None = None,\n) -&gt; bool\n</code></pre> <p>Actualiza el estado operativo de una cuenta.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <code>status</code> <code>AccountStatus</code> <p>Nuevo estado. Ver <code>AccountStatus</code>.</p> required <code>notes</code> <code>str | None</code> <p>Notas opcionales a a\u00f1adir al campo <code>notes</code> del perfil.</p> <code>None</code> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se actualiz\u00f3, <code>False</code> si la cuenta no existe.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.import_cookies","title":"import_cookies  <code>async</code>","text":"<pre><code>import_cookies(\n    account_id: str, cookies: list[dict[str, Any]]\n) -&gt; bool\n</code></pre> <p>Importa cookies en formato Playwright para una cuenta existente.</p> <p>Guarda las cookies en disco, resetea el contador de refresco y marca la cuenta como <code>ACTIVE</code>.</p> <p>Formato de cookies (Playwright)::</p> <pre><code>[\n    {\n        \"name\": \"c_user\",\n        \"value\": \"12345678\",\n        \"domain\": \".facebook.com\",\n        \"path\": \"/\",\n        \"expires\": 1735689600.0,\n        \"httpOnly\": false,\n        \"secure\": true,\n        \"sameSite\": \"None\"\n    },\n    ...\n]\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta destino.</p> required <code>cookies</code> <code>list[dict[str, Any]]</code> <p>Lista de dicts de cookies en formato Playwright.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se importaron correctamente, <code>False</code> si la cuenta</p> <code>bool</code> <p>no existe.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si <code>cookies</code> est\u00e1 vac\u00edo o no es una lista.</p> <code>StorageError</code> <p>Si no se puede escribir el archivo.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.import_cookies_from_file","title":"import_cookies_from_file  <code>async</code>","text":"<pre><code>import_cookies_from_file(\n    account_id: str, cookies_file: str | Path\n) -&gt; bool\n</code></pre> <p>Importa cookies desde un archivo JSON externo.</p> <p>\u00datil cuando el operador exporta cookies desde el navegador (extensi\u00f3n EditThisCookie, Cookie-Editor, etc.) y las guarda en un archivo antes de pasarlas a Reaper.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta destino.</p> required <code>cookies_file</code> <code>str | Path</code> <p>Ruta al archivo JSON de cookies externo.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se importaron correctamente.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Si el archivo no existe.</p> <code>ValueError</code> <p>Si el archivo no contiene una lista v\u00e1lida.</p> <code>StorageError</code> <p>Si no se puede escribir internamente.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.export_cookies","title":"export_cookies  <code>async</code>","text":"<pre><code>export_cookies(\n    account_id: str,\n) -&gt; list[dict[str, Any]] | None\n</code></pre> <p>Exporta las cookies de una cuenta en formato Playwright.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]] | None</code> <p>Lista de dicts de cookies, o <code>None</code> si la cuenta no tiene cookies.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.save_cookies","title":"save_cookies  <code>async</code>","text":"<pre><code>save_cookies(\n    platform: str,\n    account_id: str,\n    cookies: list[dict[str, Any]],\n) -&gt; str\n</code></pre> <p>Persiste cookies de una cuenta en el backend activo.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <code>cookies</code> <code>list[dict[str, Any]]</code> <p>Lista de dicts de cookies en formato Playwright.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Locator (string opaco) del lugar donde quedaron guardadas.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si el backend no puede persistirlas.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.load_cookies","title":"load_cookies  <code>async</code>","text":"<pre><code>load_cookies(\n    platform: str, account_id: str\n) -&gt; list[dict[str, Any]] | None\n</code></pre> <p>Lee las cookies de una cuenta desde el backend activo.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]] | None</code> <p>Lista de dicts de cookies, o <code>None</code> si la cuenta no tiene.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si el backend no puede leerlas.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.delete_cookies","title":"delete_cookies  <code>async</code>","text":"<pre><code>delete_cookies(platform: str, account_id: str) -&gt; bool\n</code></pre> <p>Elimina las cookies de una cuenta del backend activo.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si exist\u00edan y se eliminaron, <code>False</code> si no exist\u00edan.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Cierra los recursos del backend (pools de conexi\u00f3n, clientes).</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.get_account_for_request","title":"get_account_for_request  <code>async</code>","text":"<pre><code>get_account_for_request(\n    platform: str, *, preferred: str | None = None\n) -&gt; AccountProfile | None\n</code></pre> <p>Selecciona la cuenta a usar en una petici\u00f3n.</p> <p>Si se indica <code>preferred</code> (account_id, username o ID de usuario de plataforma), se usa esa cuenta concreta, ignorando el rotador. Es la forma de forzar manualmente una cuenta para una petici\u00f3n determinada.</p> <p>Si no hay <code>preferred</code>, carga todas las cuentas de la plataforma y delega la selecci\u00f3n al <code>AccountRotator</code> (comportamiento original).</p> <p>Retorna <code>None</code> (sin excepci\u00f3n) cuando no hay cuentas disponibles, permitiendo que el scraper caiga en modo an\u00f3nimo.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>preferred</code> <code>str | None</code> <p>Identificador opcional de la cuenta a forzar (<code>account_id</code>, <code>username</code> o ID de usuario de la plataforma).</p> <code>None</code> <p>Returns:</p> Type Description <code>AccountProfile | None</code> <p><code>AccountProfile</code> seleccionada, o <code>None</code> si no hay candidatas.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.resolve_account","title":"resolve_account  <code>async</code>","text":"<pre><code>resolve_account(\n    identifier: str, platform: str | None = None\n) -&gt; AccountProfile | None\n</code></pre> <p>Localiza una cuenta por su identificador.</p> <p>Acepta, en orden de precedencia: 1. <code>account_id</code> (UUID4 interno de la cuenta). 2. <code>username</code> (nombre de usuario o email registrado). 3. ID de usuario de la plataforma (<code>c_user</code> en Facebook,    <code>ds_user_id</code> en Instagram), buscado en las cookies.</p> <p>Parameters:</p> Name Type Description Default <code>identifier</code> <code>str</code> <p>El valor a buscar (account_id, username o ID de usuario).</p> required <code>platform</code> <code>str | None</code> <p>Restringe la b\u00fasqueda a <code>\"facebook\"</code> o         <code>\"instagram\"</code>. <code>None</code> busca en ambas plataformas.</p> <code>None</code> <p>Returns:</p> Type Description <code>AccountProfile | None</code> <p><code>AccountProfile</code> si se encontr\u00f3 exactamente una cuenta,</p> <code>AccountProfile | None</code> <p><code>None</code> si no hay coincidencia.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si el backend falla al leer.</p>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.record_success","title":"record_success  <code>async</code>","text":"<pre><code>record_success(account_id: str) -&gt; None\n</code></pre> <p>Registra una petici\u00f3n exitosa y verifica el umbral de refresco.</p> <p>Si se alcanza el umbral <code>cookie_refresh_threshold</code>, la cuenta se marca como <code>NEEDS_REFRESH</code> y se emite un aviso. La cuenta sigue siendo seleccionable pero con score reducido.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta que realiz\u00f3 la petici\u00f3n.</p> required"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.record_failure","title":"record_failure  <code>async</code>","text":"<pre><code>record_failure(account_id: str, error_message: str) -&gt; None\n</code></pre> <p>Registra una petici\u00f3n fallida y eval\u00faa si aplicar rate-limit.</p> <p>Si los \u00faltimos <code>CONSECUTIVE_FAILURES_FOR_RATE_LIMIT</code> fallos son consecutivos, marca la cuenta como <code>RATE_LIMITED</code> con penalizaci\u00f3n en el rotador.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta que realiz\u00f3 la petici\u00f3n.</p> required <code>error_message</code> <code>str</code> <p>Descripci\u00f3n del error ocurrido.</p> required"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.pool_summary","title":"pool_summary  <code>async</code>","text":"<pre><code>pool_summary(platform: str | None = None) -&gt; dict[str, Any]\n</code></pre> <p>Retorna un resumen del estado del pool de cuentas.</p> <p>\u00datil para monitoreo y diagn\u00f3stico del operador.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str | None</code> <p>Filtrar por plataforma, o <code>None</code> para todas.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict con m\u00e9tricas agregadas del pool.</p> <p>Ejemplo de retorno::</p> <pre><code>{\n    \"total\": 5,\n    \"active\": 3,\n    \"needs_refresh\": 1,\n    \"rate_limited\": 1,\n    \"suspended\": 0,\n    \"cookie_expired\": 0,\n    \"unknown\": 0,\n    \"by_platform\": {\n        \"facebook\": {\"total\": 3, \"active\": 2, ...},\n        \"instagram\": {\"total\": 2, \"active\": 1, ...},\n    }\n}\n</code></pre>"},{"location":"api/auth/#reaper.auth.account_manager.AccountManager.get_ranked_accounts","title":"get_ranked_accounts  <code>async</code>","text":"<pre><code>get_ranked_accounts(platform: str) -&gt; list[dict[str, Any]]\n</code></pre> <p>Retorna las cuentas rankeadas por score del rotador (para diagn\u00f3stico).</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>Lista de dicts con <code>username</code>, <code>status</code>, <code>score</code> y</p> <code>list[dict[str, Any]]</code> <p>m\u00e9tricas clave de actividad.</p>"},{"location":"api/auth/#accountprofile","title":"<code>AccountProfile</code>","text":""},{"location":"api/auth/#reaper.auth.models.AccountProfile","title":"reaper.auth.models.AccountProfile  <code>dataclass</code>","text":"<pre><code>AccountProfile(\n    account_id: str = (lambda: str(uuid.uuid4()))(),\n    platform: str = \"facebook\",\n    username: str = \"\",\n    email: str | None = None,\n    status: AccountStatus = AccountStatus.UNKNOWN,\n    cookies_path: str | None = None,\n    activity: AccountActivity = AccountActivity(),\n    created_at: str = _now_iso(),\n    updated_at: str = _now_iso(),\n    notes: str = \"\",\n)\n</code></pre> <p>Perfil completo de una cuenta autenticada gestionada por Reaper.</p> <p>Cada <code>AccountProfile</code> representa una cuenta de Facebook o Instagram con sus credenciales de sesi\u00f3n (cookies), su estado operativo y el historial de actividad que gu\u00eda la rotaci\u00f3n inteligente.</p> <p>La carpeta de cookies se gestiona autom\u00e1ticamente por <code>AccountManager</code> bajo la ruta <code>{accounts_dir}/{platform}/{account_id}/cookies.json</code>.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>Identificador \u00fanico (UUID4). Generado autom\u00e1ticamente         si no se especifica.</p> <code>(lambda: str(uuid4()))()</code> <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> <code>'facebook'</code> <code>username</code> <code>str</code> <p>Nombre de usuario o identificador legible de la cuenta.</p> <code>''</code> <code>email</code> <code>str | None</code> <p>Email asociado a la cuenta (opcional, no se usa para auth).</p> <code>None</code> <code>status</code> <code>AccountStatus</code> <p>Estado operativo. Ver <code>AccountStatus</code>.</p> <code>UNKNOWN</code> <code>cookies_path</code> <code>str | None</code> <p>Ruta absoluta al archivo de cookies JSON en formato           Playwright. <code>None</code> si todav\u00eda no se han importado cookies.</p> <code>None</code> <code>activity</code> <code>AccountActivity</code> <p>Historial de actividad. Se crea vac\u00edo por defecto.</p> <code>AccountActivity()</code> <code>created_at</code> <code>str</code> <p>ISO-8601 de creaci\u00f3n del perfil.</p> <code>_now_iso()</code> <code>updated_at</code> <code>str</code> <p>ISO-8601 de la \u00faltima modificaci\u00f3n.</p> <code>_now_iso()</code> <code>notes</code> <code>str</code> <p>Campo libre para anotaciones del operador.</p> <code>''</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si <code>platform</code> no est\u00e1 en <code>SUPPORTED_PLATFORMS</code>.</p>"},{"location":"api/auth/#reaper.auth.models.AccountProfile.has_cookies","title":"has_cookies  <code>property</code>","text":"<pre><code>has_cookies: bool\n</code></pre> <p>True si la cuenta tiene cookies asociadas.</p> <p>Se basa en <code>cookies_path</code> (locator opaco). Cada backend es responsable de dejar este campo a <code>None</code> cuando no hay cookies: <code>LocalFileStorage</code> lo reconstruye seg\u00fan exista <code>cookies.json</code>, y los backends de BD lo guardan como identificador simb\u00f3lico.</p>"},{"location":"api/auth/#reaper.auth.models.AccountProfile.is_selectable","title":"is_selectable  <code>property</code>","text":"<pre><code>is_selectable: bool\n</code></pre> <p>True si el rotador puede seleccionar esta cuenta.</p> <p>Una cuenta es seleccionable cuando: - Tiene cookies disponibles en disco. - Su estado no est\u00e1 en <code>NON_SELECTABLE_STATUSES</code>.</p>"},{"location":"api/auth/#reaper.auth.models.AccountProfile.touch_updated","title":"touch_updated","text":"<pre><code>touch_updated() -&gt; None\n</code></pre> <p>Actualiza <code>updated_at</code> al momento presente.</p>"},{"location":"api/auth/#reaper.auth.models.AccountProfile.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Serializa el perfil completo a un dict JSON-compatible.</p> <p>El dict resultante es apto para <code>json.dumps()</code> sin <code>default=str</code> ya que todos los valores son tipos primitivos de Python.</p>"},{"location":"api/auth/#reaper.auth.models.AccountProfile.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; 'AccountProfile'\n</code></pre> <p>Deserializa desde un dict (tipicamente le\u00eddo de JSON).</p> <p>Tolerante a versiones: campos ausentes usan valores por defecto.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Dict con los campos del perfil.</p> required <p>Returns:</p> Type Description <code>'AccountProfile'</code> <p>Instancia de <code>AccountProfile</code> reconstituida.</p>"},{"location":"api/auth/#accountactivity","title":"<code>AccountActivity</code>","text":""},{"location":"api/auth/#reaper.auth.models.AccountActivity","title":"reaper.auth.models.AccountActivity  <code>dataclass</code>","text":"<pre><code>AccountActivity(\n    total_requests: int = 0,\n    successful_requests: int = 0,\n    failed_requests: int = 0,\n    last_used_at: str | None = None,\n    last_success_at: str | None = None,\n    last_error_at: str | None = None,\n    recent_errors: list[str] = list(),\n    requests_since_cookie_refresh: int = 0,\n)\n</code></pre> <p>Historial de actividad y m\u00e9tricas operativas de una cuenta.</p> <p>Este objeto es el n\u00facleo del sistema de rotaci\u00f3n inteligente: el <code>AccountRotator</code> lo lee para calcular un score de salud y decidir qu\u00e9 cuenta usar en la siguiente petici\u00f3n.</p> <p>Todos los campos son serializables (int, float, str, list[str], None).</p> <p>Attributes:</p> Name Type Description <code>total_requests</code> <code>int</code> <p>Total acumulado de peticiones realizadas.</p> <code>successful_requests</code> <code>int</code> <p>Peticiones que retornaron datos v\u00e1lidos.</p> <code>failed_requests</code> <code>int</code> <p>Peticiones con error o sin datos.</p> <code>last_used_at</code> <code>str | None</code> <p>ISO-8601 del \u00faltimo uso (o None).</p> <code>last_success_at</code> <code>str | None</code> <p>ISO-8601 del \u00faltimo \u00e9xito (o None).</p> <code>last_error_at</code> <code>str | None</code> <p>ISO-8601 del \u00faltimo error (o None).</p> <code>recent_errors</code> <code>list[str]</code> <p>Lista FIFO con los \u00faltimos N mensajes de error.                              M\u00e1ximo <code>MAX_RECENT_ERRORS</code> entradas.</p> <code>requests_since_cookie_refresh</code> <code>int</code> <p>Contador que se resetea al importar cookies nuevas.                              El <code>AccountManager</code> usa este valor para saber                              cu\u00e1ndo solicitar un refresco de cookies.</p>"},{"location":"api/auth/#reaper.auth.models.AccountActivity.success_rate","title":"success_rate  <code>property</code>","text":"<pre><code>success_rate: float\n</code></pre> <p>Tasa de \u00e9xito entre 0.0 y 1.0. Retorna 1.0 si no hay peticiones.</p>"},{"location":"api/auth/#reaper.auth.models.AccountActivity.failure_rate","title":"failure_rate  <code>property</code>","text":"<pre><code>failure_rate: float\n</code></pre> <p>Tasa de fallos entre 0.0 y 1.0.</p>"},{"location":"api/auth/#reaper.auth.models.AccountActivity.record_success","title":"record_success","text":"<pre><code>record_success() -&gt; None\n</code></pre> <p>Registra una petici\u00f3n exitosa actualizando todos los contadores.</p>"},{"location":"api/auth/#reaper.auth.models.AccountActivity.record_failure","title":"record_failure","text":"<pre><code>record_failure(error_message: str) -&gt; None\n</code></pre> <p>Registra una petici\u00f3n fallida y a\u00f1ade el error a la cola FIFO.</p> <p>Parameters:</p> Name Type Description Default <code>error_message</code> <code>str</code> <p>Descripci\u00f3n del error ocurrido.            Se almacena con timestamp para facilitar el diagn\u00f3stico.</p> required"},{"location":"api/auth/#reaper.auth.models.AccountActivity.reset_cookie_refresh_counter","title":"reset_cookie_refresh_counter","text":"<pre><code>reset_cookie_refresh_counter() -&gt; None\n</code></pre> <p>Resetea el contador de peticiones desde el \u00faltimo refresco de cookies.</p> <p>Llamado por <code>AccountManager</code> cuando se importan nuevas cookies para la cuenta.</p>"},{"location":"api/auth/#reaper.auth.models.AccountActivity.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Serializa la actividad a un dict JSON-compatible.</p>"},{"location":"api/auth/#reaper.auth.models.AccountActivity.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; 'AccountActivity'\n</code></pre> <p>Deserializa desde un dict (tipicamente le\u00eddo de JSON).</p> <p>Campos ausentes usan el valor por defecto del dataclass, lo que hace la carga tolerante a versiones anteriores del schema.</p>"},{"location":"api/auth/#accountstatus","title":"<code>AccountStatus</code>","text":""},{"location":"api/auth/#reaper.auth.models.AccountStatus","title":"reaper.auth.models.AccountStatus","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Estado operativo de una cuenta autenticada.</p> <p>Se usa <code>str</code> como mixin para que el valor sea directamente serializable a JSON sin conversi\u00f3n extra (<code>json.dumps</code> acepta str).</p> Valores <p>ACTIVE:          La cuenta est\u00e1 operativa y sus cookies son v\u00e1lidas. SUSPENDED:       La cuenta fue suspendida por la plataforma.                  No se seleccionar\u00e1 para rotaci\u00f3n. RATE_LIMITED:    La plataforma devolvi\u00f3 se\u00f1ales de rate-limit.                  El rotador la evitar\u00e1 temporalmente. COOKIE_EXPIRED:  Las cookies han expirado o son inv\u00e1lidas.                  Requiere refresco manual antes de volver a usarla. NEEDS_REFRESH:   Las cookies est\u00e1n pr\u00f3ximas a necesitar refresco                  (umbral de peticiones alcanzado). Se puede seguir                  usando pero con prioridad reducida. UNKNOWN:         Estado inicial; no se ha verificado todav\u00eda.</p>"},{"location":"api/auth/#accountrotator","title":"<code>AccountRotator</code>","text":""},{"location":"api/auth/#reaper.auth.rotator.AccountRotator","title":"reaper.auth.rotator.AccountRotator","text":"<pre><code>AccountRotator(cookie_refresh_threshold: int = 50)\n</code></pre> <p>Selecciona la cuenta \u00f3ptima para la siguiente petici\u00f3n.</p> <p>El rotador es stateless: no almacena estado entre llamadas. Recibe el pool completo de cuentas en cada llamada a <code>select()</code> y calcula el ranking en el momento.</p> <p>Esta decisi\u00f3n de dise\u00f1o simplifica la integraci\u00f3n: <code>AccountManager</code> llama a <code>select()</code> pasando las cuentas actualizadas sin que el rotador necesite ser notificado de cambios.</p> <p>Parameters:</p> Name Type Description Default <code>cookie_refresh_threshold</code> <code>int</code> <p>N\u00famero de peticiones exitosas tras el cual una cuenta se considera \"pr\u00f3xima a necesitar refresco\". Se usa para penalizar su score y dar preferencia a otras cuentas. Debe coincidir con el valor configurado en <code>AccountManager</code>.</p> <code>50</code>"},{"location":"api/auth/#reaper.auth.rotator.AccountRotator.select","title":"select","text":"<pre><code>select(\n    accounts: list[AccountProfile], platform: str\n) -&gt; AccountProfile | None\n</code></pre> <p>Selecciona la cuenta con mayor score para la plataforma indicada.</p> <p>Proceso: 1. Filtra cuentas de la plataforma correcta. 2. Filtra cuentas no seleccionables (<code>NON_SELECTABLE_STATUSES</code> o sin cookies). 3. Punt\u00faa las candidatas. 4. Retorna la de mayor score.</p> <p>Parameters:</p> Name Type Description Default <code>accounts</code> <code>list[AccountProfile]</code> <p>Pool completo de cuentas (todas las plataformas).</p> required <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <p>Returns:</p> Type Description <code>AccountProfile | None</code> <p>La <code>AccountProfile</code> con mejor score, o <code>None</code> si no hay</p> <code>AccountProfile | None</code> <p>candidatas disponibles.</p>"},{"location":"api/auth/#reaper.auth.rotator.AccountRotator.rank_all","title":"rank_all","text":"<pre><code>rank_all(\n    accounts: list[AccountProfile], platform: str\n) -&gt; list[ScoredAccount]\n</code></pre> <p>Retorna el ranking completo de candidatas para diagn\u00f3stico.</p> <p>A diferencia de <code>select()</code>, no filtra por estado no seleccionable: retorna todas las cuentas de la plataforma con su score calculado. \u00datil para mostrar el estado del pool al operador.</p> <p>Parameters:</p> Name Type Description Default <code>accounts</code> <code>list[AccountProfile]</code> <p>Pool completo de cuentas.</p> required <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <p>Returns:</p> Type Description <code>list[ScoredAccount]</code> <p>Lista de <code>ScoredAccount</code> ordenada de mayor a menor score.</p>"},{"location":"api/auth/#login-interactivo","title":"Login interactivo","text":""},{"location":"api/auth/#reaper.auth.login.run_login_new_account","title":"reaper.auth.login.run_login_new_account  <code>async</code>","text":"<pre><code>run_login_new_account(\n    manager: AccountManager,\n    platform: str,\n    username: str,\n    email: str | None = None,\n    notes: str = \"\",\n) -&gt; AccountProfile | None\n</code></pre> <p>Flujo completo de login para registrar una cuenta nueva.</p> <p>Combina la apertura del navegador, la captura de cookies y el registro en el <code>AccountManager</code> en una sola llamada. Es la forma recomendada de a\u00f1adir una cuenta cuando el operador tambi\u00e9n va a hacer el login en el mismo momento.</p> <p>Si prefieres registrar la cuenta primero y hacer el login m\u00e1s tarde, usa <code>manager.add_account()</code> y despu\u00e9s <code>run_login_refresh()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>manager</code> <code>AccountManager</code> <p>Instancia de <code>AccountManager</code> donde se registrar\u00e1 la cuenta.</p> required <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>username</code> <code>str</code> <p>Nombre de usuario o email de la cuenta (solo informativo).</p> required <code>email</code> <code>str | None</code> <p>Email asociado a la cuenta (opcional, solo informativo).</p> <code>None</code> <code>notes</code> <code>str</code> <p>Notas libres para identificar la cuenta.</p> <code>''</code> <p>Returns:</p> Type Description <code>AccountProfile | None</code> <p><code>AccountProfile</code> registrado y con cookies importadas,</p> <code>AccountProfile | None</code> <p>o <code>None</code> si el login fall\u00f3 o fue cancelado.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si la plataforma no est\u00e1 soportada.</p>"},{"location":"api/auth/#reaper.auth.login.run_login_refresh","title":"reaper.auth.login.run_login_refresh  <code>async</code>","text":"<pre><code>run_login_refresh(\n    manager: AccountManager, account_id: str\n) -&gt; bool\n</code></pre> <p>Flujo de login para refrescar las cookies de una cuenta existente.</p> <p>Ideal para responder al aviso <code>NEEDS_REFRESH</code> que emite el <code>AccountManager</code> cuando se alcanza el umbral de peticiones. Tambi\u00e9n \u00fatil para <code>COOKIE_EXPIRED</code> o <code>RATE_LIMITED</code>.</p> <p>El flujo: 1. Carga el perfil del <code>AccountManager</code> para confirmar que existe. 2. Abre Firefox con la p\u00e1gina de login de la plataforma correspondiente. 3. El usuario completa el login manualmente. 4. Las cookies nuevas reemplazan las antiguas v\u00eda <code>manager.import_cookies()</code>. 5. El estado vuelve a <code>ACTIVE</code> y el contador de refresco se resetea a 0.</p> <p>Parameters:</p> Name Type Description Default <code>manager</code> <code>AccountManager</code> <p>Instancia de <code>AccountManager</code> donde est\u00e1 la cuenta.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta a refrescar.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si el refresco fue exitoso, <code>False</code> si fall\u00f3 o fue cancelado.</p>"},{"location":"api/auth/#storage","title":"Storage","text":""},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage","title":"reaper.auth.storage.base.BaseAccountStorage","text":"<p>               Bases: <code>ABC</code></p> <p>Interfaz abstracta para backends de persistencia de cuentas.</p> <p>Todos los m\u00e9todos son as\u00edncronos para ser compatibles tanto con backends de archivos (asyncio + aiofiles) como con ORMs as\u00edncronos (SQLAlchemy async, Motor para MongoDB, etc.).</p> Implementaciones concretas <ul> <li><code>LocalFileStorage</code>  \u2014 JSON en disco (default, incluida en la librer\u00eda)</li> <li><code>PostgresStorage</code>   \u2014 PostgreSQL 15+ v\u00eda <code>asyncpg</code></li> <li><code>MongoStorage</code>      \u2014 MongoDB v\u00eda <code>motor</code></li> </ul> <p>Cada backend persiste tanto el perfil como las cookies de sesi\u00f3n, por lo que el <code>AccountManager</code> puede operar de forma id\u00e9ntica sin importar el backend seleccionado (disco o base de datos).</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.save","title":"save  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>save(account: AccountProfile) -&gt; None\n</code></pre> <p>Guarda o sobreescribe el perfil completo de una cuenta.</p> <p>Si la cuenta ya existe (mismo <code>account_id</code>), la sobreescribe. Si no existe, la crea.</p> <p>Parameters:</p> Name Type Description Default <code>account</code> <code>AccountProfile</code> <p>Perfil completo a persistir.</p> required <p>Raises:</p> Type Description <code>StorageError</code> <p>Si el backend no puede completar la operaci\u00f3n.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.load","title":"load  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>load(account_id: str) -&gt; AccountProfile | None\n</code></pre> <p>Carga un perfil por su identificador \u00fanico.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta a cargar.</p> required <p>Returns:</p> Type Description <code>AccountProfile | None</code> <p><code>AccountProfile</code> si existe, <code>None</code> si no se encuentra.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error de lectura inesperado.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.delete","title":"delete  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>delete(account_id: str) -&gt; bool\n</code></pre> <p>Elimina una cuenta y todos sus datos asociados del backend.</p> <p>Nota: El archivo de cookies se elimina por <code>AccountManager</code>, no por el storage, para mantener la separaci\u00f3n de responsabilidades.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta a eliminar.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se elimin\u00f3, <code>False</code> si no exist\u00eda.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error durante la eliminaci\u00f3n.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.list_all","title":"list_all  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>list_all(\n    platform: str | None = None,\n) -&gt; list[AccountProfile]\n</code></pre> <p>Retorna todos los perfiles almacenados, opcionalmente filtrados.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str | None</code> <p>Si se especifica (<code>\"facebook\"</code> o <code>\"instagram\"</code>),       retorna solo las cuentas de esa plataforma.       <code>None</code> retorna todas las plataformas.</p> <code>None</code> <p>Returns:</p> Type Description <code>list[AccountProfile]</code> <p>Lista de <code>AccountProfile</code>. Puede estar vac\u00eda.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error de lectura inesperado.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.update_activity","title":"update_activity  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>update_activity(\n    account_id: str, activity: AccountActivity\n) -&gt; bool\n</code></pre> <p>Actualiza solo el bloque de actividad de una cuenta existente.</p> <p>Esta operaci\u00f3n es m\u00e1s eficiente que <code>save()</code> completo cuando solo ha cambiado la actividad (caso m\u00e1s frecuente: post-petici\u00f3n).</p> <p>Los backends de BD pueden implementarla como un UPDATE parcial (<code>UPDATE accounts SET activity=? WHERE account_id=?</code>) en lugar de reescribir todo el registro.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta a actualizar.</p> required <code>activity</code> <code>AccountActivity</code> <p>Objeto <code>AccountActivity</code> con los valores actualizados.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se actualiz\u00f3, <code>False</code> si la cuenta no existe.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error durante la actualizaci\u00f3n.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.save_cookies","title":"save_cookies  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>save_cookies(\n    platform: str,\n    account_id: str,\n    cookies: list[dict[str, Any]],\n) -&gt; str\n</code></pre> <p>Persiste las cookies de sesi\u00f3n de una cuenta en el backend.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <code>cookies</code> <code>list[dict[str, Any]]</code> <p>Lista de dicts de cookies en formato Playwright.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Locator (string opaco) que el llamante guarda en</p> <code>str</code> <p><code>AccountProfile.cookies_path</code>. En <code>LocalFileStorage</code> es la</p> <code>str</code> <p>ruta del <code>cookies.json</code>; en los backends de BD, un identificador</p> <code>str</code> <p>simb\u00f3lico.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si no se puede persistir.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.load_cookies","title":"load_cookies  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>load_cookies(\n    platform: str, account_id: str\n) -&gt; list[dict[str, Any]] | None\n</code></pre> <p>Lee las cookies de sesi\u00f3n de una cuenta desde el backend.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]] | None</code> <p>Lista de dicts de cookies, o <code>None</code> si no existen.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si existe cookies pero no se pueden leer.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.delete_cookies","title":"delete_cookies  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>delete_cookies(platform: str, account_id: str) -&gt; bool\n</code></pre> <p>Elimina las cookies de sesi\u00f3n de una cuenta.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si exist\u00edan y se eliminaron, <code>False</code> si no exist\u00edan.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si hay un error durante la eliminaci\u00f3n.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.connect","title":"connect  <code>async</code>","text":"<pre><code>connect() -&gt; None\n</code></pre> <p>Inicializa conexiones/pools si el backend lo requiere.</p> <p>Por defecto no hace nada. <code>PostgresStorage</code> crea aqu\u00ed su pool.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Libera los recursos (pools, clientes) del backend.</p> <p>Por defecto no hace nada.</p>"},{"location":"api/auth/#reaper.auth.storage.base.BaseAccountStorage.exists","title":"exists  <code>async</code>","text":"<pre><code>exists(account_id: str) -&gt; bool\n</code></pre> <p>Comprueba si una cuenta existe en el backend.</p> <p>Implementaci\u00f3n por defecto basada en <code>load()</code>. Los backends eficientes pueden sobreescribirla con una query de existencia.</p> <p>Parameters:</p> Name Type Description Default <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta a comprobar.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si existe, <code>False</code> si no.</p>"},{"location":"api/auth/#reaper.auth.storage.base.StorageError","title":"reaper.auth.storage.base.StorageError","text":"<p>               Bases: <code>Exception</code></p> <p>Error en una operaci\u00f3n de almacenamiento.</p> <p>Envuelve excepciones de bajo nivel (IOError, sqlalchemy.exc, etc.) para que <code>AccountManager</code> no dependa de implementaciones concretas.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage","title":"reaper.auth.storage.local.LocalFileStorage","text":"<pre><code>LocalFileStorage(\n    accounts_dir: str | Path = \"data/accounts\",\n)\n</code></pre> <p>               Bases: <code>BaseAccountStorage</code></p> <p>Almacenamiento de cuentas en archivos JSON locales.</p> <p>Thread-safe para operaciones de lectura concurrente. Para escrituras concurrentes desde m\u00faltiples procesos se recomienda migrar a una BD que soporte transacciones.</p> <p>Parameters:</p> Name Type Description Default <code>accounts_dir</code> <code>str | Path</code> <p>Directorio ra\u00edz para almacenar todas las cuentas.           Se crea autom\u00e1ticamente si no existe.           Ejemplo: <code>Path(\"data/accounts\")</code>.</p> <code>'data/accounts'</code> <p>Estructura en disco::</p> <pre><code>{accounts_dir}/{platform}/{account_id}/account.json\n{accounts_dir}/{platform}/{account_id}/cookies.json\n</code></pre>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.save","title":"save  <code>async</code>","text":"<pre><code>save(account: AccountProfile) -&gt; None\n</code></pre> <p>Guarda el perfil completo. Crea el directorio de la cuenta si no existe.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.load","title":"load  <code>async</code>","text":"<pre><code>load(account_id: str) -&gt; AccountProfile | None\n</code></pre> <p>Carga un perfil buscando en todas las plataformas.</p> <p>Realiza b\u00fasqueda en todos los directorios de plataforma porque <code>account_id</code> no incluye la plataforma como prefijo.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(account_id: str) -&gt; bool\n</code></pre> <p>Elimina el directorio completo de la cuenta (perfil + cookies).</p> <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si se encontr\u00f3 y elimin\u00f3, <code>False</code> si no exist\u00eda.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.list_all","title":"list_all  <code>async</code>","text":"<pre><code>list_all(\n    platform: str | None = None,\n) -&gt; list[AccountProfile]\n</code></pre> <p>Lista todos los perfiles, opcionalmente filtrados por plataforma.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.update_activity","title":"update_activity  <code>async</code>","text":"<pre><code>update_activity(\n    account_id: str, activity: AccountActivity\n) -&gt; bool\n</code></pre> <p>Actualiza solo el bloque de actividad sin reescribir el perfil completo.</p> <p>Lee el JSON actual del disco, sobreescribe el campo <code>activity</code> y vuelve a escribir. Es m\u00e1s eficiente que <code>save()</code> completo.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.cookies_path_for","title":"cookies_path_for","text":"<pre><code>cookies_path_for(platform: str, account_id: str) -&gt; Path\n</code></pre> <p>Retorna la ruta can\u00f3nica del archivo de cookies para una cuenta.</p> <p>La ruta es predecible y reproducible: siempre es <code>{accounts_dir}/{platform}/{account_id}/cookies.json</code>.</p> <p>Este m\u00e9todo lo usan <code>AccountManager</code> y <code>CookieManager</code> para saber d\u00f3nde leer/escribir cookies sin necesidad de consultar el perfil guardado en disco.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>Path (puede no existir todav\u00eda si no se han importado cookies).</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.save_cookies","title":"save_cookies  <code>async</code>","text":"<pre><code>save_cookies(\n    platform: str,\n    account_id: str,\n    cookies: list[dict[str, Any]],\n) -&gt; str\n</code></pre> <p>Persiste las cookies en disco en formato Playwright.</p> <p>Crea el directorio de la cuenta si no existe.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <code>cookies</code> <code>list[dict[str, Any]]</code> <p>Lista de dicts de cookies (formato Playwright).</p> required <p>Returns:</p> Name Type Description <code>Locator</code> <code>str</code> <p>ruta absoluta del <code>cookies.json</code> escrito, para</p> <code>str</code> <p>almacenar en <code>AccountProfile.cookies_path</code>.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si no se puede escribir el archivo.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.load_cookies","title":"load_cookies  <code>async</code>","text":"<pre><code>load_cookies(\n    platform: str, account_id: str\n) -&gt; list[dict[str, Any]] | None\n</code></pre> <p>Lee las cookies de disco en formato Playwright.</p> <p>Returns:</p> Type Description <code>list[dict[str, Any]] | None</code> <p>Lista de dicts de cookies, o <code>None</code> si el archivo no existe.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si el archivo existe pero no se puede leer/parsear.</p>"},{"location":"api/auth/#reaper.auth.storage.local.LocalFileStorage.delete_cookies","title":"delete_cookies  <code>async</code>","text":"<pre><code>delete_cookies(platform: str, account_id: str) -&gt; bool\n</code></pre> <p>Elimina el archivo de cookies de una cuenta.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>account_id</code> <code>str</code> <p>UUID4 de la cuenta.</p> required <p>Returns:</p> Type Description <code>bool</code> <p><code>True</code> si el archivo exist\u00eda y fue eliminado, <code>False</code> si no.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage","title":"reaper.auth.storage.postgres.PostgresStorage","text":"<pre><code>PostgresStorage(\n    dsn: str,\n    *,\n    pool_size: int = 5,\n    max_overflow: int = 10,\n    timeout: float = 30.0,\n    table: str = \"accounts\",\n)\n</code></pre> <p>               Bases: <code>BaseAccountStorage</code></p> <p>Almacenamiento de cuentas en PostgreSQL v\u00eda <code>asyncpg</code>.</p> <p>Parameters:</p> Name Type Description Default <code>dsn</code> <code>str</code> <p>Cadena de conexi\u00f3n de PostgreSQL, p.ej. <code>\"postgresql://user:pass@localhost:5432/reaper\"</code>.</p> required <code>pool_size</code> <code>int</code> <p>N\u00famero de conexiones del pool. Defecto: 5.</p> <code>5</code> <code>max_overflow</code> <code>int</code> <p>Conexiones extra temporales por encima de <code>pool_size</code> bajo carga. Defecto: 10.</p> <code>10</code> <code>timeout</code> <code>float</code> <p>Segundos de espera para obtener una conexi\u00f3n del pool. Defecto: 30.</p> <code>30.0</code> <code>table</code> <code>str</code> <p>Nombre de la tabla de cuentas. Defecto: <code>\"accounts\"</code>.</p> <code>'accounts'</code> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si no se puede conectar/crear el esquema en <code>connect()</code> o si falla una operaci\u00f3n de lectura/escritura.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.connect","title":"connect  <code>async</code>","text":"<pre><code>connect() -&gt; None\n</code></pre> <p>Crea el pool de conexiones y el esquema si no existe.</p> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si la conexi\u00f3n falla o el DSN es inv\u00e1lido.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Cierra el pool de conexiones, liberando todos los recursos.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.save","title":"save  <code>async</code>","text":"<pre><code>save(account: AccountProfile) -&gt; None\n</code></pre> <p>Inserta o actualiza el perfil completo (sin tocar las cookies).</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.load","title":"load  <code>async</code>","text":"<pre><code>load(account_id: str) -&gt; AccountProfile | None\n</code></pre> <p>Carga un perfil por <code>account_id</code> o <code>None</code> si no existe.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(account_id: str) -&gt; bool\n</code></pre> <p>Elimina la fila completa de la cuenta.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.list_all","title":"list_all  <code>async</code>","text":"<pre><code>list_all(\n    platform: str | None = None,\n) -&gt; list[AccountProfile]\n</code></pre> <p>Lista todos los perfiles, opcionalmente filtrados por plataforma.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.update_activity","title":"update_activity  <code>async</code>","text":"<pre><code>update_activity(\n    account_id: str, activity: AccountActivity\n) -&gt; bool\n</code></pre> <p>Actualiza solo la columna <code>activity</code> (UPDATE parcial).</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.save_cookies","title":"save_cookies  <code>async</code>","text":"<pre><code>save_cookies(\n    platform: str,\n    account_id: str,\n    cookies: list[dict[str, Any]],\n) -&gt; str\n</code></pre> <p>Guarda las cookies como JSONB en la fila de la cuenta.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.load_cookies","title":"load_cookies  <code>async</code>","text":"<pre><code>load_cookies(\n    platform: str, account_id: str\n) -&gt; list[dict[str, Any]] | None\n</code></pre> <p>Lee las cookies de la fila, o <code>None</code> si la cuenta no tiene.</p>"},{"location":"api/auth/#reaper.auth.storage.postgres.PostgresStorage.delete_cookies","title":"delete_cookies  <code>async</code>","text":"<pre><code>delete_cookies(platform: str, account_id: str) -&gt; bool\n</code></pre> <p>Pone la columna <code>cookies</code> a NULL.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage","title":"reaper.auth.storage.mongodb.MongoStorage","text":"<pre><code>MongoStorage(\n    uri: str,\n    database: str = \"reaper\",\n    collection: str = COLLECTION_DEFAULT,\n)\n</code></pre> <p>               Bases: <code>BaseAccountStorage</code></p> <p>Almacenamiento de cuentas en MongoDB v\u00eda <code>motor</code>.</p> <p>Parameters:</p> Name Type Description Default <code>uri</code> <code>str</code> <p>Cadena de conexi\u00f3n de MongoDB, p.ej. <code>\"mongodb://localhost:27017\"</code>. Soporta auth y replica sets.</p> required <code>database</code> <code>str</code> <p>Nombre de la base de datos. Defecto: <code>\"reaper\"</code>.</p> <code>'reaper'</code> <code>collection</code> <code>str</code> <p>Nombre de la colecci\u00f3n de cuentas. Defecto: <code>\"accounts\"</code>.</p> <code>COLLECTION_DEFAULT</code> <p>Raises:</p> Type Description <code>StorageError</code> <p>Si falla una operaci\u00f3n de lectura/escritura.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.connect","title":"connect  <code>async</code>","text":"<pre><code>connect() -&gt; None\n</code></pre> <p>Prepara \u00edndices de la colecci\u00f3n (idempotente, perezoso).</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Cierra el cliente, liberando sockets y pools internos.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.save","title":"save  <code>async</code>","text":"<pre><code>save(account: AccountProfile) -&gt; None\n</code></pre> <p>Inserta o actualiza los campos de perfil (sin tocar <code>cookies</code>).</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.load","title":"load  <code>async</code>","text":"<pre><code>load(account_id: str) -&gt; AccountProfile | None\n</code></pre> <p>Carga un perfil por <code>account_id</code> o <code>None</code> si no existe.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.delete","title":"delete  <code>async</code>","text":"<pre><code>delete(account_id: str) -&gt; bool\n</code></pre> <p>Elimina el documento completo de la cuenta.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.list_all","title":"list_all  <code>async</code>","text":"<pre><code>list_all(\n    platform: str | None = None,\n) -&gt; list[AccountProfile]\n</code></pre> <p>Lista todos los perfiles, opcionalmente filtrados por plataforma.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.update_activity","title":"update_activity  <code>async</code>","text":"<pre><code>update_activity(\n    account_id: str, activity: AccountActivity\n) -&gt; bool\n</code></pre> <p>Actualiza solo el subdocumento <code>activity</code> (<code>$set</code> parcial).</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.save_cookies","title":"save_cookies  <code>async</code>","text":"<pre><code>save_cookies(\n    platform: str,\n    account_id: str,\n    cookies: list[dict[str, Any]],\n) -&gt; str\n</code></pre> <p>Guarda las cookies en el documento de la cuenta.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.load_cookies","title":"load_cookies  <code>async</code>","text":"<pre><code>load_cookies(\n    platform: str, account_id: str\n) -&gt; list[dict[str, Any]] | None\n</code></pre> <p>Lee las cookies del documento, o <code>None</code> si no tiene.</p>"},{"location":"api/auth/#reaper.auth.storage.mongodb.MongoStorage.delete_cookies","title":"delete_cookies  <code>async</code>","text":"<pre><code>delete_cookies(platform: str, account_id: str) -&gt; bool\n</code></pre> <p>Elimina los campos <code>cookies</code> y <code>cookies_path</code> del documento.</p>"},{"location":"api/auth/#configuracion-de-storage","title":"Configuraci\u00f3n de storage","text":""},{"location":"api/auth/#reaper.auth.storage.config.StorageConfig","title":"reaper.auth.storage.config.StorageConfig  <code>dataclass</code>","text":"<pre><code>StorageConfig(\n    backend: str = \"local\",\n    accounts_dir: str = \"data/accounts\",\n    actions_dir: str = \"data/actions\",\n    postgres: PostgresConfig | None = None,\n    mongodb: MongoConfig | None = None,\n)\n</code></pre> <p>Configuraci\u00f3n completa del subsistema de almacenamiento.</p> <p>Attributes:</p> Name Type Description <code>backend</code> <code>str</code> <p><code>\"local\"</code>, <code>\"postgres\"</code> o <code>\"mongodb\"</code>. Defecto: <code>\"local\"</code> (comportamiento original, JSON en disco).</p> <code>accounts_dir</code> <code>str</code> <p>Directorio ra\u00edz usado por <code>LocalFileStorage</code>. Solo se aplica con <code>backend=\"local\"</code>. Defecto: <code>\"data/accounts\"</code>.</p> <code>actions_dir</code> <code>str</code> <p>Directorio ra\u00edz usado por <code>LocalActionStorage</code>. Solo se aplica con <code>backend=\"local\"</code>. Defecto: <code>\"data/actions\"</code>.</p> <code>postgres</code> <code>PostgresConfig | None</code> <p>Par\u00e1metros del backend PostgreSQL (si se elige).</p> <code>mongodb</code> <code>MongoConfig | None</code> <p>Par\u00e1metros del backend MongoDB (si se elige).</p>"},{"location":"api/auth/#reaper.auth.storage.config.PostgresConfig","title":"reaper.auth.storage.config.PostgresConfig  <code>dataclass</code>","text":"<pre><code>PostgresConfig(\n    dsn: str,\n    pool_size: int = 5,\n    max_overflow: int = 10,\n    timeout: float = 30.0,\n    table: str = \"accounts\",\n    actions_table: str = \"actions\",\n)\n</code></pre> <p>Par\u00e1metros de conexi\u00f3n del backend PostgreSQL.</p> <p>Attributes:</p> Name Type Description <code>dsn</code> <code>str</code> <p>Cadena de conexi\u00f3n asyncpg, p.ej. <code>\"postgresql://user:pass@localhost:5432/reaper\"</code>.</p> <code>pool_size</code> <code>int</code> <p>Conexiones del pool. Defecto: 5.</p> <code>max_overflow</code> <code>int</code> <p>Conexiones extra bajo carga. Defecto: 10.</p> <code>timeout</code> <code>float</code> <p>Segundos de espera de conexi\u00f3n. Defecto: 30.0.</p> <code>table</code> <code>str</code> <p>Nombre de la tabla de cuentas. Defecto: <code>\"accounts\"</code>.</p> <code>actions_table</code> <code>str</code> <p>Nombre de la tabla de acciones de escritura. Defecto: <code>\"actions\"</code>.</p>"},{"location":"api/auth/#reaper.auth.storage.config.MongoConfig","title":"reaper.auth.storage.config.MongoConfig  <code>dataclass</code>","text":"<pre><code>MongoConfig(\n    uri: str,\n    database: str = \"reaper\",\n    collection: str = \"accounts\",\n    actions_collection: str = \"actions\",\n)\n</code></pre> <p>Par\u00e1metros de conexi\u00f3n del backend MongoDB.</p> <p>Attributes:</p> Name Type Description <code>uri</code> <code>str</code> <p>Cadena de conexi\u00f3n, p.ej. <code>\"mongodb://localhost:27017\"</code>.</p> <code>database</code> <code>str</code> <p>Nombre de la base de datos. Defecto: <code>\"reaper\"</code>.</p> <code>collection</code> <code>str</code> <p>Nombre de la colecci\u00f3n. Defecto: <code>\"accounts\"</code>.</p> <code>actions_collection</code> <code>str</code> <p>Nombre de la colecci\u00f3n de acciones de escritura. Defecto: <code>\"actions\"</code>.</p>"},{"location":"api/auth/#reaper.auth.storage.config.load_storage_config","title":"reaper.auth.storage.config.load_storage_config","text":"<pre><code>load_storage_config(\n    path: str | Path | None = None,\n) -&gt; StorageConfig\n</code></pre> <p>Carga la configuraci\u00f3n de almacenamiento desde un fichero TOML.</p> <p>Si no se encuentra ning\u00fan fichero, retorna la configuraci\u00f3n por defecto (<code>backend=\"local\"</code>), preservando el comportamiento original.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path | None</code> <p>Ruta expl\u00edcita al fichero. Si es <code>None</code>, busca en <code>REAPER_CONFIG</code> y luego en <code>DEFAULT_CONFIG_PATHS</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>StorageConfig</code> <p><code>StorageConfig</code> con los valores del fichero o los defaults.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si <code>backend</code> no es soportado o falta su secci\u00f3n de configuraci\u00f3n requerida.</p>"},{"location":"api/auth/#reaper.auth.storage.config.find_config_file","title":"reaper.auth.storage.config.find_config_file","text":"<pre><code>find_config_file(\n    path: str | Path | None = None,\n) -&gt; Path | None\n</code></pre> <p>Resuelve la ruta del fichero de configuraci\u00f3n o <code>None</code> si no existe.</p> <p>El orden de b\u00fasqueda es: ruta expl\u00edcita, variable de entorno <code>REAPER_CONFIG</code>, y luego <code>DEFAULT_CONFIG_PATHS</code>.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path | None</code> <p>Ruta expl\u00edcita al fichero, o <code>None</code> para b\u00fasqueda autom\u00e1tica.</p> <code>None</code> <p>Returns:</p> Type Description <code>Path | None</code> <p><code>Path</code> del primer fichero existente, o <code>None</code>.</p>"},{"location":"api/auth/#reaper.auth.storage.config.build_storage","title":"reaper.auth.storage.config.build_storage","text":"<pre><code>build_storage(\n    config: StorageConfig | None = None,\n) -&gt; BaseAccountStorage\n</code></pre> <p>Instancia el backend de almacenamiento indicado por la configuraci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>StorageConfig | None</code> <p>Configuraci\u00f3n de storage. Si es <code>None</code>, la carga desde el fichero de configuraci\u00f3n con :func:<code>load_storage_config</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>BaseAccountStorage</code> <p><code>LocalFileStorage</code>, <code>PostgresStorage</code> o <code>MongoStorage</code> seg\u00fan</p> <code>BaseAccountStorage</code> <p><code>config.backend</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si el backend no est\u00e1 soportado o falta su configuraci\u00f3n.</p>"},{"location":"api/config/","title":"API Reference \u2014 <code>reaper.config</code>","text":""},{"location":"api/config/#scraperconfig","title":"<code>ScraperConfig</code>","text":""},{"location":"api/config/#reaper.config.ScraperConfig","title":"reaper.config.ScraperConfig  <code>dataclass</code>","text":"<pre><code>ScraperConfig(\n    url: str,\n    headless: bool = True,\n    debug: bool = False,\n    screenshot: bool = False,\n    auto_scroll: bool = False,\n    infinity_scroll: bool = False,\n    proxy_server: str | None = None,\n    proxy_username: str | None = None,\n    proxy_password: str | None = None,\n    artifacts_dir: Path = (\n        lambda: Path(\"data/debug_artifacts\")\n    )(),\n    results_dir: Path = (lambda: Path(\"data/results\"))(),\n    account_manager: Any = None,\n    preferred_account: str | None = None,\n)\n</code></pre> <p>Configuraci\u00f3n completa para una sesi\u00f3n de scraping.</p> <p>Es el \u00fanico objeto de configuraci\u00f3n que existe en la librer\u00eda. Se crea en <code>Reaper.scrape()</code> y se pasa a todos los componentes sin modificaci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>URL objetivo a scrapear. \u00danico par\u00e1metro obligatorio. Debe empezar con <code>http://</code> o <code>https://</code>.</p> required <code>headless</code> <code>bool</code> <p>Ejecutar Firefox sin interfaz gr\u00e1fica. Recomendado <code>True</code> en producci\u00f3n. Defecto: True.</p> <code>True</code> <code>debug</code> <code>bool</code> <p>Activar logging DEBUG y guardar artefactos (HTML, screenshots, tr\u00e1fico GraphQL) en <code>artifacts_dir</code>. Defecto: False.</p> <code>False</code> <code>screenshot</code> <code>bool</code> <p>Capturar PNG del contenedor principal al cargar la p\u00e1gina. \u00datil para diagn\u00f3stico visual. Defecto: False.</p> <code>False</code> <code>auto_scroll</code> <code>bool</code> <p>Scroll autom\u00e1tico midiendo cambios de <code>scrollHeight</code> del DOM. Recomendado para cargar comentarios paginados. Defecto: True.</p> <code>False</code> <code>infinity_scroll</code> <code>bool</code> <p>Scroll continuo monitorizando actividad de red. Para feeds con carga infinita (grupos, b\u00fasquedas). Requiere <code>auto_scroll=True</code>. Defecto: False.</p> <code>False</code> <code>proxy_server</code> <code>str | None</code> <p>URL del proxy, p.ej. <code>\"http://ip:8080\"</code> o <code>\"socks5://ip:1080\"</code>. Defecto: None.</p> <code>None</code> <code>proxy_username</code> <code>str | None</code> <p>Usuario del proxy. Ignorado si no hay <code>proxy_server</code>. Defecto: None.</p> <code>None</code> <code>proxy_password</code> <code>str | None</code> <p>Contrase\u00f1a del proxy. Igual que <code>proxy_username</code>. Defecto: None.</p> <code>None</code> <code>artifacts_dir</code> <code>Path</code> <p>Directorio para artefactos de debug (screenshots, HTML, tr\u00e1fico). Solo se usa si <code>debug=True</code>. Defecto: <code>data/debug_artifacts</code>.</p> <code>(lambda: Path('data/debug_artifacts'))()</code> <code>results_dir</code> <code>Path</code> <p>Directorio para guardar resultados JSON. Defecto: <code>data/results</code>.</p> <code>(lambda: Path('data/results'))()</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si alguna invariante de configuraci\u00f3n se viola: - URL vac\u00eda. - Esquema de URL inv\u00e1lido (no http/https). - Credenciales de proxy sin servidor de proxy. - <code>infinity_scroll=True</code> con <code>auto_scroll=False</code>.</p>"},{"location":"api/config/#reaper.config.ScraperConfig.has_proxy","title":"has_proxy  <code>property</code>","text":"<pre><code>has_proxy: bool\n</code></pre> <p>True si hay un servidor proxy configurado.</p>"},{"location":"api/config/#reaper.config.ScraperConfig.proxy_config","title":"proxy_config  <code>property</code>","text":"<pre><code>proxy_config: dict[str, str] | None\n</code></pre> <p>Devuelve el dict de proxy listo para Playwright, o None.</p> <p>Playwright espera la estructura::</p> <pre><code>{\"server\": \"...\", \"username\": \"...\", \"password\": \"...\"}\n</code></pre> <p>Returns:</p> Type Description <code>dict[str, str] | None</code> <p>Dict con <code>server</code> (y opcionalmente <code>username</code>/<code>password</code>),</p> <code>dict[str, str] | None</code> <p>o None si no hay proxy configurado.</p>"},{"location":"api/config/#reaper.config.ScraperConfig.has_account_manager","title":"has_account_manager  <code>property</code>","text":"<pre><code>has_account_manager: bool\n</code></pre> <p>True si hay un AccountManager configurado para rotaci\u00f3n de cuentas.</p>"},{"location":"api/network/","title":"API Reference \u2014 <code>reaper.network</code>","text":"<p>Toda la API de esta p\u00e1gina es importable directamente desde <code>reaper.network</code> (y tambi\u00e9n desde los subm\u00f3dulos que se indican).</p>"},{"location":"api/network/#contentfetcher","title":"<code>ContentFetcher</code>","text":""},{"location":"api/network/#reaper.network.content_fetcher.ContentFetcher","title":"reaper.network.content_fetcher.ContentFetcher","text":"<pre><code>ContentFetcher(\n    url: str,\n    headless: bool = True,\n    debug: bool = False,\n    config: BrowserConfig | None = None,\n)\n</code></pre> <p>Obtiene HTML completamente renderizado de una URL con Playwright (Firefox).</p> <p>Orquesta la navegaci\u00f3n, la interceptaci\u00f3n de red y las t\u00e9cnicas de scroll. Act\u00faa como facade sobre Playwright: el c\u00f3digo consumidor solo interact\u00faa con <code>fetch()</code> y recibe un <code>FetchResult</code> completamente tipado.</p> Responsabilidad \u00fanica <p>Navegar a una URL, activar la captura de tr\u00e1fico de red, esperar la carga completa del contenido din\u00e1mico y devolver HTML + tr\u00e1fico. NO parsea, NO interpreta, NO extrae datos de la p\u00e1gina.</p> Modos de scroll <ul> <li><code>auto_scroll=True</code>:     scroll midiendo cambios de scrollHeight DOM.                             Recomendado para posts individuales con comentarios.</li> <li><code>infinity_scroll=True</code>: scroll monitorizando actividad de red.                             Recomendado para feeds y b\u00fasquedas largas.</li> <li>Ambos False:              sin scroll (posts simples, Instagram).</li> </ul> <p>Uso b\u00e1sico::</p> <pre><code>fetcher = ContentFetcher(\"https://www.facebook.com/reel/816043001524221\")\nresult = await fetcher.fetch(auto_scroll=True)\nif result.success:\n    html = result.html_content\n    traffic = result.traffic\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>URL completa de la p\u00e1gina a obtener.</p> required <code>headless</code> <code>bool</code> <p>Si True, lanza el navegador sin interfaz gr\u00e1fica.       Recomendado True en producci\u00f3n y daemons.</p> <code>True</code> <code>debug</code> <code>bool</code> <p>Si True, activa logs detallados, guarda screenshots,       HTML y tr\u00e1fico de red en <code>BrowserConfig.DEBUG_OUTPUT_DIR</code>.</p> <code>False</code> <code>config</code> <code>BrowserConfig | None</code> <p>Instancia de BrowserConfig con par\u00e1metros personalizados.       Si None, usa la configuraci\u00f3n por defecto.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si url est\u00e1 vac\u00eda o no es un string.</p>"},{"location":"api/network/#reaper.network.content_fetcher.ContentFetcher.fetch","title":"fetch  <code>async</code>","text":"<pre><code>fetch(\n    screenshot: bool = False,\n    auto_scroll: bool = False,\n    infinity_scroll: bool = False,\n    proxy_server: str | None = None,\n    proxy_username: str | None = None,\n    proxy_password: str | None = None,\n    cookies: list[dict] | None = None,\n) -&gt; FetchResult\n</code></pre> <p>Ejecuta la navegaci\u00f3n completa usando Camoufox.</p> <p>Camoufox es un Firefox modificado que: - Evade detecci\u00f3n de Facebook (no muestra logo congelado) - Captura tr\u00e1fico sin errores 0x80004005 - No requiere stealth JS adicional</p> <p>Parameters:</p> Name Type Description Default <code>screenshot</code> <code>bool</code> <p>Captura PNG del contenedor.</p> <code>False</code> <code>auto_scroll</code> <code>bool</code> <p>Scroll midiendo cambios de altura DOM.</p> <code>False</code> <code>infinity_scroll</code> <code>bool</code> <p>Scroll monitorizando tr\u00e1fico de red.</p> <code>False</code> <code>proxy_server</code> <code>str | None</code> <p>URL del proxy (ej: \"http://ip:port\").</p> <code>None</code> <code>proxy_username</code> <code>str | None</code> <p>Usuario para autenticaci\u00f3n del proxy.</p> <code>None</code> <code>proxy_password</code> <code>str | None</code> <p>Contrase\u00f1a para autenticaci\u00f3n del proxy.</p> <code>None</code> <code>cookies</code> <code>list[dict] | None</code> <p>Lista de cookies en formato Playwright.</p> <code>None</code> <p>Returns:</p> Type Description <code>FetchResult</code> <p>FetchResult con HTML renderizado, tr\u00e1fico capturado y metadatos.</p>"},{"location":"api/network/#browserconfig","title":"<code>BrowserConfig</code>","text":""},{"location":"api/network/#reaper.network.content_fetcher.BrowserConfig","title":"reaper.network.content_fetcher.BrowserConfig","text":"<pre><code>BrowserConfig()\n</code></pre> <p>Par\u00e1metros de configuraci\u00f3n del navegador y comportamiento de scroll.</p> <p>Centraliza los ajustes en un \u00fanico lugar sin tocar la l\u00f3gica de navegaci\u00f3n. Para personalizar, instancia y sobreescribe atributos de instancia::</p> <pre><code>cfg = BrowserConfig()\ncfg.PAGE_LOAD_WAIT = 8.0\ncfg.MAX_SCROLL_ITERATIONS = 30\nfetcher = ContentFetcher(url, config=cfg)\n</code></pre>"},{"location":"api/network/#fetchresult","title":"<code>FetchResult</code>","text":""},{"location":"api/network/#reaper.network.interceptor.FetchResult","title":"reaper.network.interceptor.FetchResult  <code>dataclass</code>","text":"<pre><code>FetchResult(\n    original_url: str,\n    final_url: str | None = None,\n    html_content: str | None = None,\n    traffic: CapturedTraffic | None = None,\n    fetched_at: datetime = datetime.now(),\n    success: bool = False,\n    error: str | None = None,\n    authenticated: bool = False,\n    account_id: str | None = None,\n    debug_session_dir: Path | None = None,\n    updated_cookies: list[dict] | None = None,\n)\n</code></pre> <p>Resultado completo de una operaci\u00f3n de obtenci\u00f3n de contenido.</p> <p>Encapsula el HTML renderizado, el tr\u00e1fico de red y los metadatos de sesi\u00f3n. En caso de error, <code>success=False</code> y <code>error</code> describe el fallo.</p> <p>Attributes:</p> Name Type Description <code>original_url</code> <code>str</code> <p>URL solicitada originalmente.</p> <code>final_url</code> <code>str | None</code> <p>URL final tras redirecciones.</p> <code>html_content</code> <code>str | None</code> <p>HTML renderizado (DOM final con JS ejecutado), o None.</p> <code>traffic</code> <code>CapturedTraffic | None</code> <p>Tr\u00e1fico de red capturado (GraphQL + API).</p> <code>fetched_at</code> <code>datetime</code> <p>Timestamp de la captura.</p> <code>success</code> <code>bool</code> <p>True si el HTML fue obtenido sin errores cr\u00edticos.</p> <code>error</code> <code>str | None</code> <p>Mensaje descriptivo si <code>success=False</code>.</p> <code>authenticated</code> <code>bool</code> <p>True si la sesi\u00f3n us\u00f3 cookies de autenticaci\u00f3n.</p> <code>account_id</code> <code>str | None</code> <p>ID de la cuenta usada para la sesi\u00f3n autenticada.</p> <code>debug_session_dir</code> <code>Path | None</code> <p>Path al directorio de debug. None si debug=False.</p> <code>updated_cookies</code> <code>list[dict] | None</code> <p>Cookies le\u00eddas del contexto Playwright despu\u00e9s de                la navegaci\u00f3n. El servidor puede haber emitido                cabeceras <code>Set-Cookie</code> que extienden el TTL o                rotan tokens de seguridad de corta vida.                <code>BaseScraper._fetch_with_account()</code> compara este                campo con las cookies originales y persiste las                actualizadas si difieren, alargando autom\u00e1ticamente                la vida de la sesi\u00f3n sin necesidad de login manual.                <code>None</code> en sesiones an\u00f3nimas o si la lectura fall\u00f3.</p> Notes <p>Los parsers deben usar <code>traffic.graphql_responses</code> para datos estructurados en lugar de depender \u00fanicamente del HTML.</p>"},{"location":"api/network/#capturedtraffic","title":"<code>CapturedTraffic</code>","text":""},{"location":"api/network/#reaper.network.interceptor.CapturedTraffic","title":"reaper.network.interceptor.CapturedTraffic  <code>dataclass</code>","text":"<pre><code>CapturedTraffic(\n    graphql_requests: list[CapturedRequest] = list(),\n    graphql_responses: list[CapturedResponse] = list(),\n    api_requests: list[CapturedRequest] = list(),\n    api_responses: list[CapturedResponse] = list(),\n)\n</code></pre> <p>Tr\u00e1fico de red completo capturado durante una sesi\u00f3n de fetch.</p> <p>Organiza requests y responses por categor\u00eda para acceso directo desde los parsers. Exclusivo para Facebook (graphql) e Instagram (api).</p> <p>Uso desde parsers::</p> <pre><code>for resp in result.traffic.graphql_responses:\n    for fragment in CapturedTraffic.normalize_body(resp.body):\n        data = fragment.get(\"data\", {})\n\nsearch_responses = result.traffic.graphql_by_operation(\"SearchResultsPage\")\n</code></pre> <p>Attributes:</p> Name Type Description <code>graphql_requests</code> <code>list[CapturedRequest]</code> <p>Peticiones a <code>/api/graphql/</code>.</p> <code>graphql_responses</code> <code>list[CapturedResponse]</code> <p>Respuestas de las operaciones GraphQL.</p> <code>api_requests</code> <code>list[CapturedRequest]</code> <p>Peticiones a <code>/api/v1/</code> (Instagram REST API).</p> <code>api_responses</code> <code>list[CapturedResponse]</code> <p>Respuestas de la API REST de Instagram.</p>"},{"location":"api/network/#reaper.network.interceptor.CapturedTraffic.total_requests","title":"total_requests  <code>property</code>","text":"<pre><code>total_requests: int\n</code></pre> <p>N\u00famero total de peticiones capturadas.</p>"},{"location":"api/network/#reaper.network.interceptor.CapturedTraffic.total_responses","title":"total_responses  <code>property</code>","text":"<pre><code>total_responses: int\n</code></pre> <p>N\u00famero total de respuestas capturadas.</p>"},{"location":"api/network/#reaper.network.interceptor.CapturedTraffic.graphql_by_operation","title":"graphql_by_operation","text":"<pre><code>graphql_by_operation(\n    operation_name: str,\n) -&gt; list[CapturedResponse]\n</code></pre> <p>Filtra respuestas GraphQL por nombre de operaci\u00f3n (b\u00fasqueda parcial).</p> <p>Parameters:</p> Name Type Description Default <code>operation_name</code> <code>str</code> <p>Nombre o fragmento del <code>friendly_name</code> a buscar.</p> required <p>Returns:</p> Type Description <code>list[CapturedResponse]</code> <p>Lista de CapturedResponse cuya request tiene el friendly_name indicado.</p>"},{"location":"api/network/#reaper.network.interceptor.CapturedTraffic.all_requests","title":"all_requests","text":"<pre><code>all_requests() -&gt; list[CapturedRequest]\n</code></pre> <p>Retorna todas las peticiones de todas las categor\u00edas.</p>"},{"location":"api/network/#reaper.network.interceptor.CapturedTraffic.all_responses","title":"all_responses","text":"<pre><code>all_responses() -&gt; list[CapturedResponse]\n</code></pre> <p>Retorna todas las respuestas de todas las categor\u00edas.</p>"},{"location":"api/network/#reaper.network.interceptor.CapturedTraffic.summary","title":"summary","text":"<pre><code>summary() -&gt; str\n</code></pre> <p>L\u00ednea de resumen del tr\u00e1fico capturado.</p>"},{"location":"api/network/#persistencia-y-analisis-offline-de-sesiones","title":"Persistencia y an\u00e1lisis offline de sesiones","text":"<p>Guardado, recarga y consulta de sesiones de debug sin abrir el navegador.</p>"},{"location":"api/network/#reaper.network.content_fetcher.serialize_traffic","title":"reaper.network.content_fetcher.serialize_traffic","text":"<pre><code>serialize_traffic(\n    traffic: CapturedTraffic,\n) -&gt; dict[str, Any]\n</code></pre> <p>Convierte un <code>CapturedTraffic</code> a un dict JSON-serializable.</p> <p>Serializa todos los dataclasses anidados convirtiendo los <code>datetime</code> a cadenas ISO-8601. Compatible con <code>deserialize_traffic()</code>.</p> <p>Parameters:</p> Name Type Description Default <code>traffic</code> <code>CapturedTraffic</code> <p>Objeto CapturedTraffic retornado por <code>NetworkInterceptor.get_traffic()</code>.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>dict serializable con la siguiente estructura::</p> <p>{     \"version\":           \"3.0\",     \"captured_at\":       \"\",     \"summary\":           \"GraphQL: Xreq/Xres | ...\",     \"graphql_requests\":  [{CapturedRequest serializado} ...],     \"graphql_responses\": [{CapturedResponse serializado} ...],     \"api_requests\":      [...],     \"api_responses\":     [...] } <p>Example::</p> <pre><code>traffic = interceptor.get_traffic()\ndata = serialize_traffic(traffic)\nPath(\"traffic.json\").write_text(json.dumps(data, indent=2))\n</code></pre>"},{"location":"api/network/#reaper.network.content_fetcher.deserialize_traffic","title":"reaper.network.content_fetcher.deserialize_traffic","text":"<pre><code>deserialize_traffic(\n    data: dict[str, Any],\n    session_dir: str | Path | None = None,\n) -&gt; CapturedTraffic\n</code></pre> <p>Reconstruye un <code>CapturedTraffic</code> desde un dict serializado con :func:<code>serialize_traffic</code>.</p> <p>Si <code>session_dir</code> se provee, resuelve los bodies de respuesta que fueron guardados como ficheros separados (marcados con el prefijo <code>__FILE__</code>).</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>dict cargado desde <code>traffic.json</code>.</p> required <code>session_dir</code> <code>str | Path | None</code> <p>Directorio ra\u00edz de la sesi\u00f3n de debug (para resolver          referencias <code>__FILE__</code>). Puede ser str o Path.</p> <code>None</code> <p>Returns:</p> Type Description <code>CapturedTraffic</code> <p>CapturedTraffic completamente reconstruido con todos sus campos tipados.</p> <p>Example::</p> <pre><code>data = json.loads(Path(\"debug_artifacts/session/traffic.json\").read_text())\ntraffic = deserialize_traffic(data, session_dir=\"debug_artifacts/session\")\n\nfor resp in traffic.graphql_responses:\n    if resp.body:\n        print(resp.body.get(\"data\", {}).keys())\n</code></pre>"},{"location":"api/network/#reaper.network.content_fetcher.load_debug_session","title":"reaper.network.content_fetcher.load_debug_session","text":"<pre><code>load_debug_session(session_dir: str | Path) -&gt; FetchResult\n</code></pre> <p>Carga una sesi\u00f3n de debug guardada en disco y reconstruye un <code>FetchResult</code> listo para an\u00e1lisis offline.</p> <p>Funciona con sesiones guardadas por <code>ContentFetcher</code> cuando <code>debug=True</code>. No requiere abrir un navegador ni hacer peticiones de red::</p> <pre><code>from reaper.network import load_debug_session\n\nresult = load_debug_session(\"data/debug_artifacts/www.facebook.com_reel_20250601_143022\")\nprint(result.final_url, result.fetched_at)\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>session_dir</code> <code>str | Path</code> <p>Ruta al directorio de la sesi\u00f3n de debug.          Debe contener al menos <code>meta.json</code>.</p> required <p>Returns:</p> Type Description <code>FetchResult</code> <p>FetchResult completamente reconstruido.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>Si <code>session_dir</code> no existe o no contiene <code>meta.json</code>.</p> <code>ValueError</code> <p>Si <code>meta.json</code> tiene formato inv\u00e1lido.</p>"},{"location":"api/network/#reaper.network.content_fetcher.list_debug_sessions","title":"reaper.network.content_fetcher.list_debug_sessions","text":"<pre><code>list_debug_sessions(\n    base_dir: str | Path = \"data/debug_artifacts\",\n) -&gt; list[dict[str, Any]]\n</code></pre> <p>Lista todas las sesiones de debug guardadas, ordenadas de m\u00e1s reciente a m\u00e1s antigua.</p> <p>\u00datil para elegir qu\u00e9 sesi\u00f3n cargar con :func:<code>load_debug_session</code> o para an\u00e1lisis batch de m\u00faltiples sesiones.</p> <p>Parameters:</p> Name Type Description Default <code>base_dir</code> <code>str | Path</code> <p>Directorio ra\u00edz de artefactos de debug.</p> <code>'data/debug_artifacts'</code> <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>Lista de dicts con metadatos b\u00e1sicos de cada sesi\u00f3n::</p> <p>[     {         \"path\":            Path(\"data/debug_artifacts/session_name\"),         \"original_url\":    \"https://...\",         \"fetched_at\":      \"2025-01-01T12:00:00\",         \"success\":         True,         \"authenticated\":   False,         \"traffic_summary\": \"GraphQL: 8req/8res | API: 2req/2res\"     },     ... ]</p> <code>list[dict[str, Any]]</code> <p>Las sesiones sin <code>meta.json</code> v\u00e1lido se omiten silenciosamente.</p> <p>Example::</p> <pre><code>sessions = list_debug_sessions()\nfor s in sessions:\n    print(s[\"fetched_at\"], s[\"original_url\"][:60], s[\"traffic_summary\"])\n\nresult = load_debug_session(sessions[0][\"path\"])\n</code></pre>"},{"location":"api/parsers/","title":"API Reference \u2014 <code>reaper.parsers</code>","text":""},{"location":"api/parsers/#baseparser","title":"<code>BaseParser</code>","text":""},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser","title":"reaper.parsers.base_parser.BaseParser","text":"<pre><code>BaseParser(\n    html_content: str,\n    final_url: str,\n    original_url: str = \"\",\n    traffic: CapturedTraffic | None = None,\n    debug: bool = False,\n    platform: str = \"\",\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Clase base abstracta para todos los parsers de contenido.</p> <p>Define la interfaz com\u00fan y utilidades plataforma-agn\u00f3sticas. No instanciar directamente; usar siempre una subclase concreta.</p> <p>Attributes:</p> Name Type Description <code>html_content</code> <p>HTML crudo de la p\u00e1gina a parsear.</p> <code>final_url</code> <p>URL final (tras redirecciones) de la p\u00e1gina capturada.</p> <code>original_url</code> <p>URL original solicitada por el usuario.</p> <code>traffic</code> <p>Tr\u00e1fico de red capturado con GraphQL responses.</p> <code>debug</code> <p>Si True, emite logs de diagn\u00f3stico adicionales v\u00eda <code>logging</code>.</p> <code>result</code> <code>dict[str, Any]</code> <p>Esqueleto de resultado garantizado. Siempre contiene: <code>platform</code>, <code>status</code>, <code>error</code>, <code>raw_data_available</code>, <code>scraped_at</code>, <code>final_url</code>, <code>post_url</code>. Las subclases extienden este dict con sus campos espec\u00edficos v\u00eda <code>self.result.update({...})</code> en su propio <code>__init__</code>.</p> <p>Inicializa el parser base y construye el esqueleto de resultado.</p> <p>Parameters:</p> Name Type Description Default <code>html_content</code> <code>str</code> <p>HTML completo renderizado de la p\u00e1gina.</p> required <code>final_url</code> <code>str</code> <p>URL final de la p\u00e1gina (puede diferir de la original).</p> required <code>original_url</code> <code>str</code> <p>URL original solicitada por el usuario. Si est\u00e1 vac\u00eda se usa <code>final_url</code> como fallback.</p> <code>''</code> <code>traffic</code> <code>CapturedTraffic | None</code> <p>Tr\u00e1fico de red capturado durante el fetch. CRUCIAL: Los parsers deben usar esto para obtener datos estructurados de las APIs GraphQL.</p> <code>None</code> <code>debug</code> <code>bool</code> <p>Si True, activa logs de diagn\u00f3stico en nivel DEBUG.</p> <code>False</code> <code>platform</code> <code>str</code> <p>Identificador de plataforma (<code>\"facebook\"</code> o <code>\"instagram\"</code>). Poblado por las subclases concretas.</p> <code>''</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si <code>html_content</code> est\u00e1 vac\u00edo.</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser.parse","title":"parse  <code>abstractmethod</code>","text":"<pre><code>parse() -&gt; dict[str, Any]\n</code></pre> <p>Ejecuta el proceso completo de extracci\u00f3n.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Diccionario con todos los datos extra\u00eddos. La estructura</p> <code>dict[str, Any]</code> <p>exacta depende de cada subclase concreta.</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser._extract_json_blocks","title":"_extract_json_blocks","text":"<pre><code>_extract_json_blocks() -&gt; list[dict]\n</code></pre> <p>Extrae y parsea todos los bloques JSON embebidos en el HTML.</p> <p>Busca etiquetas <code>&lt;script type=\"application/json\"&gt;</code> y parsea su contenido. Los bloques inv\u00e1lidos se descartan silenciosamente (o con log DEBUG si <code>self.debug</code> est\u00e1 activo).</p> <p>Returns:</p> Type Description <code>list[dict]</code> <p>Lista de dicts, uno por cada bloque JSON v\u00e1lido encontrado.</p> <code>list[dict]</code> <p>Retorna lista vac\u00eda si no hay bloques o si el HTML es inv\u00e1lido.</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser._recursive_search","title":"_recursive_search","text":"<pre><code>_recursive_search(\n    data: Any,\n    condition: Callable[[dict], bool],\n    extract: Callable[[dict], Any] | None = None,\n) -&gt; Any | None\n</code></pre> <p>B\u00fasqueda Depth-First en una estructura de datos anidada.</p> <p>Recorre dicts y listas recursivamente hasta encontrar el primer nodo que cumpla <code>condition</code>.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Ra\u00edz de la estructura a explorar.</p> required <code>condition</code> <code>Callable[[dict], bool]</code> <p>Callable que recibe un dict y retorna True si es el nodo buscado.</p> required <code>extract</code> <code>Callable[[dict], Any] | None</code> <p>Callable opcional para transformar el nodo encontrado antes de retornarlo.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any | None</code> <p>El primer nodo encontrado (o su transformaci\u00f3n si se</p> <code>Any | None</code> <p>proporcion\u00f3 <code>extract</code>), o None si no se encontr\u00f3.</p> Example <p>node = self._recursive_search( ...     block, ...     condition=lambda n: n.get(\"__isFeedUnit\") == \"Story\", ... )</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser._find_all_nodes","title":"_find_all_nodes","text":"<pre><code>_find_all_nodes(\n    data: Any,\n    condition: Callable[[dict], bool],\n    depth: int = 0,\n    max_depth: int = 50,\n) -&gt; list[dict]\n</code></pre> <p>B\u00fasqueda DFS que recopila TODOS los nodos que cumplen la condici\u00f3n.</p> <p>A diferencia de <code>_recursive_search</code>, no se detiene en el primer resultado sino que acumula todos los nodos coincidentes.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Estructura de datos a recorrer.</p> required <code>condition</code> <code>Callable[[dict], bool]</code> <p>Funci\u00f3n que retorna True si el nodo cumple el criterio.</p> required <code>depth</code> <code>int</code> <p>Profundidad actual (uso interno para la recursi\u00f3n).</p> <code>0</code> <code>max_depth</code> <code>int</code> <p>Profundidad m\u00e1xima de recursi\u00f3n para evitar stack overflow.</p> <code>50</code> <p>Returns:</p> Type Description <code>list[dict]</code> <p>Lista con todos los nodos que cumplen la condici\u00f3n.</p> Example <p>stories = self._find_all_nodes( ...     block, ...     condition=lambda n: n.get(\"__typename\") == \"Story\", ... )</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser._safe_get","title":"_safe_get","text":"<pre><code>_safe_get(\n    data: Any, *keys: str, default: Any = None\n) -&gt; Any\n</code></pre> <p>Navega una ruta de claves anidadas en un dict de forma segura.</p> <p>Evita KeyError y TypeError al acceder a estructuras JSON profundamente anidadas cuya existencia no est\u00e1 garantizada.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>Any</code> <p>Diccionario ra\u00edz desde el que iniciar la navegaci\u00f3n.</p> required <code>*keys</code> <code>str</code> <p>Secuencia de claves que forman la ruta de navegaci\u00f3n.</p> <code>()</code> <code>default</code> <code>Any</code> <p>Valor a retornar si la ruta no existe o es None.</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p>El valor al final de la ruta, o <code>default</code> si alg\u00fan paso</p> <code>Any</code> <p>falla o retorna None.</p> Example <p>url = self._safe_get(actor, \"profile_picture\", \"uri\", default=\"\") count = self._safe_get(feedback, \"reaction_count\", \"count\", default=0)</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser._parse_timestamp","title":"_parse_timestamp  <code>staticmethod</code>","text":"<pre><code>_parse_timestamp(ts: Any) -&gt; datetime | None\n</code></pre> <p>Convierte un timestamp Unix a un objeto <code>datetime</code>.</p> <p>Parameters:</p> Name Type Description Default <code>ts</code> <code>Any</code> <p>Timestamp en segundos desde epoch (int, float o string). Acepta None de forma segura.</p> required <p>Returns:</p> Type Description <code>datetime | None</code> <p><code>datetime</code> local si la conversi\u00f3n es exitosa, <code>None</code></p> <code>datetime | None</code> <p>si el valor es inv\u00e1lido, None, o fuera de rango.</p> Example <p>dt = BaseParser._parse_timestamp(1700000000) dt = BaseParser._parse_timestamp(\"1700000000\") dt = BaseParser._parse_timestamp(None)  # \u2192 None</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser._get_graphql_response_by_operation","title":"_get_graphql_response_by_operation","text":"<pre><code>_get_graphql_response_by_operation(\n    operation_name: str,\n) -&gt; list[dict[str, Any]]\n</code></pre> <p>Filtra las respuestas GraphQL por nombre de operaci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>operation_name</code> <code>str</code> <p>Nombre exacto de la operaci\u00f3n GraphQL a filtrar (ej. <code>\"CommentsListComponentsPaginationQuery\"</code>).</p> required <p>Returns:</p> Type Description <code>list[dict[str, Any]]</code> <p>Lista de bodies parseados (dict) de las respuestas que coinciden.</p> <code>list[dict[str, Any]]</code> <p>Retorna lista vac\u00eda si no hay tr\u00e1fico disponible.</p>"},{"location":"api/parsers/#reaper.parsers.base_parser.BaseParser._get_first_graphql_response","title":"_get_first_graphql_response","text":"<pre><code>_get_first_graphql_response(\n    operation_name: str,\n) -&gt; dict[str, Any] | None\n</code></pre> <p>Obtiene la primera respuesta GraphQL que coincide con la operaci\u00f3n.</p> <p>Wrapper conveniente sobre <code>_get_graphql_response_by_operation</code> cuando solo se necesita un resultado.</p> <p>Parameters:</p> Name Type Description Default <code>operation_name</code> <code>str</code> <p>Nombre de la operaci\u00f3n GraphQL a filtrar.</p> required <p>Returns:</p> Type Description <code>dict[str, Any] | None</code> <p>Body parseado (dict) de la primera respuesta que coincide,</p> <code>dict[str, Any] | None</code> <p>o None si no hay coincidencias.</p>"},{"location":"api/public/","title":"API p\u00fablica \u2014 <code>reaper</code>","text":"<p>Todos los s\u00edmbolos documentados en esta p\u00e1gina se importan directamente desde el paquete <code>reaper</code> sin necesidad de acceder a subm\u00f3dulos internos.</p> <pre><code>from reaper import (\n    scrape,\n    Reaper,\n    ScraperConfig,\n    ScrapingError,\n    UnsupportedPlatformError,\n    AccountManager,\n    AccountProfile,\n    AccountActivity,\n    AccountStatus,\n    BaseAccountStorage,\n    LocalFileStorage,\n    StorageError,\n    ActionManager,\n    ActionType,\n    ActionResult,\n    ActionError,\n    build_action_storage,\n)\n</code></pre>"},{"location":"api/public/#scrape","title":"<code>scrape()</code>","text":""},{"location":"api/public/#reaper.scrape","title":"reaper.scrape  <code>async</code>","text":"<pre><code>scrape(\n    url: str,\n    *,\n    account_manager: AccountManager | None = None,\n    headless: bool = True,\n    debug: bool = False,\n    screenshot: bool = False,\n    auto_scroll: bool = True,\n    infinity_scroll: bool = False,\n    proxy_server: str | None = None,\n    proxy_username: str | None = None,\n    proxy_password: str | None = None,\n    account: str | None = None,\n) -&gt; dict[str, Any]\n</code></pre> <p>Scrape una URL y devuelve los datos extra\u00eddos como diccionario.</p> <p>Funci\u00f3n de conveniencia a nivel de m\u00f3dulo. Internamente crea una instancia de <code>Reaper</code> y delega en <code>Reaper.scrape()</code>.</p> <p>Para m\u00faltiples scrapes con rotaci\u00f3n de cuentas, es m\u00e1s eficiente usar la clase <code>Reaper</code> directamente, ya que reutiliza el <code>AccountManager</code> entre peticiones sin recrearlo.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>URL de Facebook o Instagram a scrapear. Obligatorio.</p> required <code>account_manager</code> <code>AccountManager | None</code> <p>Gestor de cuentas para rotaci\u00f3n autenticada.              <code>None</code> = modo an\u00f3nimo (defecto).</p> <code>None</code> <code>headless</code> <code>bool</code> <p>Ejecutar Firefox sin interfaz gr\u00e1fica. Defecto: True.</p> <code>True</code> <code>debug</code> <code>bool</code> <p>Logging detallado + artefactos. Defecto: False.</p> <code>False</code> <code>screenshot</code> <code>bool</code> <p>Capturar PNG del contenedor principal. Defecto: False.</p> <code>False</code> <code>auto_scroll</code> <code>bool</code> <p>Scroll autom\u00e1tico por altura DOM. Defecto: True.</p> <code>True</code> <code>infinity_scroll</code> <code>bool</code> <p>Scroll continuo por tr\u00e1fico de red. Defecto: False.</p> <code>False</code> <code>proxy_server</code> <code>str | None</code> <p>URL del proxy, p.ej. <code>\"http://ip:8080\"</code>.</p> <code>None</code> <code>proxy_username</code> <code>str | None</code> <p>Usuario del proxy.</p> <code>None</code> <code>proxy_password</code> <code>str | None</code> <p>Contrase\u00f1a del proxy.</p> <code>None</code> <code>account</code> <code>str | None</code> <p>Cuenta a forzar en esta sesi\u00f3n (account_id, username              o ID de usuario de la plataforma). Requiere              <code>account_manager</code>. <code>None</code> = rotaci\u00f3n autom\u00e1tica.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>dict[str, Any]</code> con los datos scrapeados.</p> <code>dict[str, Any]</code> <p>Siempre incluye: <code>url</code>, <code>platform</code>, <code>status</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>URL vac\u00eda o esquema inv\u00e1lido.</p> <code>UnsupportedPlatformError</code> <p>URL no es Facebook ni Instagram.</p> <code>ScrapingError</code> <p>Error en runtime durante fetch o parseo.</p> <p>Examples::</p> <pre><code># Modo an\u00f3nimo (comportamiento original)\nresult = asyncio.run(scrape(\"https://www.instagram.com/p/abc123\"))\n\n# Con cuenta autenticada\nmanager = AccountManager()\nawait manager.add_account(\"facebook\", \"usuario\", cookies=[...])\nresult = await scrape(\n    \"https://www.facebook.com/reel/123\",\n    account_manager=manager,\n)\n\n# Forzar una cuenta concreta por username\nresult = await scrape(\n    \"https://www.facebook.com/reel/123\",\n    account_manager=manager,\n    account=\"mi_usuario@gmail.com\",\n)\n</code></pre>"},{"location":"api/public/#reaper","title":"<code>Reaper</code>","text":""},{"location":"api/public/#reaper.core.Reaper","title":"reaper.core.Reaper","text":"<pre><code>Reaper(account_manager: 'AccountManager | None' = None)\n</code></pre> <p>Orquestador principal de Reaper.</p> <p>Puede reutilizarse para m\u00faltiples scrapes manteniendo el mismo <code>AccountManager</code>, lo que permite que el rotador acumule historial de actividad entre peticiones.</p> <p>Parameters:</p> Name Type Description Default <code>account_manager</code> <code>'AccountManager | None'</code> <p>Gestor de cuentas para rotaci\u00f3n autenticada.              <code>None</code> = modo an\u00f3nimo (comportamiento original).</p> <code>None</code> <p>Ejemplo con cuentas::</p> <pre><code>from reaper.auth import AccountManager\nfrom reaper import Reaper\n\nmanager = AccountManager()\nreaper  = Reaper(account_manager=manager)\n\nresult = await reaper.scrape(\"https://www.facebook.com/reel/123\")\nresult = await reaper.scrape(\"https://www.instagram.com/p/abc/\")\n</code></pre> <p>Ejemplo sin cuentas (comportamiento original)::</p> <pre><code>reaper = Reaper()\nresult = await reaper.scrape(\"https://www.facebook.com/reel/123\")\n</code></pre>"},{"location":"api/public/#reaper.core.Reaper.scrape","title":"scrape  <code>async</code>","text":"<pre><code>scrape(\n    url: str,\n    *,\n    headless: bool = True,\n    debug: bool = False,\n    screenshot: bool = False,\n    auto_scroll: bool = True,\n    infinity_scroll: bool = False,\n    proxy_server: str | None = None,\n    proxy_username: str | None = None,\n    proxy_password: str | None = None,\n    account: str | None = None,\n) -&gt; dict[str, Any]\n</code></pre> <p>Extrae datos de una URL de Facebook o Instagram.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>URL de Facebook o Instagram. Obligatorio.</p> required <code>headless</code> <code>bool</code> <p>Ejecutar Firefox sin interfaz gr\u00e1fica. Defecto: True.</p> <code>True</code> <code>debug</code> <code>bool</code> <p>Logging detallado + artefactos. Defecto: False.</p> <code>False</code> <code>screenshot</code> <code>bool</code> <p>Capturar PNG del contenedor principal. Defecto: False.</p> <code>False</code> <code>auto_scroll</code> <code>bool</code> <p>Scroll autom\u00e1tico por altura DOM. Defecto: True.</p> <code>True</code> <code>infinity_scroll</code> <code>bool</code> <p>Scroll continuo por tr\u00e1fico de red. Defecto: False.</p> <code>False</code> <code>proxy_server</code> <code>str | None</code> <p>URL del proxy, p.ej. <code>\"http://ip:8080\"</code>.</p> <code>None</code> <code>proxy_username</code> <code>str | None</code> <p>Usuario del proxy.</p> <code>None</code> <code>proxy_password</code> <code>str | None</code> <p>Contrase\u00f1a del proxy.</p> <code>None</code> <code>account</code> <code>str | None</code> <p>Identificador de la cuenta a forzar en esta sesi\u00f3n:             <code>account_id</code> (UUID4), <code>username</code> o ID de usuario             de la plataforma (<code>c_user</code>/<code>ds_user_id</code>).             <code>None</code> = rotaci\u00f3n autom\u00e1tica. Requiere que el             <code>Reaper</code> se haya construido con <code>account_manager</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>dict[str, Any]</code> con los datos scrapeados.</p> <code>dict[str, Any]</code> <p>Siempre incluye: <code>url</code>, <code>platform</code>, <code>status</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>URL vac\u00eda o esquema inv\u00e1lido.</p> <code>UnsupportedPlatformError</code> <p>URL no es Facebook ni Instagram.</p> <code>ScrapingError</code> <p>Error en runtime durante fetch o parseo.</p>"},{"location":"api/public/#excepciones","title":"Excepciones","text":""},{"location":"api/public/#reaper.core.ScrapingError","title":"reaper.core.ScrapingError","text":"<p>               Bases: <code>Exception</code></p> <p>El proceso de scraping fall\u00f3 en tiempo de ejecuci\u00f3n.</p>"},{"location":"api/public/#reaper.core.UnsupportedPlatformError","title":"reaper.core.UnsupportedPlatformError","text":"<p>               Bases: <code>Exception</code></p> <p>La URL no corresponde a ninguna plataforma soportada.</p>"},{"location":"api/public/#acciones-de-escritura","title":"Acciones de escritura","text":"<p>S\u00edmbolos de <code>reaper.actions</code> re-exportados desde el paquete ra\u00edz <code>reaper</code>. Ver API Reference \u2014 reaper.actions para la referencia completa.</p>"},{"location":"api/public/#reaper.actions.ActionManager","title":"reaper.actions.ActionManager","text":"<pre><code>ActionManager(\n    account_manager: Any | None = None,\n    storage: BaseActionStorage | None = None,\n    *,\n    headless: bool = True,\n    browser_type: str = \"firefox\",\n    proxy_server: str | None = None,\n    proxy_username: str | None = None,\n    proxy_password: str | None = None,\n    base_url: str = DEFAULT_BASE_URL,\n    browser_factory: Any | None = None,\n)\n</code></pre> <p>Orquesta acciones de escritura autenticadas y persiste sus resultados.</p> <p>Parameters:</p> Name Type Description Default <code>account_manager</code> <code>Any | None</code> <p>Gestor de cuentas que provee cookies y rotaci\u00f3n. Obligatorio para todas las acciones (la escritura requiere sesi\u00f3n).</p> <code>None</code> <code>storage</code> <code>BaseActionStorage | None</code> <p>Backend de persistencia de acciones. Si es <code>None</code>, se construye desde la configuraci\u00f3n de <code>reaper.toml</code> (defecto: <code>LocalActionStorage</code> en <code>data/actions</code>).</p> <code>None</code> <code>headless</code> <code>bool</code> <p>Ejecutar el navegador sin interfaz gr\u00e1fica. Defecto: True.</p> <code>True</code> <code>browser_type</code> <code>str</code> <p>Perfil de fingerprint (<code>\"firefox\"</code> o <code>\"chromium\"</code>).</p> <code>'firefox'</code> <code>proxy_server / proxy_username / proxy_password</code> <p>Configuraci\u00f3n del proxy de salida para las sesiones.</p> required <code>base_url</code> <code>str</code> <p>Ra\u00edz de navegaci\u00f3n de Facebook. Defecto: <code>https://m.facebook.com/</code>.</p> <code>DEFAULT_BASE_URL</code> <code>browser_factory</code> <code>Any | None</code> <p>Factory de navegador alternativo inyectable para tests (equivalente a <code>SessionActor.use_browser_factory</code>).</p> <code>None</code>"},{"location":"api/public/#reaper.actions.ActionManager.storage","title":"storage  <code>property</code>","text":"<pre><code>storage: BaseActionStorage\n</code></pre> <p>Backend de persistencia en uso (para consultas del operador).</p>"},{"location":"api/public/#reaper.actions.ActionManager.create_post","title":"create_post  <code>async</code>","text":"<pre><code>create_post(\n    *,\n    text: str | None = None,\n    images: list[str | Path] | None = None,\n    group: str | None = None,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Publica un post de texto y/o im\u00e1genes en el muro (o en un grupo).</p> <p>Parameters:</p> Name Type Description Default <code>text</code> <code>str | None</code> <p>Texto/caption de la publicaci\u00f3n.</p> <code>None</code> <code>images</code> <code>list[str | Path] | None</code> <p>Rutas locales de im\u00e1genes a adjuntar (jpg, png, gif, ...).</p> <code>None</code> <code>group</code> <code>str | None</code> <p>Si se indica (ID o URL de grupo), la publicaci\u00f3n se hace       directamente dentro de ese grupo en lugar del muro.</p> <code>None</code> <code>account</code> <code>str | None</code> <p>Cuenta a forzar (account_id, username o c_user).       <code>None</code> = rotaci\u00f3n autom\u00e1tica del AccountManager.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM del post.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido con el resultado de la operaci\u00f3n.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si no hay cuenta autenticada, el post est\u00e1 vac\u00edo o las im\u00e1genes no son v\u00e1lidas.</p>"},{"location":"api/public/#reaper.actions.ActionManager.share_post","title":"share_post  <code>async</code>","text":"<pre><code>share_post(\n    *,\n    post_url: str,\n    group: str,\n    group_name: str | None = None,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Comparte una publicaci\u00f3n existente dentro de un grupo.</p> <p>Parameters:</p> Name Type Description Default <code>post_url</code> <code>str</code> <p>URL de la publicaci\u00f3n a compartir.</p> required <code>group</code> <code>str</code> <p>Grupo de destino (ID o URL). Se usa como texto de          b\u00fasqueda en el di\u00e1logo si no se da <code>group_name</code>.</p> required <code>group_name</code> <code>str | None</code> <p>Nombre visible del grupo de destino (recomendado);          es el texto que se busca en el di\u00e1logo de compartir.</p> <code>None</code> <code>account</code> <code>str | None</code> <p>Cuenta a forzar. <code>None</code> = rotaci\u00f3n autom\u00e1tica.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si la URL del post o la cuenta son inv\u00e1lidas.</p>"},{"location":"api/public/#reaper.actions.ActionManager.comment","title":"comment  <code>async</code>","text":"<pre><code>comment(\n    *,\n    post_url: str,\n    text: str,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Responde con texto (comenta) una publicaci\u00f3n existente.</p> <p>Parameters:</p> Name Type Description Default <code>post_url</code> <code>str</code> <p>URL de la publicaci\u00f3n a comentar.</p> required <code>text</code> <code>str</code> <p>Texto del comentario.</p> required <code>account</code> <code>str | None</code> <p>Cuenta a forzar. <code>None</code> = rotaci\u00f3n autom\u00e1tica.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si el texto est\u00e1 vac\u00edo o la cuenta es inv\u00e1lida.</p>"},{"location":"api/public/#reaper.actions.ActionManager.like","title":"like  <code>async</code>","text":"<pre><code>like(\n    *,\n    post_url: str,\n    account: str | None = None,\n    confirm_timeout: int = 15000,\n) -&gt; ActionResult\n</code></pre> <p>Reacciona con \"Me gusta\" a una publicaci\u00f3n existente.</p> <p>Parameters:</p> Name Type Description Default <code>post_url</code> <code>str</code> <p>URL de la publicaci\u00f3n a reaccionar.</p> required <code>account</code> <code>str | None</code> <p>Cuenta a forzar. <code>None</code> = rotaci\u00f3n autom\u00e1tica.</p> <code>None</code> <code>confirm_timeout</code> <code>int</code> <p>Timeout (ms) de la confirmaci\u00f3n DOM.</p> <code>15000</code> <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> persistido.</p> <p>Raises:</p> Type Description <code>ActionError</code> <p>Si la URL del post o la cuenta son inv\u00e1lidas.</p>"},{"location":"api/public/#reaper.actions.ActionManager.close","title":"close  <code>async</code>","text":"<pre><code>close() -&gt; None\n</code></pre> <p>Cierra los recursos del backend de almacenamiento de acciones.</p>"},{"location":"api/public/#reaper.actions.ActionResult","title":"reaper.actions.ActionResult  <code>dataclass</code>","text":"<pre><code>ActionResult(\n    action: str,\n    platform: str = \"facebook\",\n    action_id: str = (lambda: uuid.uuid4().hex)(),\n    account_id: str | None = None,\n    account_username: str | None = None,\n    status: str = \"ok\",\n    post_url: str | None = None,\n    post_id: str | None = None,\n    comment_id: str | None = None,\n    group: str | None = None,\n    text: str | None = None,\n    error: str | None = None,\n    executed_at: str = _now_iso(),\n)\n</code></pre> <p>Registro del resultado de una acci\u00f3n de escritura ejecutada.</p> <p>Es el contrato de datos entre <code>ActionManager</code>, el <code>SessionActor</code> y el almacenamiento de acciones. Se serializa con <code>to_dict()</code> / <code>from_dict()</code> para persistir en cualquier backend (JSON, PostgreSQL, MongoDB).</p> <p>Attributes:</p> Name Type Description <code>action_id</code> <code>str</code> <p>UUID4 interno del registro. Se genera al ejecutar.</p> <code>action</code> <code>str</code> <p>Valor de <code>ActionType</code> (<code>\"post\"</code>, <code>\"comment\"</code>, ...).</p> <code>platform</code> <code>str</code> <p>Plataforma sobre la que se ejecut\u00f3 (<code>\"facebook\"</code>).</p> <code>account_id</code> <code>str | None</code> <p>UUID4 de la cuenta autenticada usada.</p> <code>account_username</code> <code>str | None</code> <p>Username de la cuenta (para logs/auditor\u00eda).</p> <code>status</code> <code>str</code> <p><code>\"ok\"</code> si la plataforma confirm\u00f3 la acci\u00f3n,              <code>\"error\"</code> si fall\u00f3.</p> <code>post_url</code> <code>str | None</code> <p>URL de la publicaci\u00f3n creada o objetivo.</p> <code>post_id</code> <code>str | None</code> <p>ID de la publicaci\u00f3n creada o objetivo (si se detecta).</p> <code>comment_id</code> <code>str | None</code> <p>ID del comentario publicado (solo <code>comment</code>).</p> <code>group</code> <code>str | None</code> <p>Identificador del grupo (id o URL) para acciones de grupo.</p> <code>text</code> <code>str | None</code> <p>Texto enviado (contenido del post o del comentario).</p> <code>error</code> <code>str | None</code> <p>Mensaje de error cuando <code>status=\"error\"</code>.</p> <code>executed_at</code> <code>str</code> <p>Timestamp UTC ISO-8601 de la ejecuci\u00f3n.</p>"},{"location":"api/public/#reaper.actions.ActionResult.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; dict[str, Any]\n</code></pre> <p>Convierte el resultado en un dict serializable a JSON.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict con todos los campos del registro.</p>"},{"location":"api/public/#reaper.actions.ActionResult.from_dict","title":"from_dict  <code>classmethod</code>","text":"<pre><code>from_dict(data: dict[str, Any]) -&gt; ActionResult\n</code></pre> <p>Reconstruye un <code>ActionResult</code> desde un dict deserializado.</p> <p>Parameters:</p> Name Type Description Default <code>data</code> <code>dict[str, Any]</code> <p>Dict producido por :meth:<code>to_dict</code>.</p> required <p>Returns:</p> Type Description <code>ActionResult</code> <p><code>ActionResult</code> con los valores del dict.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si falta el campo <code>action</code>.</p>"},{"location":"api/public/#reaper.actions.ActionType","title":"reaper.actions.ActionType","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Tipo de acci\u00f3n de escritura soportada por Reaper.</p> Valores <p>POST:           Crear una publicaci\u00f3n nueva en el muro de la cuenta. GROUP_POST:     Publicar un post nuevo directamente dentro de un grupo. SHARE_TO_GROUP: Compartir una publicaci\u00f3n existente dentro de un grupo. COMMENT:        Responder con texto a una publicaci\u00f3n existente. LIKE:           Reaccionar con \"Me gusta\" a una publicaci\u00f3n existente.</p>"},{"location":"api/public/#reaper.actions.ActionError","title":"reaper.actions.ActionError","text":"<p>               Bases: <code>Exception</code></p> <p>Error en la ejecuci\u00f3n de una acci\u00f3n de escritura.</p> <p>Se lanza cuando la acci\u00f3n no puede ejecutarse: falta de cuenta con cookies v\u00e1lidas, elemento de la interfaz no encontrado, o la plataforma rechaza la operaci\u00f3n.</p>"},{"location":"api/public/#reaper.actions.build_action_storage","title":"reaper.actions.build_action_storage","text":"<pre><code>build_action_storage(\n    config: StorageConfig | None = None,\n) -&gt; BaseActionStorage\n</code></pre> <p>Instancia el backend de almacenamiento de acciones de la configuraci\u00f3n.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>StorageConfig | None</code> <p>Configuraci\u00f3n de storage (la misma de las cuentas). Si es <code>None</code>, la carga desde el fichero con :func:<code>reaper.auth.storage.load_storage_config</code>.</p> <code>None</code> <p>Returns:</p> Type Description <code>BaseActionStorage</code> <p><code>LocalActionStorage</code>, <code>PostgresActionStorage</code> o</p> <code>BaseActionStorage</code> <p><code>MongoActionStorage</code> seg\u00fan <code>config.backend</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si el backend no est\u00e1 soportado o falta su configuraci\u00f3n.</p>"},{"location":"api/scrapers/","title":"API Reference \u2014 <code>reaper.scrapers</code>","text":""},{"location":"api/scrapers/#basescraper","title":"<code>BaseScraper</code>","text":""},{"location":"api/scrapers/#reaper.scrapers.base.BaseScraper","title":"reaper.scrapers.base.BaseScraper","text":"<pre><code>BaseScraper(config: ScraperConfig)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Clase base abstracta para scrapers de plataforma.</p> <p>Las subclases deben implementar <code>run()</code> y <code>_fetch()</code>. El m\u00e9todo <code>_fetch_with_account()</code> est\u00e1 implementado aqu\u00ed y es el punto de entrada principal para fetches que requieren rotaci\u00f3n de cuentas y refresco autom\u00e1tico de cookies.</p> <p>Parameters:</p> Name Type Description Default <code>config</code> <code>ScraperConfig</code> <p>Configuraci\u00f3n validada de la sesi\u00f3n de scraping.</p> required"},{"location":"api/scrapers/#reaper.scrapers.base.BaseScraper.run","title":"run  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>run() -&gt; dict[str, Any]\n</code></pre> <p>Ejecuta la sesi\u00f3n completa de scraping.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p><code>dict[str, Any]</code> con los datos extra\u00eddos.</p> <code>dict[str, Any]</code> <p>Debe incluir siempre: <code>url</code>, <code>platform</code>, <code>status</code>.</p> <p>Raises:</p> Type Description <code>ScrapingError</code> <p>Si el proceso falla irrecuperablemente.</p>"},{"location":"api/scrapers/#reaper.scrapers.base.BaseScraper._fetch","title":"_fetch  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>_fetch(\n    override_url: str | None = None,\n    cookies: list[dict[str, Any]] | None = None,\n) -&gt; Any\n</code></pre> <p>Fetch base: crea ContentFetcher y ejecuta la navegaci\u00f3n.</p> <p>Cada scraper implementa este m\u00e9todo con sus par\u00e1metros espec\u00edficos (p.ej. InstagramScraper desactiva infinity_scroll siempre).</p> <p>Parameters:</p> Name Type Description Default <code>override_url</code> <code>str | None</code> <p>URL alternativa a la de <code>config.url</code>.           Se usa en fetches secundarios (merge reel\u2194video).</p> <code>None</code> <code>cookies</code> <code>list[dict[str, Any]] | None</code> <p>Lista de cookies en formato Playwright a inyectar           en el contexto del navegador antes de navegar.           <code>None</code> = sin cookies (modo an\u00f3nimo).</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p><code>FetchResult</code> con HTML renderizado, tr\u00e1fico, metadatos y</p> <code>Any</code> <p><code>updated_cookies</code> con las cookies post-navegaci\u00f3n si la</p> <code>Any</code> <p>sesi\u00f3n era autenticada.</p>"},{"location":"api/scrapers/#reaper.scrapers.base.BaseScraper._fetch_with_account","title":"_fetch_with_account  <code>async</code>","text":"<pre><code>_fetch_with_account(\n    platform: str, override_url: str | None = None\n) -&gt; Any\n</code></pre> <p>Fetch con selecci\u00f3n de cuenta, registro de actividad y auto-refresh.</p> <p>Coordina el flujo completo de una petici\u00f3n autenticada:</p> <ol> <li>Consulta al <code>AccountManager</code> por la mejor cuenta disponible.</li> <li>Lee las cookies de disco para esa cuenta.</li> <li>Ejecuta el fetch con las cookies inyectadas en el contexto Playwright.</li> <li>Registra \u00e9xito o fallo en la actividad de la cuenta.</li> <li>Si el fetch fue exitoso, compara las cookies originales con las    devueltas por el servidor post-navegaci\u00f3n. Si difieren, las persiste    autom\u00e1ticamente \u2014 el servidor habr\u00e1 extendido TTLs o rotado tokens    de seguridad, alargando la vida de la sesi\u00f3n.</li> </ol> <p>Si <code>config.account_manager</code> es <code>None</code>, o si no hay cuentas disponibles, delega en <code>_fetch()</code> directamente en modo an\u00f3nimo. Este fallback garantiza compatibilidad total hacia atr\u00e1s.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p><code>\"facebook\"</code> o <code>\"instagram\"</code>.</p> required <code>override_url</code> <code>str | None</code> <p>URL alternativa (raramente necesaria en el fetch primario).</p> <code>None</code> <p>Returns:</p> Type Description <code>Any</code> <p><code>FetchResult</code> igual que <code>_fetch()</code>.</p>"},{"location":"api/scrapers/#reaper.scrapers.base.BaseScraper._base_result","title":"_base_result","text":"<pre><code>_base_result(platform: str) -&gt; dict[str, Any]\n</code></pre> <p>Genera el esqueleto m\u00ednimo garantizado para cualquier resultado.</p> <p>Parameters:</p> Name Type Description Default <code>platform</code> <code>str</code> <p>Nombre de la plataforma (<code>\"facebook\"</code> o <code>\"instagram\"</code>).</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dict con <code>url</code>, <code>platform</code> y <code>status=\"ok\"</code>.</p>"},{"location":"api/utils/","title":"API Reference \u2014 <code>reaper.utils</code>","text":""},{"location":"api/utils/#logging","title":"Logging","text":""},{"location":"api/utils/#reaper.utils.logger.setup_logging","title":"reaper.utils.logger.setup_logging","text":"<pre><code>setup_logging(\n    level: str | int = \"INFO\",\n    use_color: bool | None = None,\n    log_file: str | None = None,\n    propagate: bool = False,\n) -&gt; None\n</code></pre> <p>Configura el logging del proyecto completo.</p> <p>Debe llamarse una sola vez al inicio del programa, antes de cualquier llamada a <code>get_logger</code> o uso de loggers del proyecto. Configura el logger ra\u00edz <code>\"reaper\"</code> con los handlers apropiados.</p> <p>Parameters:</p> Name Type Description Default <code>level</code> <code>str | int</code> <p>Nivel de log como string (<code>\"DEBUG\"</code>, <code>\"INFO\"</code>, etc.) o como constante <code>logging.*</code>. Default: <code>\"INFO\"</code>.</p> <code>'INFO'</code> <code>use_color</code> <code>bool | None</code> <p>Si True, activa colores ANSI en el handler de consola. Si None (default), autodetecta seg\u00fan <code>sys.stderr.isatty()</code>.</p> <code>None</code> <code>log_file</code> <code>str | None</code> <p>Ruta opcional a un fichero de log adicional. El fichero usa siempre el formato plano (sin color) y nivel <code>DEBUG</code>.</p> <code>None</code> <code>propagate</code> <code>bool</code> <p>Si True, los mensajes se propagan tambi\u00e9n al logger ra\u00edz de Python (\u00fatil para frameworks que configuran el root). Default: <code>False</code> \u2014 Reaper gestiona sus propios handlers.</p> <code>False</code> <p>Example::</p> <pre><code># CLI normal\nsetup_logging(level=\"INFO\")\n\n# Sesi\u00f3n de depuraci\u00f3n\nsetup_logging(level=\"DEBUG\", use_color=True)\n\n# Producci\u00f3n con fichero de log\nsetup_logging(level=\"WARNING\", log_file=\"/var/log/reaper.log\")\n</code></pre>"},{"location":"api/utils/#reaper.utils.logger.get_logger","title":"reaper.utils.logger.get_logger","text":"<pre><code>get_logger(name: str) -&gt; logging.Logger\n</code></pre> <p>Devuelve el logger para el m\u00f3dulo indicado.</p> <p>Wrapper m\u00ednimo sobre <code>logging.getLogger(name)</code> que act\u00faa como punto de importaci\u00f3n \u00fanico para todos los m\u00f3dulos del proyecto. No a\u00f1ade configuraci\u00f3n propia \u2014 el logger hereda la configuraci\u00f3n establecida por <code>setup_logging</code> a trav\u00e9s de la jerarqu\u00eda <code>\"reaper.*\"</code>.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Nombre del m\u00f3dulo. Usar siempre <code>__name__</code> para preservar la jerarqu\u00eda autom\u00e1tica de loggers.</p> required <p>Returns:</p> Type Description <code>Logger</code> <p>Logger configurado para el m\u00f3dulo.</p> <p>Example::</p> <pre><code># En cualquier m\u00f3dulo del proyecto:\nfrom reaper.utils.logger import get_logger\nlogger = get_logger(__name__)\n</code></pre>"},{"location":"api/utils/#plataformas","title":"Plataformas","text":""},{"location":"api/utils/#reaper.utils.platforms.detect_platform_from_url","title":"reaper.utils.platforms.detect_platform_from_url","text":"<pre><code>detect_platform_from_url(url: str) -&gt; str\n</code></pre> <p>Detecta la plataforma de redes sociales a partir de la URL.</p> <p>Extrae el netloc de la URL y lo compara contra el cat\u00e1logo de dominios <code>PLATFORM_DOMAINS</code>. La comparaci\u00f3n usa <code>netloc == dominio</code> o <code>netloc.endswith('.' + dominio)</code>, lo que evita falsos positivos por substring (p. ej. <code>\"t.co\"</code> no coincide con <code>\"snapchat.com\"</code>).</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>URL completa o parcial con dominio. Se acepta con o sin  protocolo.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Identificador de la plataforma en min\u00fasculas (ej. <code>\"facebook\"</code>,</p> <code>str</code> <p><code>\"youtube\"</code>, <code>\"x\"</code>), o <code>\"unknown\"</code> si el dominio no coincide</p> <code>str</code> <p>con ninguna entrada del cat\u00e1logo.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; detect_platform_from_url(\"https://www.facebook.com/reel/12345\")\n'facebook'\n&gt;&gt;&gt; detect_platform_from_url(\"https://youtu.be/dQw4w9WgXcQ\")\n'youtube'\n&gt;&gt;&gt; detect_platform_from_url(\"https://twitter.com/user/status/123\")\n'x'\n&gt;&gt;&gt; detect_platform_from_url(\"https://t.co/abc123\")\n'x'\n&gt;&gt;&gt; detect_platform_from_url(\"https://vm.tiktok.com/abc123/\")\n'tiktok'\n&gt;&gt;&gt; detect_platform_from_url(\"https://t.me/channel/123\")\n'telegram'\n&gt;&gt;&gt; detect_platform_from_url(\"https://wa.me/+34612345678\")\n'whatsapp'\n&gt;&gt;&gt; detect_platform_from_url(\"https://www.snapchat.com/add/user\")\n'snapchat'\n&gt;&gt;&gt; detect_platform_from_url(\"https://bsky.app/profile/user\")\n'bluesky'\n&gt;&gt;&gt; detect_platform_from_url(\"https://discord.gg/abc123\")\n'discord'\n&gt;&gt;&gt; detect_platform_from_url(\"https://www.google.com\")\n'unknown'\n&gt;&gt;&gt; detect_platform_from_url(\"\")\n'unknown'\n</code></pre>"},{"location":"api/utils/#clasificador-de-urls-de-facebook","title":"Clasificador de URLs de Facebook","text":""},{"location":"api/utils/#reaper.utils.fb_url_classifier.get_parser_from_fb_url","title":"reaper.utils.fb_url_classifier.get_parser_from_fb_url","text":"<pre><code>get_parser_from_fb_url(url: str) -&gt; str\n</code></pre> <p>Determina el parser a utilizar para una URL de Facebook.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>URL de Facebook a clasificar.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Nombre de la clase de parser:</p> <code>str</code> <p><code>\"PostParser\"</code>, <code>\"ReelParser\"</code>, <code>\"VideoParser\"</code>,</p> <code>str</code> <p><code>\"GroupParser\"</code>, <code>\"ProfileParser\"</code>, <code>\"PhotoParser\"</code>,</p> <code>str</code> <p><code>\"HashtagParser\"</code>, <code>\"UnknownParser\"</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; get_parser_from_fb_url(\"https://www.facebook.com/reel/816043001524221\")\n'ReelParser'\n&gt;&gt;&gt; get_parser_from_fb_url(\"https://www.facebook.com/photo.php?fbid=861685686929188\")\n'PhotoParser'\n&gt;&gt;&gt; get_parser_from_fb_url(\"https://www.facebook.com/permalink.php?story_fbid=pfbid035&amp;id=123\")\n'PostParser'\n&gt;&gt;&gt; get_parser_from_fb_url(\"https://www.facebook.com/permalink.php?id=61576849942202\")\n'PostParser'\n&gt;&gt;&gt; get_parser_from_fb_url(\"https://www.facebook.com/groups/12345\")\n'GroupParser'\n&gt;&gt;&gt; get_parser_from_fb_url(\"https://www.facebook.com/zurdobo7\")\n'ProfileParser'\n&gt;&gt;&gt; get_parser_from_fb_url(\n...     \"https://www.facebook.com/NoticiasTelemundo/posts/\"\n...     \"-liveblog-l-la-casa-blanca/1532119218279349/\"\n... )\n'PostParser'\n</code></pre>"},{"location":"api/utils/#detector-de-autenticacion","title":"Detector de autenticaci\u00f3n","text":""},{"location":"api/utils/#reaper.utils.fb_auth_detector.requires_auth","title":"reaper.utils.fb_auth_detector.requires_auth","text":"<pre><code>requires_auth(\n    html_content: str, final_url: str = \"\"\n) -&gt; AuthResult\n</code></pre> <p>Detecta si el HTML de Facebook o Instagram indica que se requiere autenticaci\u00f3n.</p> <p>Analiza el HTML en capas ordenadas de m\u00e1s espec\u00edfica a m\u00e1s general. El orden es cr\u00edtico: capas m\u00e1s espec\u00edficas deben ejecutarse primero para evitar que se\u00f1ales generales produzcan falsos negativos.</p> <pre><code>1. Contenido expl\u00edcitamente no disponible \u2014 dos niveles de se\u00f1ales:\n     \u00b7 JSON (ScheduledServerJS, HTML SSR): canonicalRouteName, tracePolicy\n     \u00b7 DOM (post-hidrataci\u00f3n): &lt;title&gt;, &lt;span&gt; con texto de error\n2. Presencia de se\u00f1ales de contenido real (early exit: NO bloqueado)\n3. URL final redirigida a /login, /checkpoint (FB) o /accounts/login (IG)\n4. Se\u00f1ales JSON fuertes (tracePolicy, canonicalRouteName) \u2014 solo Facebook\n5. Se\u00f1ales JSON combinadas (CometErrorRoot + privacy) \u2014 solo Facebook\n</code></pre> <p>Parameters:</p> Name Type Description Default <code>html_content</code> <code>str</code> <p>HTML renderizado por Playwright (SSR o post-hidrataci\u00f3n).</p> required <code>final_url</code> <code>str</code> <p>URL final tras redirecciones HTTP.</p> <code>''</code> <p>Returns:</p> Type Description <code>AuthResult</code> <p>class:<code>AuthResult</code>: - <code>requires_auth=False</code> + <code>auth_type=\"content_unavailable\"</code> si   el contenido fue eliminado o restringido permanentemente (FB e IG). - <code>requires_auth=False</code> sin <code>auth_type</code> si la p\u00e1gina tiene   contenido real. - <code>requires_auth=True</code> con <code>auth_type</code> correspondiente si hay   muro de auth.</p>"},{"location":"api/utils/#reaper.utils.fb_auth_detector.AuthResult","title":"reaper.utils.fb_auth_detector.AuthResult  <code>dataclass</code>","text":"<pre><code>AuthResult(\n    requires_auth: bool,\n    reason: str | None = None,\n    auth_type: str | None = None,\n)\n</code></pre> <p>Resultado del an\u00e1lisis de bloqueo de acceso.</p>"},{"location":"api/utils/#metricas","title":"M\u00e9tricas","text":""},{"location":"api/utils/#reaper.utils.metrics.SocialMediaParser","title":"reaper.utils.metrics.SocialMediaParser","text":"<pre><code>SocialMediaParser()\n</code></pre> <p>Parser de m\u00e9tricas de redes sociales con soporte de sufijos K, M y B.</p> <p>Convierte representaciones textuales de cantidades (como <code>\"5.2K\"</code>, <code>\"1.5M\"</code>, <code>\"2,300\"</code>) a valores enteros. Dise\u00f1ado para parsear los valores de engagement que aparecen en el HTML o tr\u00e1fico JSON de plataformas como Facebook o Instagram.</p> <p>Los resultados se acumulan en <code>self.result</code> y tambi\u00e9n se devuelven como valor de retorno de cada m\u00e9todo, lo que permite encadenar llamadas o usar la instancia como acumulador.</p> <p>Attributes:</p> Name Type Description <code>result</code> <code>dict[str, int]</code> <p>Diccionario acumulativo con todos los campos parseados.     Se rellena en cada llamada a <code>_parse_metric</code> o     <code>parse_engagement_metrics</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; parser = SocialMediaParser()\n&gt;&gt;&gt; parser._parse_metric(\"5.2K\", \"likes\")\n5200\n&gt;&gt;&gt; parser._parse_metric(\"1.5M\", \"shares\")\n1500000\n&gt;&gt;&gt; parser.result\n{'likes': 5200, 'shares': 1500000}\n</code></pre> <p>Inicializa el parser con un acumulador de resultados vac\u00edo.</p>"},{"location":"api/utils/#reaper.utils.metrics.SocialMediaParser.parse_engagement_metrics","title":"parse_engagement_metrics","text":"<pre><code>parse_engagement_metrics(\n    reaction_count: str | int | float | None,\n    share_count: str | int | float | None,\n    comment_count: str | int | float | None = None,\n) -&gt; dict[str, int]\n</code></pre> <p>Parsea las m\u00e9tricas principales de engagement en una sola llamada.</p> <p>Parsea reacciones, compartidos y opcionalmente comentarios, y los almacena en <code>self.result</code>. Es el m\u00e9todo de conveniencia habitual para extraer m\u00e9tricas del tr\u00e1fico de redes sociales.</p> <p>Parameters:</p> Name Type Description Default <code>reaction_count</code> <code>str | int | float | None</code> <p>Total de reacciones (likes, loves, etc.).</p> required <code>share_count</code> <code>str | int | float | None</code> <p>Total de veces que se comparti\u00f3 el contenido.</p> required <code>comment_count</code> <code>str | int | float | None</code> <p>Total de comentarios. Si es <code>None</code>, no se a\u00f1ade            <code>comment_count</code> al resultado.</p> <code>None</code> <p>Returns:</p> Type Description <code>dict[str, int]</code> <p>Diccionario con las m\u00e9tricas parseadas. Siempre contiene</p> <code>dict[str, int]</code> <p><code>reaction_count</code> y <code>share_count</code>. Contiene <code>comment_count</code></p> <code>dict[str, int]</code> <p>solo si se proporcion\u00f3 un valor distinto de <code>None</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; parser = SocialMediaParser()\n&gt;&gt;&gt; parser.parse_engagement_metrics(\"5.2K\", \"342\", 89)\n{'reaction_count': 5200, 'share_count': 342, 'comment_count': 89}\n</code></pre> <pre><code>&gt;&gt;&gt; parser2 = SocialMediaParser()\n&gt;&gt;&gt; parser2.parse_engagement_metrics(\"1.1M\", None)\n{'reaction_count': 1100000, 'share_count': 0}\n</code></pre>"},{"location":"api/utils/#codificacion","title":"Codificaci\u00f3n","text":""},{"location":"api/utils/#reaper.utils.encoding.datetime_encoder","title":"reaper.utils.encoding.datetime_encoder","text":"<pre><code>datetime_encoder(obj: Any) -&gt; str\n</code></pre> <p>Encoder personalizado para serializaci\u00f3n JSON de objetos <code>datetime</code>.</p> <p>Usar como argumento <code>default</code> en <code>json.dump</code> / <code>json.dumps</code> cuando el diccionario a serializar contiene campos de tipo <code>datetime</code>.</p> <p>Parameters:</p> Name Type Description Default <code>obj</code> <code>Any</code> <p>Objeto a serializar. Si es <code>datetime</code>, se convierte a ISO 8601.  Para cualquier otro tipo no serializable natively por JSON, se  lanza <code>TypeError</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Representaci\u00f3n ISO 8601 del datetime (ej. <code>\"2024-01-15T14:30:00\"</code>).</p> <p>Raises:</p> Type Description <code>TypeError</code> <p>Si el objeto no es de tipo <code>datetime</code>.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; import json\n&gt;&gt;&gt; from datetime import datetime\n&gt;&gt;&gt; from utils.encoding import datetime_encoder\n&gt;&gt;&gt; data = {\"ts\": datetime(2024, 1, 15, 14, 30)}\n&gt;&gt;&gt; json.dumps(data, default=datetime_encoder)\n'{\"ts\": \"2024-01-15T14:30:00\"}'\n</code></pre>"},{"location":"api/utils/#reaper.utils.encoding.decode_alphanumeric_id","title":"reaper.utils.encoding.decode_alphanumeric_id","text":"<pre><code>decode_alphanumeric_id(b64_id: str) -&gt; str | None\n</code></pre> <p>Decodifica un <code>feedback_id</code> de Facebook en base64 para obtener el ID num\u00e9rico.</p> <p>Facebook codifica algunos IDs de feedback y comentarios en base64 con el formato <code>\"feedback:&lt;post_id&gt;\"</code> o <code>\"comment:&lt;comment_id&gt;\"</code>. Este m\u00e9todo extrae la parte num\u00e9rica tras los dos puntos.</p> <p>Parameters:</p> Name Type Description Default <code>b64_id</code> <code>str</code> <p>ID alfanum\u00e9rico en base64 tal como aparece en el HTML o tr\u00e1fico     GraphQL (ej. <code>\"ZmVlZGJhY2s6MTIzNDU2Nzg5MDEyMzQ1\"</code>)</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>El ID num\u00e9rico como string si la decodificaci\u00f3n es exitosa, o <code>None</code></p> <code>str | None</code> <p>si el formato es inv\u00e1lido, la entrada est\u00e1 vac\u00eda o la decodificaci\u00f3n falla.</p> <p>Examples:</p> <pre><code>&gt;&gt;&gt; decode_alphanumeric_id(\"ZmVlZGJhY2s6MTIzNDU2\")\n'123456'\n&gt;&gt;&gt; decode_alphanumeric_id(\"\")\nNone\n&gt;&gt;&gt; decode_alphanumeric_id(\"no_es_base64!!!\")\nNone\n</code></pre>"},{"location":"technical/architecture/","title":"Arquitectura","text":""},{"location":"technical/architecture/#capas-del-sistema","title":"Capas del sistema","text":"<p>Reaper est\u00e1 organizado en capas con responsabilidad \u00fanica. Los datos solo fluyen hacia abajo: ninguna capa conoce los detalles de implementaci\u00f3n de la que est\u00e1 por encima.</p> <pre><code>\u250c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2510\n\u2502  API p\u00fablica  reaper/__init__.py                                \u2502\n\u2502  scrape()  |  Reaper  |  AccountManager  |  ScraperConfig      \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  Orquestaci\u00f3n  core.py                                          \u2502\n\u2502  Reaper.scrape() \u2192 _dispatch() \u2192 Scraper.run()                 \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u252c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  Scrapers  scrapers/         \u2502  Auth  auth/                    \u2502\n\u2502  FacebookScraper             \u2502  AccountManager                 \u2502\n\u2502  InstagramScraper            \u2502  AccountRotator                 \u2502\n\u2502  BaseScraper                 \u2502  LocalFileStorage               \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2534\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  Red  network/                                                  \u2502\n\u2502  ContentFetcher  |  NetworkInterceptor                          \u2502\n\u2502  (Playwright \u2192 HTML + tr\u00e1fico GraphQL/API)                      \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  Parsers  parsers/                                              \u2502\n\u2502  PostParser | ReelParser | VideoParser | GroupParser            \u2502\n\u2502  ProfileParser | PhotoParser | IgPostParser | IgReelParser      \u2502\n\u251c\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2524\n\u2502  Utils  utils/                                                  \u2502\n\u2502  fb_url_classifier | fb_auth_detector | logger                  \u2502\n\u2502  metrics | encoding | http | media | platforms                  \u2502\n\u2514\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2518\n</code></pre>"},{"location":"technical/architecture/#flujo-de-datos-completo","title":"Flujo de datos completo","text":"<pre><code>scrape(url, account_manager=manager)\n    \u2502\n    \u25bc\nReaper(account_manager)\n    \u2502  crea ScraperConfig con account_manager propagado\n    \u25bc\n_dispatch(config, platform)\n    \u2502  selecciona FacebookScraper o InstagramScraper\n    \u25bc\nFacebookScraper.run()\n    \u2502\n    \u251c\u2500\u25ba BaseScraper._fetch_with_account(\"facebook\")\n    \u2502       \u2502  si hay AccountManager:\n    \u2502       \u251c\u2500\u25ba manager.get_account_for_request()  \u2190 rotador elige cuenta\n    \u2502       \u251c\u2500\u25ba local_storage.load_cookies()        \u2190 lee cookies de disco\n    \u2502       \u251c\u2500\u25ba _fetch(cookies=cookies)             \u2190 Playwright con sesi\u00f3n\n    \u2502       \u2514\u2500\u25ba manager.record_success/failure()   \u2190 actualiza actividad\n    \u2502\n    \u251c\u2500\u25ba requires_auth(html, url)           \u2190 detecta muros de login\n    \u251c\u2500\u25ba get_parser_from_fb_url(url)        \u2190 clasifica el tipo de contenido\n    \u2502\n    \u25bc\nReelParser(html, traffic).parse()\n    \u251c\u2500\u25ba _extract_json_blocks()             \u2190 &lt;script type=\"application/json\"&gt;\n    \u251c\u2500\u25ba _locate_all_stories()              \u2190 DFS buscando nodos Story\n    \u251c\u2500\u25ba _build_reel_dict(story)            \u2190 extrae campos estructurados\n    \u2514\u2500\u25ba _parse_traffic()                   \u2190 enriquece con datos GraphQL\n    \u2502\n    \u25bc\ndict[str, Any]  \u2190 resultado final\n</code></pre>"},{"location":"technical/architecture/#principios-de-diseno","title":"Principios de dise\u00f1o","text":"<p>Compatibilidad hacia atr\u00e1s total. El campo <code>account_manager</code> en <code>ScraperConfig</code> es <code>None</code> por defecto. Cuando es <code>None</code>, el flujo es id\u00e9ntico al de la versi\u00f3n 0.1.x sin ninguna penalizaci\u00f3n de rendimiento.</p> <p>Parsers sin efectos secundarios. Los parsers reciben HTML + tr\u00e1fico y devuelven un dict. No hacen peticiones de red, no escriben en disco, no modifican estado global. Esto permite usarlos offline con artefactos de debug guardados.</p> <p>Extensibilidad expl\u00edcita. <code>BaseAccountStorage</code> es un ABC dise\u00f1ado para ser implementado. Migrar de JSON en disco a PostgreSQL solo requiere implementar cinco m\u00e9todos abstractos.</p> <p>Fetch de segundo nivel sin cambio de cuenta. Los fetches secundarios (merge reel\u2194video, re-fetch de \u00e1lbum) llaman a <code>_fetch()</code> directamente, sin pasar por <code>_fetch_with_account()</code>. Son parte de la misma sesi\u00f3n y no deben contar como peticiones independientes ni cambiar de cuenta.</p>"},{"location":"technical/auth/","title":"Sistema de autenticaci\u00f3n","text":""},{"location":"technical/auth/#estructura-del-modulo","title":"Estructura del m\u00f3dulo","text":"<pre><code>reaper/auth/\n\u251c\u2500\u2500 models.py            Modelos de datos puros (AccountProfile, etc.)\n\u251c\u2500\u2500 rotator.py           Algoritmo de scoring y selecci\u00f3n\n\u251c\u2500\u2500 account_manager.py   Gestor central: coordina todos los subsistemas\n\u251c\u2500\u2500 login.py             Flujo interactivo con Playwright\n\u2514\u2500\u2500 storage/\n    \u251c\u2500\u2500 base.py          ABC: BaseAccountStorage\n    \u2514\u2500\u2500 local.py         Implementaci\u00f3n en JSON: LocalFileStorage\n</code></pre>"},{"location":"technical/auth/#modelos-de-datos","title":"Modelos de datos","text":"<p>Los tres modelos est\u00e1n documentados en detalle en la API Reference.</p> <p><code>AccountStatus</code> \u2014 enum <code>str</code> con seis estados operativos. Los estados <code>SUSPENDED</code> y <code>COOKIE_EXPIRED</code> son los \u00fanicos que excluyen la cuenta del rotador; el resto son seleccionables con distintos grados de penalizaci\u00f3n.</p> Estado Seleccionable Penalizaci\u00f3n en score <code>active</code> S\u00ed Ninguna <code>needs_refresh</code> S\u00ed \u00d70.5 <code>rate_limited</code> S\u00ed \u00d70.2 <code>cookie_expired</code> No \u2014 <code>suspended</code> No \u2014 <code>unknown</code> No (sin cookies) \u2014 <p><code>AccountActivity</code> \u2014 dataclass con contadores de \u00e9xito/fallo, timestamps ISO-8601 del \u00faltimo uso, y una cola FIFO de los \u00faltimos 20 mensajes de error. El campo <code>requests_since_cookie_refresh</code> se resetea a 0 en cada auto-refresh de cookies, por lo que en condiciones normales el sistema nunca llega al umbral.</p> <p><code>AccountProfile</code> \u2014 perfil completo. El campo <code>cookies_path</code> no se persiste en disco; <code>LocalFileStorage</code> lo infiere de la ubicaci\u00f3n del archivo al deserializar, evitando rutas absolutas hardcodeadas.</p>"},{"location":"technical/auth/#algoritmo-de-rotacion","title":"Algoritmo de rotaci\u00f3n","text":"<p>El <code>AccountRotator</code> es stateless: recibe el pool completo en cada llamada y calcula el ranking en el momento, sin estado entre llamadas.</p>"},{"location":"technical/auth/#formula-de-puntuacion","title":"F\u00f3rmula de puntuaci\u00f3n","text":"<pre><code>score = (success_rate  \u00d7 0.6)\n      + (lru_score     \u00d7 0.3)\n      - (cookie_age    \u00d7 0.1)\n</code></pre> <ul> <li><code>success_rate</code>: <code>successful_requests / total_requests</code>. Cuentas sin historial reciben 1.0.</li> <li><code>lru_score</code>: antig\u00fcedad de uso normalizada 0.0\u20131.0. La cuenta usada hace m\u00e1s tiempo obtiene 1.0.</li> <li><code>cookie_age</code>: <code>requests_since_cookie_refresh / threshold</code>. Con auto-refresh activo   este valor se mantiene cercano a 0 en condiciones normales, haciendo que este componente   tenga impacto m\u00ednimo.</li> </ul> <p>Despu\u00e9s del score bruto se aplican los multiplicadores por estado.</p>"},{"location":"technical/auth/#ejemplo-con-tres-cuentas","title":"Ejemplo con tres cuentas","text":"Cuenta success_rate lru_score cookie_age score bruto \u00d7estado score final cuenta_nueva 1.00 1.00 0.00 0.90 \u00d71.0 0.90 cuenta_activa 0.85 0.40 0.60 0.57 \u00d71.0 0.57 rate_limited 0.70 0.60 0.20 0.58 \u00d70.2 0.12"},{"location":"technical/auth/#gestor-de-cuentas","title":"Gestor de cuentas","text":"<p><code>AccountManager</code> coordina storage, rotador y login. Es el \u00fanico componente que el resto de la librer\u00eda necesita para trabajar con cuentas. Su API completa est\u00e1 en api/auth.md.</p>"},{"location":"technical/auth/#distincion-entre-save-y-update_activity","title":"Distinci\u00f3n entre <code>save()</code> y <code>update_activity()</code>","text":"<p><code>BaseAccountStorage</code> expone estas dos operaciones por separado. <code>update_activity()</code> se llama en cada petici\u00f3n (alta frecuencia) y los backends de BD pueden implementarla como un <code>UPDATE</code> parcial. <code>save()</code> es para cambios de estado completos, que ocurren raramente.</p>"},{"location":"technical/auth/#deteccion-de-contenido-no-disponible","title":"Detecci\u00f3n de contenido no disponible","text":""},{"location":"technical/auth/#el-problema-falso-positivo-en-error_route","title":"El problema: falso positivo en <code>error_route</code>","text":"<p>Cuando Facebook no puede servir un contenido (eliminado, privado, restringido), renderiza una p\u00e1gina de error con <code>\"tracePolicy\":\"comet.error\"</code> en los bloques JSON de bootstrap. Esta se\u00f1al es la misma que emite cuando el usuario necesita autenticarse para ver contenido restringido.</p> <p>Sin distinci\u00f3n entre ambos casos, el scraper reintentaba con cuentas del pool ante cualquier <code>error_route</code> \u2014 un reintento costoso e in\u00fatil cuando el contenido simplemente no existe para nadie.</p>"},{"location":"technical/auth/#senales-especificas-de-content_unavailable","title":"Se\u00f1ales espec\u00edficas de <code>content_unavailable</code>","text":"<p>Facebook incluye en los props del <code>rootView</code> un mensaje de error espec\u00edfico que solo aparece en p\u00e1ginas de contenido no disponible, no en muros de login:</p> <pre><code>{\n  \"rootView\": {\n    \"props\": {\n      \"title\": \"This content isn't available right now\",\n      \"body\": \"When this happens, it's usually because the owner only shared it with\\n        a small group of people, changed who can see it or it's been deleted.\",\n      \"privacy\": true\n    }\n  }\n}\n</code></pre> <p>El muro de login genuino nunca incluye este <code>title</code> \u2014 muestra el formulario de credenciales directamente o redirige a <code>/login</code>.</p>"},{"location":"technical/auth/#implementacion-capa-1b-en-requires_auth","title":"Implementaci\u00f3n: capa 1b en <code>requires_auth()</code>","text":"<p>La detecci\u00f3n se inserta entre la comprobaci\u00f3n de contenido presente (capa 1) y la comprobaci\u00f3n de URL redirigida (capa 2):</p> <pre><code>requires_auth(html, url)\n    \u2502\n    \u251c\u2500 Capa 1:  \u00bftracePolicy de contenido real?      \u2192 False (no bloqueado)\n    \u2502\n    \u251c\u2500 Capa 1b: \u00bft\u00edtulo/cuerpo de error espec\u00edfico?  \u2192 False, auth_type=\"content_unavailable\"\n    \u2502               (contenido eliminado/restringido)\n    \u2502\n    \u251c\u2500 Capa 2:  \u00bfURL redirigida a /login?            \u2192 True, auth_type=\"login_redirect\"\n    \u2502\n    \u251c\u2500 Capa 3:  \u00bftracePolicy=comet.error / CometErrorRoute?  \u2192 True, auth_type=\"error_route\"\n    \u2502\n    \u2514\u2500 Capa 4:  \u00bfCometErrorRoot + privacy=true?      \u2192 True, auth_type=\"privacy_wall\"\n</code></pre> <p><code>requires_auth=False</code> para <code>content_unavailable</code> es intencionado: el contenido existe pero no es accesible. No es un muro que se pueda superar autentic\u00e1ndose.</p>"},{"location":"technical/auth/#comportamiento-en-facebookscraperrun","title":"Comportamiento en <code>FacebookScraper.run()</code>","text":"<pre><code>auth = requires_auth(fetch_result.html_content, final_url)\n\nif auth.auth_type == \"content_unavailable\":\n    # Retorno inmediato \u2014 sin tocar el pool de cuentas\n    return self._content_unavailable_result(final_url)\n\nif auth.requires_auth:\n    # Flujo de reintento normal (autenticaci\u00f3n/cookies)\n    ...\n</code></pre>"},{"location":"technical/auth/#nuevo-status-en-el-resultado","title":"Nuevo <code>status</code> en el resultado","text":"<pre><code>{\n    \"platform\":           \"facebook\",\n    \"status\":             \"content_unavailable\",   # \u2190 distinguible de \"error\"\n    \"error\":              \"Content not available \u2014 deleted or restricted\",\n    \"raw_data_available\": False,\n    \"final_url\":          \"https://www.facebook.com/story.php?...\",\n    \"scraped_at\":         datetime(...),\n}\n</code></pre> <code>status</code> Significado <code>\"ok\"</code> Extracci\u00f3n exitosa con datos <code>\"error\"</code> Fallo t\u00e9cnico o muro de auth no recuperable <code>\"content_unavailable\"</code> Contenido eliminado o inaccesible para todos"},{"location":"technical/auth/#refresco-automatico-de-cookies","title":"Refresco autom\u00e1tico de cookies","text":""},{"location":"technical/auth/#como-funciona","title":"C\u00f3mo funciona","text":"<p>Despu\u00e9s de cada navegaci\u00f3n autenticada exitosa, el servidor de Facebook o Instagram emite cabeceras <code>Set-Cookie</code> que actualizan los tokens de seguridad de corta vida y extienden el TTL de las cookies de sesi\u00f3n. Este comportamiento es id\u00e9ntico al de cualquier navegador de usuario real.</p> <p>Playwright acumula estos cambios en el contexto durante la sesi\u00f3n. Tras finalizar <code>page.goto()</code>, <code>context.cookies()</code> ya devuelve las versiones actualizadas que el servidor emiti\u00f3. <code>ContentFetcher.fetch()</code> lee este estado post-navegaci\u00f3n y lo expone en <code>FetchResult.updated_cookies</code>.</p>"},{"location":"technical/auth/#flujo-de-auto-refresh-en-basescraper_fetch_with_account","title":"Flujo de auto-refresh en <code>BaseScraper._fetch_with_account()</code>","text":"<pre><code>fetch con cookies_originales\n    \u2192 Playwright navega la URL\n    \u2192 servidor responde con Set-Cookie actualizados\n    \u2192 context.cookies() \u2192 updated_cookies\n\n_cookies_differ(cookies_originales, updated_cookies)?\n    \u251c\u2500 NO \u2192 nada que hacer, las cookies no cambiaron\n    \u2514\u2500 S\u00cd \u2192 manager.import_cookies(account_id, updated_cookies)\n                \u251c\u2500 sobreescribe cookies.json en disco\n                \u251c\u2500 status \u2192 ACTIVE (si estaba NEEDS_REFRESH)\n                \u2514\u2500 requests_since_cookie_refresh \u2192 0\n</code></pre>"},{"location":"technical/auth/#_cookies_differ-que-se-compara","title":"<code>_cookies_differ()</code> \u2014 qu\u00e9 se compara","text":"<p>Solo se comparan los campos que afectan a la validez de la sesi\u00f3n:</p> <pre><code>def _cookies_differ(original, updated) -&gt; bool:\n    original_map = {c[\"name\"]: (c.get(\"value\"), c.get(\"expires\")) for c in original}\n    updated_map  = {c[\"name\"]: (c.get(\"value\"), c.get(\"expires\")) for c in updated}\n    return original_map != updated_map\n</code></pre> <p>Se ignoran <code>httpOnly</code>, <code>sameSite</code>, <code>path</code> y otros campos de metadatos que no afectan a la autenticaci\u00f3n. Dos casos considerados como cambio:</p> <ul> <li>Token rotado: el <code>value</code> de una cookie cambi\u00f3 (e.g. <code>xs</code> de Facebook   que rota frecuentemente).</li> <li>TTL extendido: el <code>expires</code> aument\u00f3, lo que extiende la vida de la sesi\u00f3n.</li> </ul>"},{"location":"technical/auth/#rol-del-cookie_refresh_threshold-con-auto-refresh-activo","title":"Rol del <code>cookie_refresh_threshold</code> con auto-refresh activo","text":"<p>Con el auto-refresh funcionando correctamente, <code>requests_since_cookie_refresh</code> se resetea a 0 despu\u00e9s de cada petici\u00f3n exitosa donde el servidor devuelva cookies actualizadas. El contador nunca alcanza el threshold en condiciones normales.</p> <p>El threshold act\u00faa como safety net para casos degradados:</p> Escenario Comportamiento Servidor devuelve cookies actualizadas Auto-refresh \u2192 contador a 0, threshold nunca se alcanza Servidor no devuelve <code>Set-Cookie</code> (raro) Contador sube, threshold emite WARNING tras N peticiones <code>context.cookies()</code> falla (error de red) Se loggea como DEBUG, auto-refresh omitido, contador sube Plataforma invalida la sesi\u00f3n remotamente Cookies dejan de funcionar \u2192 <code>COOKIE_EXPIRED</code> o fallos \u2192 login manual"},{"location":"technical/auth/#flujo-de-login-interactivo","title":"Flujo de login interactivo","text":""},{"location":"technical/auth/#por-que-es-manual","title":"Por qu\u00e9 es manual","text":"<p>Facebook e Instagram detectan con alta precisi\u00f3n los intentos de login automatizado. El enfoque manual garantiza que las verificaciones 2FA, CAPTCHAs y comprobaciones de dispositivo las completa un humano usando el navegador de forma normal.</p>"},{"location":"technical/auth/#proceso-tecnico","title":"Proceso t\u00e9cnico","text":"<ol> <li><code>async_playwright()</code> lanza Firefox con <code>headless=False</code>.</li> <li>Se crea un contexto est\u00e1ndar (viewport 1280\u00d7800, locale <code>en-US</code>).</li> <li><code>page.goto(LOGIN_URL)</code> navega a la p\u00e1gina de login.</li> <li><code>_wait_for_login_success()</code> sondea <code>page.url</code> cada segundo buscando los patrones de URL que indican feed post-login.</li> <li>Al detectar el login, <code>context.cookies()</code> extrae todas las cookies de sesi\u00f3n.</li> <li>Las cookies se pasan a <code>AccountManager.import_cookies()</code> o <code>add_account()</code>.</li> <li>El navegador se cierra con <code>browser.close()</code>.</li> </ol>"},{"location":"technical/auth/#patrones-de-url-detectados","title":"Patrones de URL detectados","text":"<pre><code>SUCCESS_URL_PATTERNS = {\n    \"facebook\": [\n        \"facebook.com/?\", \"facebook.com/home\",\n        \"facebook.com/me\", \"facebook.com/feed\", \"facebook.com/?sk=h_chr\",\n    ],\n    \"instagram\": [\n        \"instagram.com/?\",\n        \"instagram.com/accounts/onetap\",\n        \"instagram.com/accounts/login/two_factor\",\n    ],\n}\n</code></pre>"},{"location":"technical/auth/#inyeccion-de-cookies-en-playwright","title":"Inyecci\u00f3n de cookies en Playwright","text":"<p>Las cookies capturadas tienen el formato exacto que acepta <code>context.add_cookies()</code> en <code>ContentFetcher.fetch()</code>. La inyecci\u00f3n ocurre antes de <code>page.goto()</code>:</p> <pre><code>if cookies:\n    await context.add_cookies(cookies)\n    # Scope: contexto (no p\u00e1gina) \u2192 aplica a todos los dominios\n    # y persiste entre navegaciones dentro de la misma sesi\u00f3n.\n</code></pre>"},{"location":"technical/auth/#sistema-de-almacenamiento","title":"Sistema de almacenamiento","text":""},{"location":"technical/auth/#estructura-en-disco","title":"Estructura en disco","text":"<pre><code>data/accounts/\n\u251c\u2500\u2500 facebook/\n\u2502   \u2514\u2500\u2500 {account_id}/\n\u2502       \u251c\u2500\u2500 account.json    \u2190 perfil (sin cookies_path)\n\u2502       \u2514\u2500\u2500 cookies.json    \u2190 cookies en formato Playwright\n\u2514\u2500\u2500 instagram/\n    \u2514\u2500\u2500 {account_id}/\n        \u251c\u2500\u2500 account.json\n        \u2514\u2500\u2500 cookies.json\n</code></pre> <p>El campo <code>cookies_path</code> no se persiste en <code>account.json</code>. <code>LocalFileStorage._load_from_file()</code> lo infiere comprobando si existe <code>{directorio}/cookies.json</code> en el momento de deserializar.</p>"},{"location":"technical/auth/#migracion-a-base-de-datos","title":"Migraci\u00f3n a base de datos","text":"<p>Implementa los cinco m\u00e9todos abstractos de <code>BaseAccountStorage</code> (documentados en api/auth.md):</p> <pre><code>from reaper.auth.storage.base import BaseAccountStorage, StorageError\nfrom reaper.auth.models import AccountActivity, AccountProfile\n\nclass PostgresAccountStorage(BaseAccountStorage):\n\n    async def save(self, account: AccountProfile) -&gt; None:\n        # upsert \u2014 INSERT ... ON CONFLICT DO UPDATE\n        ...\n\n    async def load(self, account_id: str) -&gt; AccountProfile | None:\n        # SELECT WHERE account_id = ?\n        ...\n\n    async def delete(self, account_id: str) -&gt; bool:\n        # DELETE WHERE account_id = ?\n        ...\n\n    async def list_all(self, platform: str | None = None) -&gt; list[AccountProfile]:\n        # SELECT con WHERE platform opcional\n        ...\n\n    async def update_activity(self, account_id: str, activity: AccountActivity) -&gt; bool:\n        # UPDATE parcial \u2014 solo el campo activity\n        ...\n\nmanager = AccountManager(\n    storage=PostgresAccountStorage(session),\n    accounts_dir=\"data/accounts\",   # sigue necesario para cookies en disco\n)\n</code></pre> <p>Cookies siempre en disco</p> <p>Independientemente del backend de perfiles, los archivos de cookies siempre se almacenan en disco local. <code>AccountManager</code> instancia siempre un <code>LocalFileStorage</code> interno para la gesti\u00f3n f\u00edsica de cookies.</p>"},{"location":"technical/auth/#serializacion-de-modelos","title":"Serializaci\u00f3n de modelos","text":"<p>Todos los modelos son 100% serializables sin <code>default=str</code>:</p> <pre><code># Serializar\ndata = profile.to_dict()        # dict con solo tipos primitivos\njson.dumps(data)                # sin kwargs adicionales\n\n# Deserializar \u2014 tolerante a versiones anteriores del schema\nprofile = AccountProfile.from_dict(data)\nactivity = AccountActivity.from_dict(data[\"activity\"])\n</code></pre>"},{"location":"technical/extending/","title":"Extender Reaper","text":""},{"location":"technical/extending/#anadir-un-nuevo-parser-de-facebook","title":"A\u00f1adir un nuevo parser de Facebook","text":""},{"location":"technical/extending/#1-crear-el-archivo-del-parser","title":"1. Crear el archivo del parser","text":"<pre><code># src/reaper/parsers/facebook/event_parser.py\nfrom reaper.parsers.facebook.facebook_parser import FacebookContentParser\nfrom reaper.utils.logger import get_logger\nfrom typing import Any\nimport traceback\n\nlogger = get_logger(__name__)\n\n\nclass EventParser(FacebookContentParser):\n    \"\"\"Parser para eventos de Facebook (/events/&lt;id&gt;).\"\"\"\n\n    def __init__(self, html_content, final_url, original_url,\n                 traffic=None, debug=False):\n        super().__init__(html_content, final_url, original_url, traffic, debug)\n        self.result.update({\"__typename\": \"facebook_event\"})\n\n    def parse(self) -&gt; dict[str, Any]:\n        logger.info(\"Extrayendo EVENTO | url=%s\", self.final_url)\n        self._blocks = self._extract_json_blocks()\n\n        if not self._locate_event_node():\n            self.result[\"error\"] = \"Nodo de evento no encontrado\"\n            return self.result\n\n        self.result[\"raw_data_available\"] = True\n\n        try:\n            self._extract_event_info()\n            self._parse_facebook_traffic([\"EventQuery\"])\n        except Exception as exc:\n            self.result[\"error\"] = str(exc)\n            if self.debug:\n                traceback.print_exc()\n\n        return self.result\n\n    def _locate_event_node(self) -&gt; bool:\n        for block in self._blocks:\n            node = self._recursive_search(\n                block,\n                condition=lambda n: n.get(\"__typename\") == \"Event\" and \"name\" in n,\n            )\n            if node:\n                self._event_node = node\n                return True\n        return False\n\n    def _extract_event_info(self) -&gt; None:\n        node = self._event_node\n        self.result[\"id\"]        = node.get(\"id\", \"\")\n        self.result[\"name\"]      = node.get(\"name\", \"\")\n        self.result[\"start_at\"]  = self._parse_timestamp(node.get(\"start_timestamp\"))\n        self.result[\"end_at\"]    = self._parse_timestamp(node.get(\"end_timestamp\"))\n        self.result[\"cover_url\"] = self._safe_get(node, \"cover_media\", \"photo\",\n                                                   \"image\", \"uri\")\n</code></pre>"},{"location":"technical/extending/#2-registrar-en-parsers__init__py","title":"2. Registrar en <code>parsers/__init__.py</code>","text":"<pre><code>from reaper.parsers.facebook.event_parser import EventParser\n__all__ = [..., \"EventParser\"]\n</code></pre>"},{"location":"technical/extending/#3-anadir-al-mapa-de-parsers-en-scrapersfacebookpy","title":"3. A\u00f1adir al mapa de parsers en <code>scrapers/facebook.py</code>","text":"<pre><code>_PRIMARY_PARSER_MAP: dict[str, type] = {\n    ...,\n    \"EventParser\": EventParser,\n}\n</code></pre>"},{"location":"technical/extending/#4-anadir-la-regla-de-url-en-utilsfb_url_classifierpy","title":"4. A\u00f1adir la regla de URL en <code>utils/fb_url_classifier.py</code>","text":"<pre><code># Dentro de get_parser_from_fb_url(), antes de la regla del perfil vanity:\nif re.match(r\"^/events/\\d+\", path, re.I):\n    return \"EventParser\"\n</code></pre>"},{"location":"technical/extending/#anadir-una-nueva-plataforma","title":"A\u00f1adir una nueva plataforma","text":""},{"location":"technical/extending/#1-registrar-el-dominio-en-utilsplatformspy","title":"1. Registrar el dominio en <code>utils/platforms.py</code>","text":"<pre><code>PLATFORM_DOMAINS: dict[str, tuple[str, ...]] = {\n    \"facebook\":  (\"facebook.com\", \"fb.com\", \"fb.watch\"),\n    \"instagram\": (\"instagram.com\", \"instagr.am\"),\n    \"tiktok\":    (\"tiktok.com\", \"www.tiktok.com\"),   # nuevo\n}\n</code></pre>"},{"location":"technical/extending/#2-crear-el-scraper-en-scraperstiktokpy","title":"2. Crear el scraper en <code>scrapers/tiktok.py</code>","text":"<pre><code>from datetime import datetime\nfrom typing import Any\nfrom reaper.network.content_fetcher import ContentFetcher\nfrom reaper.scrapers.base import BaseScraper\nfrom reaper.utils.logger import get_logger\n\nlogger = get_logger(__name__)\n\n\nclass TiktokScraper(BaseScraper):\n\n    async def run(self) -&gt; dict[str, Any]:\n        fetch_result = await self._fetch_with_account(\"tiktok\")\n        if not fetch_result.success:\n            return self._error_result(fetch_result.error or \"Fetch fallido\")\n        # ... l\u00f3gica de parseo\n        return self._base_result(\"tiktok\")\n\n    async def _fetch(self, override_url=None, cookies=None):\n        fetcher = ContentFetcher(\n            url=override_url or self.config.url,\n            headless=self.config.headless,\n            debug=self.config.debug,\n        )\n        return await fetcher.fetch(\n            screenshot=self.config.screenshot,\n            auto_scroll=self.config.auto_scroll,\n            proxy_server=self.config.proxy_server,\n            proxy_username=self.config.proxy_username,\n            proxy_password=self.config.proxy_password,\n            cookies=cookies,\n        )\n\n    def _error_result(self, error_msg: str) -&gt; dict[str, Any]:\n        return {\n            **self._base_result(\"tiktok\"),\n            \"final_url\": self.config.url,\n            \"scraped_at\": datetime.now(),\n            \"error\": error_msg,\n            \"raw_data_available\": False,\n            \"status\": \"error\",\n        }\n</code></pre>"},{"location":"technical/extending/#3-registrar-en-corepy","title":"3. Registrar en <code>core.py</code>","text":"<pre><code>match platform:\n    case \"facebook\":  ...\n    case \"instagram\": ...\n    case \"tiktok\":\n        from reaper.scrapers.tiktok import TiktokScraper\n        scraper = TiktokScraper(config)\n    case _:\n        raise UnsupportedPlatformError(...)\n</code></pre>"},{"location":"technical/extending/#implementar-un-backend-de-almacenamiento-propio","title":"Implementar un backend de almacenamiento propio","text":"<p>Ver Migraci\u00f3n a base de datos en la documentaci\u00f3n t\u00e9cnica del sistema de autenticaci\u00f3n.</p>"},{"location":"technical/extending/#anadir-soporte-de-cuentas-para-una-nueva-plataforma","title":"A\u00f1adir soporte de cuentas para una nueva plataforma","text":"<p>Cuando se a\u00f1ade una nueva plataforma que tambi\u00e9n requiere autenticaci\u00f3n, basta con a\u00f1adir su nombre a <code>SUPPORTED_PLATFORMS</code> en <code>auth/models.py</code>:</p> <pre><code>SUPPORTED_PLATFORMS: frozenset[str] = frozenset({\n    \"facebook\",\n    \"instagram\",\n    \"tiktok\",     # nuevo\n})\n</code></pre> <p>Y llamar a <code>_fetch_with_account(\"tiktok\")</code> en el nuevo scraper. El <code>AccountManager</code> y el rotador funcionan con cualquier nombre de plataforma que est\u00e9 en ese conjunto.</p>"},{"location":"technical/parsers/","title":"Parsers","text":""},{"location":"technical/parsers/#jerarquia-de-herencia","title":"Jerarqu\u00eda de herencia","text":"<pre><code>BaseParser\n\u2514\u2500\u2500 FacebookContentParser\n    \u251c\u2500\u2500 PostParser\n    \u251c\u2500\u2500 ReelParser\n    \u251c\u2500\u2500 VideoParser\n    \u251c\u2500\u2500 GroupParser\n    \u251c\u2500\u2500 ProfileParser\n    \u2514\u2500\u2500 PhotoParser\nIgPostParser   (hereda de BaseParser)\nIgReelParser   (hereda de BaseParser)\n</code></pre>"},{"location":"technical/parsers/#baseparser","title":"<code>BaseParser</code>","text":""},{"location":"technical/parsers/#reaper.parsers.base_parser.BaseParser","title":"reaper.parsers.base_parser.BaseParser","text":"<pre><code>BaseParser(\n    html_content: str,\n    final_url: str,\n    original_url: str = \"\",\n    traffic: CapturedTraffic | None = None,\n    debug: bool = False,\n    platform: str = \"\",\n)\n</code></pre> <p>               Bases: <code>ABC</code></p> <p>Clase base abstracta para todos los parsers de contenido.</p> <p>Define la interfaz com\u00fan y utilidades plataforma-agn\u00f3sticas. No instanciar directamente; usar siempre una subclase concreta.</p> <p>Attributes:</p> Name Type Description <code>html_content</code> <p>HTML crudo de la p\u00e1gina a parsear.</p> <code>final_url</code> <p>URL final (tras redirecciones) de la p\u00e1gina capturada.</p> <code>original_url</code> <p>URL original solicitada por el usuario.</p> <code>traffic</code> <p>Tr\u00e1fico de red capturado con GraphQL responses.</p> <code>debug</code> <p>Si True, emite logs de diagn\u00f3stico adicionales v\u00eda <code>logging</code>.</p> <code>result</code> <code>dict[str, Any]</code> <p>Esqueleto de resultado garantizado. Siempre contiene: <code>platform</code>, <code>status</code>, <code>error</code>, <code>raw_data_available</code>, <code>scraped_at</code>, <code>final_url</code>, <code>post_url</code>. Las subclases extienden este dict con sus campos espec\u00edficos v\u00eda <code>self.result.update({...})</code> en su propio <code>__init__</code>.</p> <p>Inicializa el parser base y construye el esqueleto de resultado.</p> <p>Parameters:</p> Name Type Description Default <code>html_content</code> <code>str</code> <p>HTML completo renderizado de la p\u00e1gina.</p> required <code>final_url</code> <code>str</code> <p>URL final de la p\u00e1gina (puede diferir de la original).</p> required <code>original_url</code> <code>str</code> <p>URL original solicitada por el usuario. Si est\u00e1 vac\u00eda se usa <code>final_url</code> como fallback.</p> <code>''</code> <code>traffic</code> <code>CapturedTraffic | None</code> <p>Tr\u00e1fico de red capturado durante el fetch. CRUCIAL: Los parsers deben usar esto para obtener datos estructurados de las APIs GraphQL.</p> <code>None</code> <code>debug</code> <code>bool</code> <p>Si True, activa logs de diagn\u00f3stico en nivel DEBUG.</p> <code>False</code> <code>platform</code> <code>str</code> <p>Identificador de plataforma (<code>\"facebook\"</code> o <code>\"instagram\"</code>). Poblado por las subclases concretas.</p> <code>''</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>Si <code>html_content</code> est\u00e1 vac\u00edo.</p>"},{"location":"technical/parsers/#reaper.parsers.base_parser.BaseParser.parse","title":"parse  <code>abstractmethod</code>","text":"<pre><code>parse() -&gt; dict[str, Any]\n</code></pre> <p>Ejecuta el proceso completo de extracci\u00f3n.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Diccionario con todos los datos extra\u00eddos. La estructura</p> <code>dict[str, Any]</code> <p>exacta depende de cada subclase concreta.</p>"},{"location":"technical/parsers/#ciclo-de-vida-de-un-parser","title":"Ciclo de vida de un parser","text":"<p>Todos los parsers concretos siguen el mismo flujo interno en <code>parse()</code>:</p> <pre><code>def parse(self) -&gt; dict[str, Any]:\n    self._blocks = self._extract_json_blocks()  # &lt;script type=\"application/json\"&gt;\n\n    if not self._locate_node():                 # DFS buscando el nodo ra\u00edz\n        self.result[\"error\"] = \"Nodo no encontrado\"\n        return self.result\n\n    self.result[\"raw_data_available\"] = True\n\n    self._extract_basic_info()    # id, timestamp, permalink\n    self._extract_author()        # actor / actors[0]\n    self._extract_content()       # texto, hashtags, menciones\n    self._extract_attachments()   # fotos, v\u00eddeos adjuntos\n    self._extract_feedback()      # reacciones, shares, comentarios\n    self._parse_traffic()         # enriquecimiento desde GraphQL\n\n    return self.result\n</code></pre>"},{"location":"technical/parsers/#estrategias-de-localizacion-por-parser","title":"Estrategias de localizaci\u00f3n por parser","text":"Parser Nodo buscado Condici\u00f3n DFS <code>PostParser</code> Story <code>__isFeedUnit == \"Story\"</code> <code>ReelParser</code> creation_story <code>short_form_video_context</code> o <code>post_id</code> <code>VideoParser</code> Nodo v\u00eddeo <code>__typename == \"Video\"</code> con id num\u00e9rico <code>GroupParser</code> Nodo grupo <code>profile_header_renderer</code> o <code>__bbox.result.data.group</code> <code>ProfileParser</code> Nodo perfil <code>__typename</code> en <code>User/UserProfile/Page</code> <code>PhotoParser</code> currMedia <code>__typename == \"Photo\"</code> + <code>image</code> dict + <code>creation_story</code> <code>IgPostParser</code> Post IG <code>xdt_api__v1__media__shortcode__web_info</code> <code>IgReelParser</code> Reel IG <code>xdt_api__v1__clips__clips_on_logged_out_connection_v2</code>"},{"location":"technical/parsers/#usar-parsers-offline-con-artefactos-de-debug","title":"Usar parsers offline con artefactos de debug","text":"<pre><code>from reaper.network.content_fetcher import load_debug_session\nfrom reaper.parsers import ReelParser\n\nsesion = load_debug_session(\"data/debug_artifacts/sesion_guardada/\")\n\nresult = ReelParser(\n    html_content=sesion.html_content,\n    final_url=sesion.final_url,\n    original_url=sesion.original_url,\n    traffic=sesion.traffic,\n    debug=True,\n).parse()\n</code></pre>"},{"location":"technical/parsers/#anadir-un-parser-nuevo","title":"A\u00f1adir un parser nuevo","text":"<ol> <li>Crear <code>src/reaper/parsers/facebook/event_parser.py</code> heredando de    <code>FacebookContentParser</code>.</li> <li>Registrar en <code>src/reaper/parsers/__init__.py</code>.</li> <li>A\u00f1adir a <code>_PRIMARY_PARSER_MAP</code> en <code>src/reaper/scrapers/facebook.py</code>.</li> <li>A\u00f1adir la regla de URL en <code>src/reaper/utils/fb_url_classifier.py</code>.</li> </ol> <pre><code># event_parser.py \u2014 plantilla m\u00ednima\nfrom reaper.parsers.facebook.facebook_parser import FacebookContentParser\n\nclass EventParser(FacebookContentParser):\n    def __init__(self, html_content, final_url, original_url,\n                 traffic=None, debug=False):\n        super().__init__(html_content, final_url, original_url, traffic, debug)\n        self.result.update({\"__typename\": \"facebook_event\"})\n\n    def parse(self) -&gt; dict:\n        self._blocks = self._extract_json_blocks()\n        if not self._locate_event_node():\n            self.result[\"error\"] = \"Nodo de evento no encontrado\"\n            return self.result\n        self.result[\"raw_data_available\"] = True\n        self._extract_event_info()\n        self._parse_facebook_traffic([\"EventQuery\"])\n        return self.result\n\n    def _locate_event_node(self) -&gt; bool:\n        for block in self._blocks:\n            node = self._recursive_search(\n                block,\n                condition=lambda n: n.get(\"__typename\") == \"Event\" and \"name\" in n,\n            )\n            if node:\n                self._event_node = node\n                return True\n        return False\n\n    def _extract_event_info(self) -&gt; None:\n        node = self._event_node\n        self.result[\"id\"]       = node.get(\"id\", \"\")\n        self.result[\"name\"]     = node.get(\"name\", \"\")\n        self.result[\"start_at\"] = self._parse_timestamp(node.get(\"start_timestamp\"))\n</code></pre>"},{"location":"technical/pipeline/","title":"Pipeline de scraping","text":""},{"location":"technical/pipeline/#seleccion-de-parser-en-facebook","title":"Selecci\u00f3n de parser en Facebook","text":"<pre><code>URL de Facebook\n    \u2502\n    \u25bc\nget_parser_from_fb_url(url)\n    \u2502\n    \u251c\u2500 /reel/              \u2192 ReelParser\n    \u251c\u2500 /videos/ | ?v=      \u2192 VideoParser\n    \u251c\u2500 /photo.php | /photo/\u2192 PhotoParser\n    \u251c\u2500 /groups/*/posts/    \u2192 PostParser\n    \u251c\u2500 /groups/            \u2192 GroupParser\n    \u251c\u2500 /*/posts/           \u2192 PostParser\n    \u251c\u2500 profile.php         \u2192 ProfileParser\n    \u251c\u2500 permalink.php       \u2192 PostParser\n    \u251c\u2500 /share/r/           \u2192 ReelParser\n    \u251c\u2500 /hashtag/           \u2192 PostParser (HashtagParser)\n    \u2514\u2500 /&lt;vanity&gt;           \u2192 ProfileParser\n</code></pre>"},{"location":"technical/pipeline/#logica-de-merge-reelvideo","title":"L\u00f3gica de merge reel\u2194video","text":"<p>Facebook sirve el mismo contenido bajo dos URLs con datos distintos:</p> <ul> <li><code>/reel/&lt;id&gt;</code> \u2192 datos de engagement, texto, hashtags.</li> <li><code>/watch/?v=&lt;id&gt;</code> \u2192 URLs de descarga, m\u00e9tricas t\u00e9cnicas de v\u00eddeo.</li> </ul> <p>El scraper detecta el <code>__typename</code> del resultado primario y realiza un fetch secundario para combinar ambas fuentes:</p> <pre><code>resultado.__typename == \"facebook_video\"\n    \u2192 fetch /reel/&lt;permalink_url&gt;\n    \u2192 ReelParser \u2192 merge(reel, video)\n\nresultado.__typename == \"facebook_reel\"\n    \u2192 fetch /watch/?v=&lt;attachment_id&gt;\n    \u2192 VideoParser \u2192 merge(reel, video)\n</code></pre>"},{"location":"technical/pipeline/#mapas-de-parsers","title":"Mapas de parsers","text":"<pre><code># Parser primario por tipo de URL\n_PRIMARY_PARSER_MAP = {\n    \"PostParser\":    PostParser,\n    \"ReelParser\":    ReelParser,\n    \"VideoParser\":   VideoParser,\n    \"GroupParser\":   GroupParser,\n    \"ProfileParser\": ProfileParser,\n    \"HashtagParser\": PostParser,\n    \"PhotoParser\":   PhotoParser,\n}\n\n# Fallback si el primario no encuentra datos\n_FALLBACK_PARSER_MAP = {\n    \"ReelParser\":    VideoParser,\n    \"ProfileParser\": GroupParser,\n}\n</code></pre>"},{"location":"technical/pipeline/#estrategia-de-instagram","title":"Estrategia de Instagram","text":"<p>Instagram usa dos parsers en secuencia:</p> <pre><code>fetch(url)\n    \u2502\n    \u25bc\nIgPostParser.parse()\n    \u2502  \u00bferror?\n    \u251c\u2500 NO  \u2192 resultado final\n    \u2514\u2500 S\u00cd  \u2193\n           \u2502\n           \u25bc\n    IgReelParser.parse()\n           \u2502\n           \u25bc\n        resultado final\n</code></pre>"},{"location":"technical/pipeline/#inyeccion-de-cookies-en-el-navegador","title":"Inyecci\u00f3n de cookies en el navegador","text":"<p>Cuando hay una cuenta autenticada, las cookies se inyectan en el contexto de Playwright antes de navegar:</p> <pre><code># ContentFetcher.fetch()\nif cookies:\n    await context.add_cookies(cookies)\n    # Scope: contexto (no p\u00e1gina) \u2192 aplica a todos los dominios y\n    # persiste entre navegaciones dentro de la sesma sesi\u00f3n.\n\ninterceptor.attach(page)\nawait page.goto(url)\n</code></pre> <p>La inyecci\u00f3n en el contexto (en lugar de en la p\u00e1gina) garantiza que las cookies est\u00e9n presentes desde la primera petici\u00f3n de red y evita cualquier redirect al muro de login de la plataforma.</p>"},{"location":"technical/pipeline/#deteccion-de-bloqueo-de-acceso","title":"Detecci\u00f3n de bloqueo de acceso","text":"<p><code>requires_auth()</code> eval\u00faa el HTML en cuatro capas ordenadas de mayor a menor prioridad. El resultado determina si el scraper reintenta, retorna error o retorna <code>content_unavailable</code>.</p>"},{"location":"technical/pipeline/#capas-de-deteccion","title":"Capas de detecci\u00f3n","text":"<pre><code>requires_auth(html, final_url)\n    \u2502\n    \u251c\u2500 Capa 1   \u00bftracePolicy de contenido real en el HTML?\n    \u2502               \u2192 AuthResult(requires_auth=False)   [p\u00e1gina normal con popup]\n    \u2502\n    \u251c\u2500 Capa 1b  \u00bfT\u00edtulo/cuerpo de error de contenido no disponible?\n    \u2502               \u2192 AuthResult(requires_auth=False, auth_type=\"content_unavailable\")\n    \u2502               [contenido eliminado \u2014 autenticarse no resuelve]\n    \u2502\n    \u251c\u2500 Capa 2   \u00bfURL final redirigida a /login o /checkpoint?\n    \u2502               \u2192 AuthResult(requires_auth=True, auth_type=\"login_redirect\")\n    \u2502\n    \u251c\u2500 Capa 3   \u00bftracePolicy=\"comet.error\" o canonicalRouteName=CometErrorRoute?\n    \u2502               \u2192 AuthResult(requires_auth=True, auth_type=\"error_route\")\n    \u2502\n    \u2514\u2500 Capa 4   \u00bfCometErrorRoot.react + privacy=true? (ambas necesarias)\n                    \u2192 AuthResult(requires_auth=True, auth_type=\"privacy_wall\")\n</code></pre>"},{"location":"technical/pipeline/#flujo-completo-en-facebookscraperrun","title":"Flujo completo en <code>FacebookScraper.run()</code>","text":"<pre><code>fetch_result\n    \u2502\n    \u25bc\nrequires_auth(html, url)\n    \u2502\n    \u251c\u2500 auth_type == \"content_unavailable\"\n    \u2502       \u2514\u2500 _content_unavailable_result()   \u2192 status=\"content_unavailable\"\n    \u2502          [sin reintento, sin tocar el pool]\n    \u2502\n    \u251c\u2500 requires_auth == True\n    \u2502       \u2514\u2500 _handle_auth_wall(reason, url)\n    \u2502               \u2502\n    \u2502               \u251c\u2500 _active_account_id is None?  (fetch era an\u00f3nimo)\n    \u2502               \u2502       \u251c\u2500 account_manager is None \u2192 error\n    \u2502               \u2502       \u2514\u2500 \u2192 _retry_with_account()\n    \u2502               \u2502\n    \u2502               \u2514\u2500 _active_account_id is not None?  (fetch autenticado)\n    \u2502                       \u251c\u2500 update_status(COOKIE_EXPIRED)\n    \u2502                       \u251c\u2500 _active_account_id = None\n    \u2502                       \u2514\u2500 \u2192 _retry_with_account()\n    \u2502\n    \u2502                   _retry_with_account()\n    \u2502                       \u251c\u2500 fetch_with_account()\n    \u2502                       \u2514\u2500 requires_auth(html_retry)?\n    \u2502                               \u251c\u2500 S\u00cd \u2192 None \u2192 error definitivo\n    \u2502                               \u2514\u2500 NO \u2192 fetch_result \u2192 continuar parseo\n    \u2502\n    \u2514\u2500 requires_auth == False\n            \u2514\u2500 continuar flujo normal (selecci\u00f3n de parser)\n</code></pre>"},{"location":"technical/pipeline/#valores-de-status-en-el-resultado","title":"Valores de <code>status</code> en el resultado","text":"<code>status</code> Causa Reintento <code>\"ok\"</code> Extracci\u00f3n exitosa \u2014 <code>\"content_unavailable\"</code> Contenido eliminado o inaccesible para todos No <code>\"error\"</code> Fallo t\u00e9cnico, muro no recuperable, o doble muro No aplica"},{"location":"technical/results/","title":"Resultados por tipo de contenido","text":"<p>Todos los resultados comparten estos campos garantizados:</p> <pre><code>{\n    \"platform\":           str,        # \"facebook\" | \"instagram\"\n    \"status\":             str,        # \"ok\" | \"error\"\n    \"error\":              str | None,\n    \"raw_data_available\": bool,\n    \"scraped_at\":         datetime,\n    \"final_url\":          str,\n    \"post_url\":           str,\n}\n</code></pre>"},{"location":"technical/results/#facebook-post-regular","title":"Facebook \u2014 Post regular","text":"<pre><code>{\n    \"__typename\":         \"regular_post\",\n    \"id\":                 str,\n    \"posted_at\":          datetime | None,\n    \"permalink_url\":      str,\n    \"is_sponsored\":       bool,\n    \"author\": {\n        \"id\": str, \"name\": str, \"url\": str,\n        \"avatar\": str, \"is_verified\": bool, \"work_info\": str | None,\n    },\n    \"text\":        str,\n    \"hashtags\":    [{\"tag\": str, \"id\": str, \"url\": str, \"mobile_url\": str}],\n    \"mentions\":    [{\"id\": str, \"url\": str}],\n    \"attachments\": [{\"type\": str, \"id\": str, \"url\": str, \"caption\": str | list}],\n    \"reaction_count\": int,\n    \"share_count\":    int,\n    \"comments_count\": int,\n    \"reactions\":  [{\"id\": str, \"type\": str, \"count\": int}],\n    \"comments\":   [\n        {\n            \"id\": str, \"depth\": int, \"text\": str | None,\n            \"created_at\": datetime | None,\n            \"author\": {\"id\": str, \"name\": str, \"profile_url\": str,\n                       \"gender\": str, \"avatar\": str | None},\n            \"replies_count\": int, \"reactions\": list, \"reaction_count\": int,\n        }\n    ],\n    \"group\":         {\"id\": str, \"name\": str, \"url\": str, \"avatar\": str} | None,\n    \"original_post\": dict | None,\n}\n</code></pre>"},{"location":"technical/results/#facebook-reel","title":"Facebook \u2014 Reel","text":"<pre><code>{\n    \"__typename\":    \"facebook_reel\",\n    \"id\":            str,\n    \"posted_at\":     datetime | None,\n    \"permalink_url\": str,\n    \"text\":          str,\n    \"author\":        dict,\n    \"reaction_count\": int, \"comments_count\": int, \"share_count\": int,\n    \"privacy\":       str,\n    \"is_ad\":         bool,\n    \"attachments\": [{\n        \"type\":          \"reel\",\n        \"id\":            str,\n        \"url\":           str | None,\n        \"thumbnail_url\": str | None,\n        \"duration_ms\":   int | None,\n        \"width\":         int | None,\n        \"height\":        int | None,\n        \"play_count\":    int | None,\n        \"caption\":       list,\n    }],\n    \"collaborators\": list,\n    \"reactions\":     list,\n    \"feed\":          list,\n    \"feed_video\":    list,\n}\n</code></pre>"},{"location":"technical/results/#facebook-video-nativo","title":"Facebook \u2014 V\u00eddeo nativo","text":"<pre><code>{\n    \"__typename\":    \"facebook_video\",\n    \"id\":            str,\n    \"title\":         str,\n    \"text\":          str,\n    \"author\":        dict,\n    \"thumbnail_url\": str | None,\n    \"video\": {\n        \"url_sd\":      str | None,\n        \"captions\":    list,\n        \"duration_ms\": int | None,\n        \"width\":       int | None,\n        \"height\":      int | None,\n        \"is_looping\":  bool,\n        \"is_spherical\": bool,\n    },\n    \"reaction_count\": int, \"comments_count\": int,\n    \"share_count\":    int, \"play_count\":     int | None,\n    \"is_live_streaming\": bool,\n    \"feed\": list,\n}\n</code></pre>"},{"location":"technical/results/#facebook-grupo","title":"Facebook \u2014 Grupo","text":"<pre><code>{\n    \"__typename\": \"about_private_group\",\n    \"id\":         str,\n    \"name\":       str,\n    \"url\":        str,\n    \"privacy\": {\n        \"level\": str,             # \"CLOSED\" | \"OPEN\" | \"SECRET\"\n        \"description\": str,\n        \"is_private\": bool,\n    },\n    \"description\":        str,\n    \"total_members\":      int,\n    \"total_members_text\": str,\n    \"posts_last_day\":     int | None,\n    \"admins\": [{\"id\": str, \"name\": str, \"url\": str, \"avatar\": str}],\n    \"content_gated\": bool,\n}\n</code></pre>"},{"location":"technical/results/#facebook-perfil","title":"Facebook \u2014 Perfil","text":"<pre><code>{\n    \"__typename\": \"facebook_user_profile\",\n    \"id\":         str,\n    \"name\":       str,\n    \"username\":   str | None,\n    \"avatar\":     str,\n    \"bio\":        str,\n    \"followers_count\":  int,\n    \"following_count\":  int,\n    \"friends_count\":    int,\n    \"education\":    {\"text\": str, \"name\": str, \"url\": str, \"id\": str},\n    \"current_city\": {\"text\": str, \"name\": str, \"url\": str, \"id\": str},\n    \"contact_info\": {\n        \"email\": str, \"phone\": str,\n        \"websites\": list[str], \"social_accounts\": list[dict],\n    },\n    \"feed\": list,\n}\n</code></pre>"},{"location":"technical/results/#facebook-foto","title":"Facebook \u2014 Foto","text":"<pre><code>{\n    \"__typename\":     \"facebook_photo\",\n    \"id\":             str,\n    \"is_album\":       bool,\n    \"parent_post_url\": str | None,\n    \"author\":         dict,\n    \"attachments\": [{\n        \"type\":                  \"photo\",\n        \"id\":                    str,\n        \"url\":                   str,\n        \"width\":                 int | None,\n        \"height\":                int | None,\n        \"accessibility_caption\": str,\n    }],\n    \"text\":           str,\n    \"reaction_count\": int,\n}\n</code></pre>"},{"location":"technical/results/#instagram-post","title":"Instagram \u2014 Post","text":"<pre><code>{\n    \"platform\":      \"instagram\",\n    \"code\":          str,\n    \"id\":            str,\n    \"permalink_url\": str,\n    \"posted_at\":     datetime | None,\n    \"user\": {\n        \"id\": str, \"username\": str, \"full_name\": str,\n        \"profile_pic_url\": str, \"is_verified\": bool,\n    },\n    \"media_type\":            str,   # \"Photo\" | \"Reel\" | \"carousel\"\n    \"carousel_media_count\":  int | None,\n    \"thumbnail\":             str | None,\n    \"text\":                  str | None,\n    \"like_count\":            int,\n    \"comment_count\":         int,\n    \"image_versions\":        list | None,\n    \"video_versions\":        list | None,\n    \"tagged_users\":          list[dict],\n    \"comments\":              list[dict],\n    \"feed\":                  list[dict],\n}\n</code></pre>"},{"location":"technical/results/#instagram-reel","title":"Instagram \u2014 Reel","text":"<p>Misma estructura que el post de Instagram con un campo adicional:</p> <pre><code>{\n    # ... todos los campos de Instagram Post\n    \"media_repost_count\": int,\n}\n</code></pre>"},{"location":"technical/results/#serializar-el-resultado-a-json","title":"Serializar el resultado a JSON","text":"<pre><code>import json\nfrom reaper import scrape\nimport asyncio\n\nresult = asyncio.run(scrape(url))\n\n# El resultado contiene objetos datetime \u2014 usar default=str\njson_str = json.dumps(result, ensure_ascii=False, indent=2, default=str)\n\n# Guardar en archivo\nwith open(\"resultado.json\", \"w\", encoding=\"utf-8\") as f:\n    json.dump(result, f, ensure_ascii=False, indent=2, default=str)\n</code></pre>"},{"location":"user/accounts/","title":"Cuentas autenticadas","text":"<p>Las cuentas autenticadas permiten a Reaper realizar peticiones usando sesiones de Facebook o Instagram reales. Puedes registrar tantas cuentas como necesites y Reaper las rotar\u00e1 autom\u00e1ticamente eligiendo siempre la m\u00e1s adecuada.</p> <p>Compatibilidad</p> <p>Si no usas cuentas, Reaper funciona exactamente igual que en versiones anteriores. El par\u00e1metro <code>account_manager</code> es siempre opcional.</p>"},{"location":"user/accounts/#configuracion-inicial","title":"Configuraci\u00f3n inicial","text":"<pre><code>from reaper import AccountManager\n\n# Almacena todo en data/accounts/ (se crea autom\u00e1ticamente)\nmanager = AccountManager()\n\n# Directorio personalizado\nmanager = AccountManager(accounts_dir=\"/mis/cuentas\")\n\n# Umbral de refresco: peticiones antes de pedir cookies nuevas\nmanager = AccountManager(cookie_refresh_threshold=100)\n</code></pre>"},{"location":"user/accounts/#anadir-una-cuenta","title":"A\u00f1adir una cuenta","text":"<p>El flujo recomendado es usar el login interactivo. Si ya tienes las cookies exportadas, puedes importarlas directamente:</p> Con login interactivoCon archivo de cookiesCon cookies en memoria <pre><code>import asyncio\nfrom reaper import AccountManager\nfrom reaper.auth.login import run_login_new_account\n\nasync def main():\n    manager = AccountManager()\n    cuenta = await run_login_new_account(\n        manager=manager,\n        platform=\"facebook\",\n        username=\"mi_usuario@gmail.com\",\n    )\n    print(f\"Registrada: {cuenta.account_id}\")\n\nasyncio.run(main())\n</code></pre> <pre><code>cuenta = await manager.add_account(\n    platform=\"facebook\",\n    username=\"mi_usuario\",\n    cookies_file=\"exports/fb_cookies.json\",\n)\n</code></pre> <pre><code>cookies = [\n    {\"name\": \"c_user\", \"value\": \"123456\", \"domain\": \".facebook.com\",\n     \"path\": \"/\", \"expires\": 9999999999.0, \"httpOnly\": False,\n     \"secure\": True, \"sameSite\": \"None\"},\n    # ... resto de cookies\n]\ncuenta = await manager.add_account(\n    platform=\"facebook\",\n    username=\"mi_usuario\",\n    cookies=cookies,\n)\n</code></pre>"},{"location":"user/accounts/#usar-cuentas-en-el-scraping","title":"Usar cuentas en el scraping","text":"<pre><code>import asyncio\nfrom reaper import scrape, Reaper, AccountManager\n\nasync def main():\n    manager = AccountManager()\n\n    # Con la funci\u00f3n scrape()\n    result = await scrape(\n        \"https://www.facebook.com/reel/123456\",\n        account_manager=manager,\n    )\n\n    # Con la clase Reaper (reutiliza el manager entre peticiones)\n    reaper = Reaper(account_manager=manager)\n    result1 = await reaper.scrape(\"https://www.facebook.com/reel/111\")\n    result2 = await reaper.scrape(\"https://www.instagram.com/p/abc/\")\n\nasyncio.run(main())\n</code></pre> <p>Reaper selecciona la cuenta \u00f3ptima autom\u00e1ticamente. No necesitas indicar cu\u00e1l usar en cada petici\u00f3n.</p>"},{"location":"user/accounts/#forzar-una-cuenta-concreta","title":"Forzar una cuenta concreta","text":"<p>Puedes especificar la cuenta a usar en una petici\u00f3n o sesi\u00f3n, identific\u00e1ndola por su <code>account_id</code> (UUID interno), su <code>username</code> o su ID de usuario de la plataforma (<code>c_user</code> en Facebook, <code>ds_user_id</code> en Instagram):</p> <pre><code>import asyncio\nfrom reaper import scrape, Reaper, AccountManager\n\nasync def main():\n    manager = AccountManager()\n\n    # Por username\n    result = await scrape(\n        \"https://www.facebook.com/reel/123456\",\n        account_manager=manager,\n        account=\"mi_usuario@gmail.com\",\n    )\n\n    # Por ID de usuario de Facebook (c_user de las cookies)\n    result = await scrape(\n        \"https://www.facebook.com/reel/123456\",\n        account_manager=manager,\n        account=\"1000123456789\",\n    )\n\n    # Por account_id (UUID interno), reutilizando el manager entre peticiones\n    reaper = Reaper(account_manager=manager)\n    result = await reaper.scrape(\n        \"https://www.instagram.com/p/abc/\",\n        account=\"b3c8f0a2-...-f0a2\",\n    )\n\nasyncio.run(main())\n</code></pre> <p>Comportamiento</p> <ul> <li>Si la cuenta indicada no existe, Reaper avisa por log y contin\u00faa en modo   an\u00f3nimo.</li> <li>Si la cuenta no es seleccionable (suspendida o con cookies expiradas),   se avisa pero se intenta usar igualmente porque la pediste expl\u00edcitamente.</li> <li>Desde la CLI: <code>reaper &lt;url&gt; --account mi_usuario@gmail.com</code>.</li> </ul>"},{"location":"user/accounts/#localizar-una-cuenta-manualmente","title":"Localizar una cuenta manualmente","text":"<pre><code>async def buscar():\n    manager = AccountManager()\n    cuenta = await manager.resolve_account(\"mi_usuario@gmail.com\")\n    cuenta = await manager.resolve_account(\"1000123456789\", platform=\"facebook\")\n    print(cuenta.account_id if cuenta else \"No encontrada\")\n</code></pre>"},{"location":"user/accounts/#gestionar-el-pool-de-cuentas","title":"Gestionar el pool de cuentas","text":"<pre><code>async def gestionar():\n    manager = AccountManager()\n\n    # Ver todas las cuentas\n    todas = await manager.list_accounts()\n    for c in todas:\n        print(f\"[{c.platform}] {c.username} \u2014 {c.status.value}\")\n\n    # Filtrar por plataforma\n    fb = await manager.list_accounts(\"facebook\")\n    ig = await manager.list_accounts(\"instagram\")\n\n    # Ver ranking con scores del rotador\n    ranking = await manager.get_ranked_accounts(\"facebook\")\n    for r in ranking:\n        print(\n            f\"#{r['rank']} {r['username']:25} \"\n            f\"score={r['score']:.3f}  \"\n            f\"\u00e9xitos={r['success_rate']:.0%}  \"\n            f\"peticiones={r['total_requests']}\"\n        )\n\n    # Resumen del estado del pool\n    resumen = await manager.pool_summary()\n    print(f\"Total: {resumen['total']} | Activas: {resumen['active']}\")\n\n    # Eliminar una cuenta (borra perfil y cookies del disco)\n    await manager.remove_account(\"account-id-completo\")\n</code></pre>"},{"location":"user/accounts/#cambiar-el-estado-de-una-cuenta-manualmente","title":"Cambiar el estado de una cuenta manualmente","text":"<pre><code>from reaper import AccountStatus\n\n# Reactivar tras solucionar un problema\nawait manager.update_status(\n    account_id=\"uuid...\",\n    status=AccountStatus.ACTIVE,\n    notes=\"Reactivada el 2026-01-15 tras verificar sesi\u00f3n\",\n)\n\n# Suspender definitivamente\nawait manager.update_status(\n    account_id=\"uuid...\",\n    status=AccountStatus.SUSPENDED,\n    notes=\"Cuenta bloqueada por la plataforma\",\n)\n</code></pre>"},{"location":"user/accounts/#exportar-cookies-de-una-cuenta","title":"Exportar cookies de una cuenta","text":"<pre><code>cookies = await manager.export_cookies(account_id)\n# Retorna list[dict] en formato Playwright, o None si no hay cookies\n</code></pre>"},{"location":"user/actions/","title":"Acciones de escritura (posts, comentarios, likes)","text":"<p>Desde la versi\u00f3n 0.3.0, Reaper no solo lee contenido: tambi\u00e9n puede escribir sobre una sesi\u00f3n de Facebook autenticada. Con <code>ActionManager</code> puedes:</p> <ul> <li>Publicar texto y/o im\u00e1genes en el muro de la cuenta.</li> <li>Publicar directamente dentro de un grupo (texto e im\u00e1genes).</li> <li>Compartir una publicaci\u00f3n existente en un grupo.</li> <li>Responder con texto (comentar) a una publicaci\u00f3n.</li> <li>Reaccionar con \"Me gusta\" a una publicaci\u00f3n.</li> </ul> <p>Cada operaci\u00f3n genera un <code>ActionResult</code> que se guarda en el mismo backend de almacenamiento que eliges para las cuentas (JSON local, PostgreSQL o MongoDB), pero en una tabla/colecci\u00f3n/directorio separado (<code>actions</code> por defecto).</p> <p>Acciones de escritura requieren una cuenta</p> <p>A diferencia del scraping (que tambi\u00e9n funciona en modo an\u00f3nimo), las acciones de escritura siempre requieren un <code>AccountManager</code> con una cuenta de Facebook autenticada y con cookies v\u00e1lidas.</p>"},{"location":"user/actions/#preparacion","title":"Preparaci\u00f3n","text":"<ol> <li>Registra una cuenta con cookies (ver Cuentas autenticadas y    Login interactivo):</li> </ol> <pre><code>import asyncio\nfrom reaper import AccountManager\nfrom reaper.auth.login import run_login_new_account\n\nasync def main():\n    manager = AccountManager()\n    await run_login_new_account(manager, \"facebook\", \"mi_cuenta@gmail.com\")\n    await manager.close()\n\nasyncio.run(main())\n</code></pre> <ol> <li>El almacenamiento de resultados se configura en <code>reaper.toml</code> (igual que    el de cuentas). A\u00f1ade un directorio/tabla/colecci\u00f3n para las acciones:</li> </ol> <pre><code>[storage]\nbackend = \"local\"\naccounts_dir = \"data/accounts\"\nactions_dir  = \"data/actions\"      # opcional; default: data/actions\n</code></pre> <p>Para PostgreSQL / MongoDB el mecanismo es el mismo que en    Almacenamiento, con <code>actions_table</code> / <code>actions_collection</code>:</p> <pre><code>[storage.postgres]\ndsn = \"postgresql://user:pass@localhost:5432/reaper\"\nactions_table = \"actions\"\n\n[storage.mongodb]\nuri = \"mongodb://localhost:27017\"\nactions_collection = \"actions\"\n</code></pre>"},{"location":"user/actions/#publicar-un-post-en-el-muro","title":"Publicar un post en el muro","text":"<p>Al publicar en el muro (sin <code>group</code>), Reaper navega a tu perfil propio. La URL del perfil (no hace falta indicarla) se deriva autom\u00e1ticamente del cookie <code>c_user</code> de la cuenta (<code>profile.php?id={c_user}</code>); si no se puede resolver, cae al home de la cuenta. En la UI de escritorio Reaper abre el campo \"\u00bfQu\u00e9 est\u00e1s pensando?\" (el bot\u00f3n que despliega el composer) antes de escribir.</p>"},{"location":"user/actions/#solo-texto","title":"Solo texto","text":"<pre><code>import asyncio\nfrom reaper import ActionManager, AccountManager\n\nasync def main():\n    manager = ActionManager(account_manager=AccountManager())\n\n    result = await manager.create_post(text=\"Hola desde Reaper \ud83d\udd90\ufe0f\")\n\n    print(f\"status:        {result.status}\")\n    print(f\"post_id:       {result.post_id}\")\n    print(f\"post_url:      {result.post_url}\")\n    print(f\"guardado en:   {manager.storage!r}\")\n\n    await manager.close()\n    return result\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/actions/#con-imagenes-locales","title":"Con im\u00e1genes locales","text":"<p>Se aceptan rutas locales (<code>.jpg</code>, <code>.jpeg</code>, <code>.png</code>, <code>.gif</code>, <code>.webp</code>, <code>.bmp</code>). Se puede pasar una o varias:</p> <pre><code>import asyncio\nfrom reaper import ActionManager, AccountManager\n\nasync def main():\n    manager = ActionManager(account_manager=AccountManager())\n\n    result = await manager.create_post(\n        text=\"Mis fotos del viaje \ud83d\udcf8\",\n        images=[\"fotos/amanecer.jpg\", \"fotos/playa.png\"],\n    )\n    print(result.status, result.post_url)\n\n    await manager.close()\n\nasyncio.run(main())\n</code></pre> <p>Imagen sin texto</p> <p>Si no pasas <code>text</code>, el post solo sube las im\u00e1genes. Al menos uno de los dos (<code>text</code> o <code>images</code>) es obligatorio; si faltan ambos se lanza <code>ActionError</code>.</p>"},{"location":"user/actions/#forzando-una-cuenta-concreta","title":"Forzando una cuenta concreta","text":"<p>Usa <code>account=</code> para elegir una cuenta del pool por <code>account_id</code>, <code>username</code> o ID de usuario de Facebook (<code>c_user</code>). Sin el par\u00e1metro se aplica la rotaci\u00f3n autom\u00e1tica del <code>AccountManager</code>:</p> <pre><code>result = await manager.create_post(\n    text=\"Publicando con una cuenta fija\",\n    account=\"mi_cuenta@gmail.com\",   # o account_id, o c_user num\u00e9rico\n)\n</code></pre>"},{"location":"user/actions/#ajustando-el-timeout-de-confirmacion","title":"Ajustando el timeout de confirmaci\u00f3n","text":"<p>Tras publicar, Reaper espera a que el DOM confirme el post (<code>confirm_timeout</code> en milisegundos, default 15000):</p> <pre><code>result = await manager.create_post(\n    text=\"A ver si sale\",\n    confirm_timeout=30_000,\n)\n</code></pre>"},{"location":"user/actions/#publicar-directamente-en-un-grupo","title":"Publicar directamente en un grupo","text":"<p>Para publicar dentro de un grupo, pasa <code>group</code> como ID num\u00e9rico o como URL de grupo. Reaper navega a la portada del grupo y publica ah\u00ed.</p> <pre><code>import asyncio\nfrom reaper import ActionManager, AccountManager\n\nasync def main():\n    manager = ActionManager(account_manager=AccountManager())\n\n    # Por ID num\u00e9rico, solo texto\n    result = await manager.create_post(\n        text=\"Aviso importante para el grupo \ud83d\udce2\",\n        group=\"123456789\",\n    )\n    print(result.action, result.status)   # \"group_post\", \"ok\"\n\n    # Por URL, con im\u00e1genes\n    result = await manager.create_post(\n        text=\"Fotos del evento del s\u00e1bado\",\n        images=[\"evento/1.jpg\", \"evento/2.jpg\"],\n        group=\"https://www.facebook.com/groups/987654321/\",\n    )\n    await manager.close()\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/actions/#compartir-una-publicacion-en-un-grupo","title":"Compartir una publicaci\u00f3n en un grupo","text":"<p>Comparte cualquier post (por URL) hacia un grupo. El di\u00e1logo de compartir de Facebook localiza el grupo por su nombre visible, as\u00ed que lo recomendable es pasar <code>group_name</code>.</p> <pre><code>import asyncio\nfrom reaper import ActionManager, AccountManager\n\nPOST_URL = \"https://www.facebook.com/story.php?story_fbid=123456&amp;id=789\"\n\nasync def main():\n    manager = ActionManager(account_manager=AccountManager())\n\n    result = await manager.share_post(\n        post_url=POST_URL,\n        group=\"123456789\",\n        group_name=\"Mi Grupo de Test\",   # texto de b\u00fasqueda en el di\u00e1logo\n    )\n    print(result.status, result.post_url)\n\n    await manager.close()\n\nasyncio.run(main())\n</code></pre> <p>Si solo tienes el ID/URL del grupo y no el nombre, se usa ese texto como b\u00fasqueda (puede no encontrar el grupo si el buscador no lo resuelve por ID):</p> <pre><code>result = await manager.share_post(\n    post_url=POST_URL,\n    group=\"123456789\",            # se usa como texto de b\u00fasqueda\n)\n</code></pre>"},{"location":"user/actions/#responder-con-texto-comentar","title":"Responder con texto (comentar)","text":"<p>Comenta cualquier publicaci\u00f3n por URL:</p> <pre><code>import asyncio\nfrom reaper import ActionManager, AccountManager\n\nasync def main():\n    manager = ActionManager(account_manager=AccountManager())\n\n    result = await manager.comment(\n        post_url=\"https://www.facebook.com/story.php?story_fbid=123456&amp;id=789\",\n        text=\"Excelente contenido \ud83d\udc4d\",\n    )\n    print(result.status, result.comment_id)\n\n    await manager.close()\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/actions/#reaccionar-con-me-gusta","title":"Reaccionar con \"Me gusta\"","text":"<pre><code>import asyncio\nfrom reaper import ActionManager, AccountManager\n\nasync def main():\n    manager = ActionManager(account_manager=AccountManager())\n\n    result = await manager.like(\n        post_url=\"https://www.facebook.com/story.php?story_fbid=123456&amp;id=789\",\n    )\n    print(result.status)   # \"ok\" si el DOM confirm\u00f3 la reacci\u00f3n activa\n\n    await manager.close()\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/actions/#configuracion-de-la-sesion","title":"Configuraci\u00f3n de la sesi\u00f3n","text":"<p><code>ActionManager</code> acepta los mismos par\u00e1metros de navegador que el scraping:</p> <pre><code>from reaper import ActionManager, AccountManager\n\nmanager = ActionManager(\n    account_manager=AccountManager(),\n    headless=False,                      # ver la ventana del navegador\n    browser_type=\"firefox\",              # o \"chromium\"\n    proxy_server=\"http://mi.proxy:8080\",\n    proxy_username=\"usuario\",\n    proxy_password=\"clave\",\n)\n</code></pre>"},{"location":"user/actions/#persistencia-de-resultados","title":"Persistencia de resultados","text":"<p>Cada acci\u00f3n se guarda autom\u00e1ticamente. Con el backend <code>local</code>, la estructura es un archivo por cada operaci\u00f3n:</p> <pre><code>data/actions/\n\u251c\u2500\u2500 {action_id}.json   \u2190 un ActionResult por acci\u00f3n\n\u2514\u2500\u2500 ...\n</code></pre> <p>Un registro guardado (<code>ActionResult.to_dict()</code>) tiene este aspecto:</p> <pre><code>{\n  \"action_id\": \"9f4a3b2c\u2026\",\n  \"action\": \"post\",\n  \"platform\": \"facebook\",\n  \"account_id\": \"2e8b4a\u2026\",\n  \"account_username\": \"mi_cuenta@gmail.com\",\n  \"status\": \"ok\",\n  \"post_url\": \"https://www.facebook.com/story.php?story_fbid=888&amp;id=789\",\n  \"post_id\": \"888\",\n  \"comment_id\": null,\n  \"group\": null,\n  \"text\": \"Hola desde Reaper\",\n  \"error\": null,\n  \"executed_at\": \"2026-08-12T17:25:35.377660+00:00\"\n}\n</code></pre> <p>Puedes consultar el historial a trav\u00e9s del <code>storage</code>:</p> <pre><code>from reaper.actions.storage import build_action_storage\n\nasync def consultar():\n    storage = build_action_storage()          # respeta reaper.toml\n\n    historial = await storage.list_all(action=\"post\", limit=10)\n    for registro in historial:\n        print(registro.executed_at, registro.status, registro.post_id)\n\n    conteo = await storage.count(action=\"like\")\n    print(\"total likes:\", conteo)\n\n    await storage.close()\n\n# asyncio.run(consultar())\n</code></pre>"},{"location":"user/actions/#manejo-de-errores","title":"Manejo de errores","text":"<p>Hay dos niveles de fallo:</p> <ol> <li><code>ActionError</code> (excepci\u00f3n) \u2014 problemas antes de ejecutar nada:    faltan <code>AccountManager</code>/cuentas/cookies, el post est\u00e1 vac\u00edo, la URL es    inv\u00e1lida o las im\u00e1genes no existen.</li> </ol> <pre><code>from reaper.actions import ActionError\n\ntry:\n    await manager.create_post(text=\"\", images=[])   # ambos vac\u00edos\nexcept ActionError as exc:\n    print(\"No se pudo: \", exc)\n</code></pre> <ol> <li><code>ActionResult</code> con <code>status=\"error\"</code> \u2014 la acci\u00f3n se ejecut\u00f3 pero el DOM    no confirm\u00f3 el resultado (selector no encontrado, di\u00e1logo no cerrado, etc.).    El registro s\u00ed se persiste, con el motivo en <code>error</code>:</li> </ol> <pre><code>result = await manager.like(post_url=POST_URL)\nif result.status == \"error\":\n    print(\"La plataforma no confirm\u00f3 el like:\", result.error)\n</code></pre>"},{"location":"user/actions/#linea-de-comandos-cli","title":"L\u00ednea de comandos (CLI)","text":"<p>Todos los verbos disponibles:</p> <pre><code># Post de texto en el muro\nreaper action post --text \"Hola desde Reaper\"\n\n# Post con una o varias im\u00e1genes\nreaper action post --text \"Mis fotos\" --images foto1.jpg foto2.png\n\n# Post directo en un grupo (ID o URL)\nreaper action post --text \"Aviso\" --group 123456789\n\n# Compartir un post en un grupo\nreaper action share --post-url \"https://www.facebook.com/story.php\u2026\" \\\n    --group 123456789 --group-name \"Mi Grupo\"\n\n# Comentar una publicaci\u00f3n\nreaper action comment --post-url \"https://www.facebook.com/story.php\u2026\" \\\n    --text \"Excelente contenido\"\n\n# Dar like\nreaper action like --post-url \"https://www.facebook.com/story.php\u2026\"\n\n# Forzar cuenta y usar proxy\nreaper action post --text \"Con cuenta fija\" --account mi_cuenta@mail.com \\\n    --proxy http://proxy:8080 --proxy-user u --proxy-pass p\n\n# Guardar el resultado en un fichero\nreaper action like --post-url \"\u2026\" --output resultado.json\n\n# Ver el navegador en pantalla (depuraci\u00f3n)\nreaper action post --text \"\u2026\" --no-headless --debug\n</code></pre> <p>C\u00f3digos de salida de la CLI:</p> C\u00f3digo Significado <code>0</code> Acci\u00f3n ejecutada y confirmada por la plataforma. <code>1</code> Acci\u00f3n ejecutada pero no confirmada (<code>status=\"error\"</code>). <code>5</code> Error de entrada/sesi\u00f3n (<code>ActionError</code>)."},{"location":"user/actions/#notas-importantes","title":"Notas importantes","text":"<p>Selectores en constante cambio</p> <p>Facebook modifica su DOM con frecuencia. Los selectores usados por los flujos est\u00e1n centralizados en <code>reaper.actions.facebook.selectors</code>. Si una acci\u00f3n deja de confirmarse, ajusta ah\u00ed los candidatos (no en la l\u00f3gica). Puedes depurar con <code>--debug</code> para inspeccionar el HTML real.</p> <p>Riesgo de casting / bloqueos</p> <p>Las acciones de escritura son mucho m\u00e1s sensibles a la anti-detecci\u00f3n que el scraping: interacciones al muro, grupos y comentarios se monitorizan activamente. Usa las mismas precauciones de Anti-Detecci\u00f3n y respeta los tiempos humanos (<code>human_type</code>) que ya aplica la librer\u00eda.</p> <p>Prueba primero en un grupo privado</p> <p>Antes de publicar en un muro p\u00fablico o un grupo grande, valida el flujo en un grupo propio/privado y con <code>headless=False</code> para observar el resultado.</p>"},{"location":"user/actions/#referencia","title":"Referencia","text":"<ul> <li>API Reference \u2014 reaper.actions</li> <li>Almacenamiento de cuentas</li> <li>Cuentas autenticadas</li> </ul>"},{"location":"user/anonymous/","title":"Scraping sin cuentas","text":"<p>El modo an\u00f3nimo es el comportamiento original de Reaper. No requiere ninguna configuraci\u00f3n adicional.</p>"},{"location":"user/anonymous/#funcion-scrape-uso-minimo","title":"Funci\u00f3n <code>scrape()</code> \u2014 uso m\u00ednimo","text":"<pre><code>import asyncio\nfrom reaper import scrape\n\nresult = asyncio.run(scrape(\"https://www.facebook.com/reel/816043001524221\"))\n\nif result[\"status\"] == \"ok\" and result[\"raw_data_available\"]:\n    print(result[\"author\"][\"name\"])\n    print(result[\"text\"])\n    print(f\"Reacciones: {result['reaction_count']}\")\n</code></pre>"},{"location":"user/anonymous/#clase-reaper-multiples-scrapes","title":"Clase <code>Reaper</code> \u2014 m\u00faltiples scrapes","text":"<pre><code>import asyncio\nfrom reaper import Reaper\n\nasync def main():\n    reaper = Reaper()\n\n    post  = await reaper.scrape(\"https://www.instagram.com/p/abc123/\")\n    reel  = await reaper.scrape(\"https://www.facebook.com/reel/456789\")\n    grupo = await reaper.scrape(\"https://www.facebook.com/groups/123456\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/anonymous/#opciones-disponibles","title":"Opciones disponibles","text":"<pre><code>result = asyncio.run(scrape(\n    url=\"https://www.facebook.com/groups/123456/posts/789012\",\n    headless=True,           # False = ver Firefox mientras trabaja\n    debug=False,             # True = logs detallados + artefactos en disco\n    screenshot=False,        # True = captura PNG del contenedor principal\n    auto_scroll=True,        # False = m\u00e1s r\u00e1pido, menos comentarios cargados\n    infinity_scroll=False,   # True = scroll continuo para feeds muy largos\n    proxy_server=None,       # \"http://ip:puerto\" o \"socks5://ip:puerto\"\n    proxy_username=None,\n    proxy_password=None,\n))\n</code></pre>"},{"location":"user/anonymous/#desde-la-linea-de-comandos","title":"Desde la l\u00ednea de comandos","text":"<pre><code># Extraer un reel de Facebook\nreaper https://www.facebook.com/reel/816043001524221\n\n# Extraer un post de Instagram\nreaper https://www.instagram.com/p/abc123XYZ/\n\n# Guardar resultado como JSON\nreaper https://www.facebook.com/reel/123 --output resultado.json --indent 4\n\n# Ver navegador mientras trabaja (diagn\u00f3stico)\nreaper https://www.facebook.com/reel/123 --no-headless\n\n# Activar modo debug completo\nreaper https://www.facebook.com/reel/123 --debug\n</code></pre>"},{"location":"user/anonymous/#verificar-si-el-scraping-fue-exitoso","title":"Verificar si el scraping fue exitoso","text":"<pre><code>result = asyncio.run(scrape(url))\n\nif result[\"status\"] == \"error\":\n    print(f\"Error: {result['error']}\")\nelif result[\"raw_data_available\"]:\n    print(\"Extracci\u00f3n exitosa\")\nelse:\n    print(\"Sin datos estructurados \u2014 usa --debug para diagnosticar\")\n</code></pre>"},{"location":"user/anonymous/#contenido-soportado","title":"Contenido soportado","text":""},{"location":"user/anonymous/#facebook","title":"Facebook","text":"Tipo URL de ejemplo Post regular <code>facebook.com/*/posts/&lt;id&gt;</code> Post permalink <code>facebook.com/permalink.php?story_fbid=...</code> Reel <code>facebook.com/reel/&lt;id&gt;</code> V\u00eddeo nativo <code>facebook.com/watch?v=&lt;id&gt;</code> Foto <code>facebook.com/photo.php?fbid=&lt;id&gt;</code> Grupo <code>facebook.com/groups/&lt;id&gt;</code> Post en grupo <code>facebook.com/groups/&lt;id&gt;/posts/&lt;id&gt;</code> Perfil <code>facebook.com/&lt;username&gt;</code>"},{"location":"user/anonymous/#instagram","title":"Instagram","text":"Tipo URL de ejemplo Post (foto/v\u00eddeo/carrusel) <code>instagram.com/p/&lt;code&gt;/</code> Reel <code>instagram.com/reel/&lt;code&gt;/</code>"},{"location":"user/debug/","title":"Modo debug","text":"<p>El modo debug activa dos cosas simult\u00e1neamente: logging detallado en la terminal y guardado de artefactos en disco para an\u00e1lisis offline.</p>"},{"location":"user/debug/#activar-debug","title":"Activar debug","text":"PythonTerminal <pre><code>result = asyncio.run(scrape(url, debug=True))\n</code></pre> <pre><code>reaper https://www.facebook.com/reel/123 --debug\n</code></pre>"},{"location":"user/debug/#que-se-guarda-en-disco","title":"Qu\u00e9 se guarda en disco","text":"<pre><code>data/debug_artifacts/\n\u2514\u2500\u2500 www.facebook.com_reel_123_20260101_143022/\n    \u251c\u2500\u2500 meta.json              \u2190 metadatos de la sesi\u00f3n\n    \u251c\u2500\u2500 page.html              \u2190 HTML completo renderizado por Firefox\n    \u251c\u2500\u2500 traffic.json           \u2190 \u00edndice del tr\u00e1fico de red capturado\n    \u251c\u2500\u2500 graphql_requests/      \u2190 peticiones GraphQL salientes\n    \u2502   \u2514\u2500\u2500 000_ReelQuery.json\n    \u2514\u2500\u2500 graphql_responses/     \u2190 respuestas GraphQL con datos estructurados\n        \u251c\u2500\u2500 000_ReelQuery.json\n        \u2514\u2500\u2500 001_CommentsQuery.json\n</code></pre>"},{"location":"user/debug/#parsear-artefactos-sin-abrir-el-navegador","title":"Parsear artefactos sin abrir el navegador","text":"<p>Una vez guardados, puedes re-parsear sin conexi\u00f3n:</p> <pre><code>from reaper.network.content_fetcher import load_debug_session, list_debug_sessions\nfrom reaper.parsers import ReelParser\n\n# Ver sesiones disponibles\nsesiones = list_debug_sessions(\"data/debug_artifacts\")\nfor s in sesiones:\n    print(s[\"fetched_at\"], s[\"original_url\"])\n\n# Cargar y parsear\nsesion = load_debug_session(sesiones[0][\"path\"])\nresult = ReelParser(\n    html_content=sesion.html_content,\n    final_url=sesion.final_url,\n    original_url=sesion.original_url,\n    traffic=sesion.traffic,\n    debug=True,\n).parse()\n</code></pre>"},{"location":"user/debug/#activar-solo-logging-verbose-sin-artefactos","title":"Activar solo logging verbose (sin artefactos)","text":"<pre><code>from reaper.utils.logger import setup_logging\n\nsetup_logging(level=\"DEBUG\")\nresult = await scrape(url)\n</code></pre>"},{"location":"user/debug/#entender-los-logs-en-modo-debug","title":"Entender los logs en modo debug","text":"<pre><code>14:22:31.045 INFO   scrapers.facebook   Iniciando extracci\u00f3n Facebook | url=...\n14:22:31.123 INFO   network.content_fetcher  Navegando a: https://...\n14:22:35.891 INFO   network.content_fetcher  Contenido obtenido. Tr\u00e1fico: GraphQL: 5req/4res\n14:22:35.892 INFO   scrapers.facebook   Parser seleccionado | parser=ReelParser\n14:22:35.910 INFO   scrapers.facebook   Extracci\u00f3n completada | parser=ReelParser\n</code></pre> <p>Con cuentas autenticadas ver\u00e1s adicionalmente:</p> <pre><code>14:22:31.040 INFO   scrapers.base  Fetch autenticado | platform=facebook |\n                    username=mi_usuario | cookies=28 | peticiones_cuenta=12 | score_aprox=91%\n</code></pre>"},{"location":"user/faq/","title":"Preguntas frecuentes","text":""},{"location":"user/faq/#sobre-el-scraping-en-general","title":"Sobre el scraping en general","text":"<p>\u00bfReaper guarda mis credenciales?</p> <p>No. Reaper nunca almacena contrase\u00f1as. Solo guarda las cookies de sesi\u00f3n que el navegador genera cuando t\u00fa inicias sesi\u00f3n manualmente. Las contrase\u00f1as nunca pasan por el c\u00f3digo de Reaper.</p> <p>\u00bfCu\u00e1nto tarda cada extracci\u00f3n?</p> <p>Entre 5 y 30 segundos dependiendo del tipo de contenido y la velocidad de la red. Posts simples con <code>auto_scroll=False</code> tardan ~5-10s. Grupos con infinity scroll pueden tardar m\u00e1s de un minuto.</p> <p>\u00bfPor qu\u00e9 <code>raw_data_available</code> es <code>False</code>?</p> <p>Significa que el parser no encontr\u00f3 datos estructurados. Causas comunes:</p> <ul> <li>La p\u00e1gina requiere login para mostrar el contenido.</li> <li>Facebook/Instagram cambi\u00f3 la estructura de sus datos internos.</li> <li>La URL apunta a contenido eliminado o no disponible.</li> </ul> <p>Usa <code>debug=True</code> para guardar el HTML y diagnosticar qu\u00e9 ocurri\u00f3.</p> <p>\u00bfPuedo extraer v\u00eddeos y descargarlos?</p> <p>Reaper extrae la URL del v\u00eddeo en el campo <code>url</code> de los adjuntos. La descarga del archivo de v\u00eddeo es responsabilidad del consumidor:</p> <pre><code>import httpx\n\nresult = await scrape(url)\nvideo_url = result[\"attachments\"][0][\"url\"]\nvideo_bytes = httpx.get(video_url).content\n</code></pre>"},{"location":"user/faq/#sobre-cuentas-autenticadas","title":"Sobre cuentas autenticadas","text":"<p>\u00bfCu\u00e1ntas cuentas puedo registrar?</p> <p>Sin l\u00edmite t\u00e9cnico. Puedes registrar todas las cuentas que necesites de Facebook y/o Instagram.</p> <p>\u00bfC\u00f3mo decide Reaper qu\u00e9 cuenta usar?</p> <p>Eval\u00faa cada cuenta con un score basado en tasa de \u00e9xito (peso 60%), tiempo desde el \u00faltimo uso (peso 30%) y proximidad al umbral de refresco (peso 10%). La de mayor score se selecciona.</p> <p>\u00bfQu\u00e9 pasa si no tengo cuentas para una plataforma?</p> <p>Reaper usa modo an\u00f3nimo autom\u00e1ticamente para esa plataforma. Tener cuentas de Facebook no impide hacer scraping an\u00f3nimo de Instagram y viceversa.</p> <p>\u00bfEl umbral de refresco desconecta la cuenta?</p> <p>No. Cuando se alcanza el umbral la cuenta pasa a <code>needs_refresh</code> pero sigue siendo usable con prioridad reducida. Solo deja de funcionar si las cookies realmente expiran (<code>cookie_expired</code>), algo que controla la plataforma.</p> <p>\u00bfPuedo usar el login en un servidor sin pantalla?</p> <p>El login interactivo requiere pantalla porque abre Firefox visible. En servidores headless exporta las cookies desde tu m\u00e1quina local e imp\u00f3rtalas:</p> <pre><code># En tu m\u00e1quina local con pantalla\npython -m reaper.auth.login --platform facebook --username usuario\n\n# Copiar el archivo generado al servidor\nscp data/accounts/facebook/UUID/cookies.json servidor:/ruta/\n\n# En el servidor\nmanager.import_cookies_from_file(account_id, \"/ruta/cookies.json\")\n</code></pre>"},{"location":"user/faq/#resolucion-de-errores-comunes","title":"Resoluci\u00f3n de errores comunes","text":"<p><code>ModuleNotFoundError: No module named 'reaper'</code></p> <pre><code>pip install -e .\n</code></pre> <p><code>playwright._impl._errors.Error: Executable doesn't exist</code></p> <pre><code>playwright install firefox\n# En servidores Linux:\nplaywright install-deps firefox\n</code></pre> <p><code>ScrapingError: Page.goto: Timeout</code></p> <p>La p\u00e1gina tard\u00f3 demasiado. Posibles causas: conexi\u00f3n lenta, proxy lento, rate limiting de la plataforma. Soluciones: usar proxy diferente, esperar unos minutos, desactivar scroll (<code>auto_scroll=False</code>).</p> <p><code>get_account_for_request()</code> devuelve <code>None</code> teniendo cuentas</p> <p>Comprueba el estado:</p> <pre><code>for c in await manager.list_accounts(\"facebook\"):\n    print(c.username, c.status.value, c.has_cookies)\n</code></pre> <p>Los estados <code>suspended</code> y <code>cookie_expired</code> bloquean la selecci\u00f3n. Tambi\u00e9n es necesario que el archivo <code>cookies.json</code> exista (<code>has_cookies=True</code>).</p>"},{"location":"user/installation/","title":"Instalaci\u00f3n","text":""},{"location":"user/installation/#requisitos","title":"Requisitos","text":"Componente Versi\u00f3n m\u00ednima Python 3.11 pip 23+ Firefox (via Playwright) instalado autom\u00e1ticamente"},{"location":"user/installation/#instalar-desde-el-repositorio","title":"Instalar desde el repositorio","text":"<pre><code># Clonar el repositorio\ngit clone &lt;repo-url&gt;\ncd reaper\n\n# Crear entorno virtual (recomendado)\npython3.11 -m venv .venv\nsource .venv/bin/activate        # Linux / macOS\n# .venv\\Scripts\\activate         # Windows\n\n# Instalar Reaper\npip install .\n\n# Instalar Firefox para Playwright (solo la primera vez)\nplaywright install firefox\n</code></pre>"},{"location":"user/installation/#instalar-en-modo-desarrollo","title":"Instalar en modo desarrollo","text":"<pre><code>pip install -e \".[dev]\"\nplaywright install firefox\n</code></pre> <p>El modo editable (<code>-e</code>) hace que los cambios en el c\u00f3digo fuente tengan efecto inmediato sin reinstalar.</p>"},{"location":"user/installation/#verificar-la-instalacion","title":"Verificar la instalaci\u00f3n","text":"<pre><code># Debe imprimir la versi\u00f3n\nreaper --version\n\n# Debe imprimir \"OK\"\npython -c \"from reaper import scrape, AccountManager; print('OK')\"\n</code></pre>"},{"location":"user/installation/#servidores-linux-sin-pantalla-headless","title":"Servidores Linux sin pantalla (headless)","text":"<p>En servidores sin interfaz gr\u00e1fica, instala tambi\u00e9n las dependencias del sistema que Firefox requiere:</p> <pre><code>playwright install-deps firefox\n</code></pre> <p>Entornos sin pantalla y login</p> <p>El flujo de login interactivo (<code>run_login_new_account</code>, <code>run_login_refresh</code>) requiere pantalla porque abre Firefox visible para que el usuario complete el login manualmente. En un servidor headless deber\u00e1s exportar las cookies desde tu m\u00e1quina local e importarlas con <code>manager.import_cookies_from_file()</code>.</p>"},{"location":"user/login/","title":"Login interactivo","text":"<p>El login en Reaper es siempre manual e interactivo. Esto es una decisi\u00f3n de dise\u00f1o deliberada: Facebook e Instagram detectan los intentos de login automatizado con alta precisi\u00f3n. El enfoque manual es intr\u00ednsecamente m\u00e1s robusto porque el usuario interact\u00faa con el navegador de forma completamente normal, incluidas verificaciones 2FA, SMS y CAPTCHAs.</p> <p>Reaper solo interviene una vez que el login ya fue completado: captura las cookies de sesi\u00f3n y las persiste para uso futuro.</p>"},{"location":"user/login/#registrar-una-cuenta-nueva","title":"Registrar una cuenta nueva","text":""},{"location":"user/login/#desde-python","title":"Desde Python","text":"<pre><code>import asyncio\nfrom reaper import AccountManager\nfrom reaper.auth.login import run_login_new_account\n\nasync def main():\n    manager = AccountManager()\n\n    cuenta = await run_login_new_account(\n        manager=manager,\n        platform=\"facebook\",          # o \"instagram\"\n        username=\"usuario@gmail.com\", # identificador legible (no se usa para auth)\n        notes=\"Cuenta principal\",     # opcional\n    )\n\n    if cuenta:\n        print(f\"OK \u2014 account_id: {cuenta.account_id}\")\n        print(f\"Estado: {cuenta.status.value}\")    # \"active\"\n    else:\n        print(\"Login no completado\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/login/#desde-la-terminal","title":"Desde la terminal","text":"<pre><code># Modo interactivo (hace preguntas)\npython -m reaper.auth.login\n\n# Especificando plataforma y usuario\npython -m reaper.auth.login --platform facebook --username usuario@gmail.com\n\n# Instagram\npython -m reaper.auth.login --platform instagram --username mi_usuario\n\n# Directorio personalizado\npython -m reaper.auth.login --platform facebook --username usuario \\\n    --accounts-dir /datos/mis_cuentas\n\n# Umbral de refresco personalizado\npython -m reaper.auth.login --platform facebook --username usuario \\\n    --threshold 100\n</code></pre>"},{"location":"user/login/#lo-que-ocurre-paso-a-paso","title":"Lo que ocurre paso a paso","text":"<ol> <li>Se abre Firefox visible en la p\u00e1gina de login de la plataforma.</li> <li>El usuario introduce sus credenciales manualmente.</li> <li>Si hay 2FA, SMS o verificaci\u00f3n de dispositivo, el usuario los completa.</li> <li>El script monitoriza la URL del navegador cada segundo buscando la URL    del feed principal (se\u00f1al de login exitoso).</li> <li>Al detectar el login, extrae las cookies del contexto de Playwright.</li> <li>Las cookies se guardan en disco y se registran en el <code>AccountManager</code>.</li> <li>El navegador se cierra autom\u00e1ticamente.</li> </ol> <p>No cierres el navegador manualmente</p> <p>El script detecta el login por la URL. Si cierras el navegador antes de que lo detecte, el proceso termina sin guardar cookies.</p> <p>Tiempo disponible</p> <p>Tienes 5 minutos para completar el login. M\u00e1s que suficiente para cualquier verificaci\u00f3n de seguridad de la plataforma.</p>"},{"location":"user/login/#refrescar-cookies-de-una-cuenta-existente","title":"Refrescar cookies de una cuenta existente","text":"<p>Las cookies tienen una vida \u00fatil limitada. Cuando una cuenta alcanza el umbral de peticiones, aparece este aviso en los logs:</p> <pre><code>WARNING  Umbral de refresco alcanzado | username=mi_usuario\n         \u2014 Importa cookies frescas con import_cookies('uuid', new_cookies)\n</code></pre> <p>La cuenta pasa a estado <code>needs_refresh</code> y sigue funcionando con prioridad reducida. Para restaurarla completamente:</p>"},{"location":"user/login/#desde-python_1","title":"Desde Python","text":"<pre><code>import asyncio\nfrom reaper import AccountManager\nfrom reaper.auth.login import run_login_refresh\n\nasync def main():\n    manager = AccountManager()\n\n    # Ver qu\u00e9 cuentas necesitan refresco\n    cuentas = await manager.list_accounts()\n    para_refrescar = [\n        c for c in cuentas\n        if c.status.value in (\"needs_refresh\", \"cookie_expired\")\n    ]\n\n    for cuenta in para_refrescar:\n        print(f\"Refrescando: {cuenta.username} ({cuenta.platform})\")\n        ok = await run_login_refresh(manager, cuenta.account_id)\n        print(\"OK\" if ok else \"No completado\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/login/#desde-la-terminal_1","title":"Desde la terminal","text":"<pre><code># Modo interactivo \u2014 muestra las cuentas y pregunta cu\u00e1l refrescar\npython -m reaper.auth.login --refresh\n\n# Especificando el account_id\npython -m reaper.auth.login --refresh \\\n    --account-id 3f2e1a-0000-0000-0000-000000000000\n</code></pre> <p>El refresco sigue el mismo flujo que el login nuevo: abre Firefox, espera a que el usuario inicie sesi\u00f3n, captura las cookies y las importa. Al terminar el estado vuelve a <code>active</code> y el contador de peticiones se resetea a 0.</p>"},{"location":"user/login/#importar-cookies-sin-login-interactivo","title":"Importar cookies sin login interactivo","text":"<p>Si exportaste las cookies desde tu navegador con una extensi\u00f3n como Cookie-Editor, puedes importarlas directamente:</p> <pre><code># Desde archivo JSON externo\nok = await manager.import_cookies_from_file(\n    account_id=\"uuid-de-la-cuenta\",\n    cookies_file=\"cookies_exportadas.json\",\n)\n\n# Desde lista en memoria\nok = await manager.import_cookies(\n    account_id=\"uuid-de-la-cuenta\",\n    cookies=lista_de_cookies,\n)\n</code></pre> <p>El archivo debe ser una lista JSON con objetos en formato Playwright:</p> <pre><code>[\n  {\n    \"name\": \"c_user\",\n    \"value\": \"123456789\",\n    \"domain\": \".facebook.com\",\n    \"path\": \"/\",\n    \"expires\": 1735689600.0,\n    \"httpOnly\": false,\n    \"secure\": true,\n    \"sameSite\": \"None\"\n  }\n]\n</code></pre>"},{"location":"user/pool/","title":"Gesti\u00f3n del pool de cuentas","text":""},{"location":"user/pool/#estados-de-una-cuenta","title":"Estados de una cuenta","text":"Estado Descripci\u00f3n \u00bfSe usa en peticiones? <code>active</code> Operativa, cookies v\u00e1lidas S\u00ed \u2014 prioridad normal <code>needs_refresh</code> Umbral de peticiones alcanzado sin auto-refresh S\u00ed \u2014 prioridad reducida (\u00d70.5) <code>rate_limited</code> Muchos errores consecutivos detectados S\u00ed \u2014 prioridad muy reducida (\u00d70.2) <code>cookie_expired</code> Las cookies ya no son v\u00e1lidas No \u2014 requiere refresco manual <code>suspended</code> Cuenta bloqueada por la plataforma No \u2014 requiere intervenci\u00f3n manual <code>unknown</code> Estado inicial, sin cookies importadas No"},{"location":"user/pool/#como-reaper-elige-la-cuenta-en-cada-peticion","title":"C\u00f3mo Reaper elige la cuenta en cada petici\u00f3n","text":"<p>El <code>AccountRotator</code> punt\u00faa cada cuenta candidata con esta f\u00f3rmula:</p> <pre><code>score = (tasa_de_\u00e9xito \u00d7 0.6)\n      + (antig\u00fcedad_de_uso \u00d7 0.3)\n      - (edad_de_cookies \u00d7 0.1)\n</code></pre> <ul> <li>Tasa de \u00e9xito: porcentaje hist\u00f3rico de peticiones exitosas.</li> <li>Antig\u00fcedad de uso: favorece las cuentas que llevan m\u00e1s tiempo sin usarse.   Las cuentas nunca usadas reciben el score m\u00e1ximo en este componente.</li> <li>Edad de cookies: penaliza levemente las cuentas pr\u00f3ximas al umbral. Con   el auto-refresh activo este componente tiende a 0 en condiciones normales.</li> </ul>"},{"location":"user/pool/#refresco-automatico-de-cookies","title":"Refresco autom\u00e1tico de cookies","text":"<p>Despu\u00e9s de cada petici\u00f3n exitosa, Reaper compara las cookies que inyect\u00f3 con las que el servidor devolvi\u00f3 en la respuesta. Si difieren \u2014 porque el servidor extendi\u00f3 el TTL o rot\u00f3 un token de seguridad \u2014 las persiste en disco autom\u00e1ticamente. No necesitas hacer nada: las sesiones se mantienen activas de forma indefinida mientras la plataforma no las invalide remotamente.</p> <p>El aviso <code>NEEDS_REFRESH</code> en los logs solo aparece si el servidor no devuelve cookies actualizadas durante N peticiones consecutivas (situaci\u00f3n inusual). Cuando aparezca, usa el login interactivo para renovar la sesi\u00f3n.</p>"},{"location":"user/pool/#ciclo-de-vida-de-una-cuenta","title":"Ciclo de vida de una cuenta","text":"<pre><code>add_account() sin cookies        \u2192 UNKNOWN\n    \u2193 import_cookies() / run_login_new_account()\ncookies importadas               \u2192 ACTIVE, contador=0\n\nOperaci\u00f3n normal con auto-refresh:\n    petici\u00f3n exitosa\n        \u2192 servidor devuelve Set-Cookie actualizados\n        \u2192 cookies.json sobrescrito con versi\u00f3n fresca\n        \u2192 contador reseteado a 0\n        \u2192 sesi\u00f3n se mantiene indefinidamente\n\nCaso degradado (servidor no actualiza cookies):\n    petici\u00f3n exitosa sin Set-Cookie\n        \u2192 contador sube\n        \u2192 al llegar al threshold \u2192 NEEDS_REFRESH + WARNING\n        \u2192 run_login_refresh() \u2192 ACTIVE, contador=0\n\nSi muchos fallos seguidos     \u2192 RATE_LIMITED (sigue usable, score \u00d70.2)\n    \u2192 update_status(ACTIVE) o run_login_refresh()\n\nSi bloqueada por plataforma   \u2192 SUSPENDED (excluida del rotador)\n    \u2192 remove_account() si ya no se va a rehabilitar\n</code></pre>"},{"location":"user/pool/#ver-el-estado-del-pool","title":"Ver el estado del pool","text":"<pre><code>import asyncio\nfrom reaper import AccountManager\n\nasync def main():\n    manager = AccountManager()\n\n    resumen = await manager.pool_summary()\n    print(f\"Total     : {resumen['total']}\")\n    print(f\"Activas   : {resumen['active']}\")\n    print(f\"Refresco  : {resumen['needs_refresh']}\")\n    print(f\"Rate-limit: {resumen['rate_limited']}\")\n    print(f\"Suspendidas:{resumen['suspended']}\")\n    print(f\"Expiradas : {resumen['cookie_expired']}\")\n\nasyncio.run(main())\n</code></pre>"},{"location":"user/pool/#ver-el-ranking-de-cuentas","title":"Ver el ranking de cuentas","text":"<pre><code>async def ver_ranking():\n    manager = AccountManager()\n\n    ranking = await manager.get_ranked_accounts(\"facebook\")\n    print(f\"{'#':&gt;3}  {'Usuario':25}  {'Score':&gt;6}  {'\u00c9xitos':&gt;7}  {'Total':&gt;6}  Estado\")\n    print(\"-\" * 70)\n    for r in ranking:\n        print(\n            f\"{r['rank']:&gt;3}. {r['username']:25}  \"\n            f\"{r['score']:&gt;6.3f}  \"\n            f\"{r['success_rate']:&gt;6.0%}  \"\n            f\"{r['total_requests']:&gt;6}  \"\n            f\"{r['status']}\"\n        )\n</code></pre> <p>Ejemplo de salida:</p> <pre><code>  #  Usuario                    Score  \u00c9xitos   Total  Estado\n----------------------------------------------------------------------\n  1. cuenta_nueva               0.900   100%       0  unknown\n  2. cuenta_activa              0.612    95%      47  active\n  3. cuenta_vieja               0.301    72%     124  needs_refresh\n  4. cuenta_problemas           0.115    60%      31  rate_limited\n</code></pre>"},{"location":"user/pool/#ver-errores-recientes-de-una-cuenta","title":"Ver errores recientes de una cuenta","text":"<pre><code>async def ver_errores():\n    manager = AccountManager()\n\n    cuentas = await manager.list_accounts()\n    for cuenta in cuentas:\n        if cuenta.activity.recent_errors:\n            print(f\"\\n{cuenta.username} \u2014 \u00faltimos errores ({len(cuenta.activity.recent_errors)}):\")\n            for error in cuenta.activity.recent_errors[:5]:\n                print(f\"  {error}\")\n</code></pre>"},{"location":"user/proxy/","title":"Uso con proxy","text":"<p>Reaper puede enrutar el tr\u00e1fico del navegador a trav\u00e9s de un servidor proxy.</p>"},{"location":"user/proxy/#desde-python","title":"Desde Python","text":"<pre><code>import asyncio\nfrom reaper import scrape\n\nresult = asyncio.run(scrape(\n    url=\"https://www.facebook.com/reel/123456\",\n    proxy_server=\"http://proxy.ejemplo.com:8080\",\n    proxy_username=\"usuario\",     # opcional\n    proxy_password=\"contrase\u00f1a\",  # opcional\n))\n</code></pre>"},{"location":"user/proxy/#desde-la-terminal","title":"Desde la terminal","text":"<pre><code># Proxy HTTP b\u00e1sico\nreaper https://www.facebook.com/reel/123 \\\n    --proxy http://192.168.1.100:8080\n\n# Proxy con autenticaci\u00f3n\nreaper https://www.facebook.com/reel/123 \\\n    --proxy http://proxy.ejemplo.com:8080 \\\n    --proxy-user usuario \\\n    --proxy-pass contrase\u00f1a\n\n# Proxy SOCKS5\nreaper https://www.facebook.com/reel/123 \\\n    --proxy socks5://127.0.0.1:1080\n</code></pre>"},{"location":"user/proxy/#combinar-proxy-y-cuentas-autenticadas","title":"Combinar proxy y cuentas autenticadas","text":"<pre><code>result = await scrape(\n    url=\"https://www.facebook.com/reel/123\",\n    account_manager=manager,\n    proxy_server=\"http://proxy.ejemplo.com:8080\",\n)\n</code></pre>"},{"location":"user/proxy/#formatos-soportados","title":"Formatos soportados","text":"<pre><code>http://ip:puerto\nhttps://ip:puerto\nsocks5://ip:puerto\nhttp://usuario:contrase\u00f1a@ip:puerto\n</code></pre>"},{"location":"user/proxy/#verificar-que-el-proxy-funciona","title":"Verificar que el proxy funciona","text":"<pre><code>curl --proxy http://tu-proxy:puerto https://www.facebook.com\n</code></pre>"},{"location":"user/storage/","title":"Almacenamiento de cuentas","text":"<p>Reaper puede guardar tus cuentas (perfil, actividad y cookies de sesi\u00f3n) en tres backends distintos, elegibles desde un \u00fanico fichero de configuraci\u00f3n:</p> Backend Descripci\u00f3n Requiere <code>local</code> Archivos JSON en disco (comportamiento original) nada <code>postgres</code> PostgreSQL v\u00eda <code>asyncpg</code> <code>pip install \"reaper[db]\"</code> y un servidor PG <code>mongodb</code> MongoDB v\u00eda <code>motor</code> <code>pip install \"reaper[db]\"</code> y un servidor Mongo <p>La elecci\u00f3n es todo o nada: si eliges <code>postgres</code>, tanto los perfiles como las cookies de sesi\u00f3n viven en PostgreSQL. No hay mezcla de backends.</p>"},{"location":"user/storage/#donde-estan-mis-sesiones","title":"\u00bfD\u00f3nde est\u00e1n mis sesiones?","text":"<p>Con el backend <code>local</code>, cada cuenta ocupa un directorio propio:</p> <pre><code>data/accounts/\n\u251c\u2500\u2500 facebook/\n\u2502   \u2514\u2500\u2500 {account_id}/\n\u2502       \u251c\u2500\u2500 account.json   \u2190 perfil (sin cookies)\n\u2502       \u2514\u2500\u2500 cookies.json   \u2190 cookies de sesi\u00f3n (formato Playwright)\n\u2514\u2500\u2500 instagram/\n    \u2514\u2500\u2500 {account_id}/...\n</code></pre> <p>Rutas relativas</p> <p>Por defecto, <code>data/accounts</code> se resuelve respecto al directorio de trabajo desde el que se ejecuta el proceso. Si lanzas Reaper desde <code>$HOME</code>, las sesiones caen en <code>~/data/accounts</code>; desde <code>/tmp</code>, en <code>/tmp/data/accounts</code>. Para ubicaciones estables, indica una ruta absoluta en el fichero de configuraci\u00f3n.</p> <p>Con <code>postgres</code> o <code>mongodb</code>, las cuentas viven en la base de datos; no se crea ning\u00fan directorio local de cuentas.</p>"},{"location":"user/storage/#configuracion-fichero-reapertoml","title":"Configuraci\u00f3n (fichero <code>reaper.toml</code>)","text":"<p>Reaper busca <code>reaper.toml</code> autom\u00e1ticamente (primer hallazgo gana):</p> <ol> <li>Ruta expl\u00edcita: <code>AccountManager(storage=...)</code> o <code>build_storage(ruta)</code>.</li> <li>Variable de entorno <code>REAPER_CONFIG</code>.</li> <li><code>./reaper.toml</code> (directorio de trabajo actual).</li> <li><code>~/.config/reaper/reaper.toml</code>.</li> <li><code>/etc/reaper/reaper.toml</code>.</li> </ol>"},{"location":"user/storage/#local-por-defecto","title":"Local (por defecto)","text":"<pre><code>[storage]\nbackend = \"local\"\naccounts_dir = \"/var/lib/reaper/accounts\"   # ruta absoluta recomendada\n</code></pre>"},{"location":"user/storage/#postgresql","title":"PostgreSQL","text":"<pre><code>[storage]\nbackend = \"postgres\"\n\n[storage.postgres]\ndsn = \"postgresql://usuario:clave@localhost:5432/reaper\"\npool_size = 5        # conexiones del pool\nmax_overflow = 10    # conexiones extra bajo carga\ntimeout = 30.0       # segundos de espera de conexi\u00f3n\ntable = \"accounts\"   # nombre de la tabla (se crea sola al conectar)\n</code></pre>"},{"location":"user/storage/#mongodb","title":"MongoDB","text":"<pre><code>[storage]\nbackend = \"mongodb\"\n\n[storage.mongodb]\nuri = \"mongodb://localhost:27017\"\ndatabase = \"reaper\"\ncollection = \"accounts\"\n</code></pre>"},{"location":"user/storage/#uso-desde-python","title":"Uso desde Python","text":"<p>El <code>AccountManager</code> respeta autom\u00e1ticamente la configuraci\u00f3n:</p> <pre><code>import asyncio\nfrom reaper import AccountManager\n\nasync def main():\n    # Lee reaper.toml y usa el backend indicado (default: local)\n    manager = AccountManager()\n\n    cuenta = await manager.add_account(\n        platform=\"facebook\",\n        username=\"mi_usuario\",\n        cookies_file=\"exports/fb_cookies.json\",\n    )\n    # Todo el ciclo de vida (perfil + cookies) va al backend configurado.\n\n    await manager.close()   # libera pools de conexi\u00f3n (PG/Mongo)\n    return cuenta\n\nasyncio.run(main())\n</code></pre> <p>Tambi\u00e9n puedes instanciar el backend directamente o ignorar el fichero:</p> <pre><code>from reaper import AccountManager\nfrom reaper.auth.storage import PostgresStorage\n\nmanager = AccountManager(\n    storage=PostgresStorage(dsn=\"postgresql://user:pass@localhost:5432/reaper\")\n)\nawait manager.close()\n</code></pre>"},{"location":"user/storage/#notas-de-seguridad","title":"Notas de seguridad","text":"<ul> <li>Las cookies contienen tokens de autenticaci\u00f3n. En los backends de BD se   guardan como JSONB/BSON sin cifrar: protege el acceso a la base de datos   y no comprometas credenciales.</li> <li>Si <code>reaper.toml</code> contiene credenciales, restringe permisos:   <code>chmod 600 reaper.toml</code> \u2014 y no lo subas a control de versiones.</li> </ul>"}]}